# Current State

> Snapshot of what has actually shipped. Updated as code lands. This is the
> truth source for "where are we now". Forward-looking plans live in
> [../roadmap/tasks.md](../roadmap/tasks.md). Decisions intentionally not
> done live in [DEFERRED_FOLLOWUPS.md](DEFERRED_FOLLOWUPS.md).

> Last updated: 2026-06-05
> Stage: M4 agent sandbox hardening in progress; the current product direction
> remains intelligent alert analysis, with alert-first extension boundaries
> documented for future source/provider growth.

## Implementation Status

| Area | Status | Notes |
|------|--------|-------|
| Repository governance (LICENSE, GOVERNANCE, MAINTAINERS, etc.) | shipped | English-only documentation |
| ADRs 0001-0013 | shipped (proposed status) | technology stack and architecture decisions recorded; every ADR has schema-valid front matter plus a non-empty `Consequences` section enforced by `make adr-check`; future supersession links must be closed through matching `supersedes` / `superseded_by` front matter. |
| Phase docs 00-05 | shipped | each milestone has a phase document |
| Master flow (S0-S6) | shipped | stage-by-stage authority and failure boundaries documented |
| Alert-first signal extension boundary | shipped | `docs/design/alert-first-signal-extension.md` keeps OpenClarion's current product focus on intelligent alert analysis while documenting how provider, evidence, workflow, report, and sandbox-runtime boundaries can support future signal sources without renaming `AlertEvent` / `AlertGroup` during the MVP |
| CI: docs hygiene gate | shipped | `make docs-hygiene` rejects non-English CJK literals, project terminology drift, E2E proof-state drift, and stale `Last updated` metadata in governed documentation. The terminology wordlist is read only from a regular non-symlink policy file with non-symlink, directory-only parents, keeps product language on the intelligent alert analysis track, rejects generic platform positioning, and confines `SignalEvent` / `SignalGroup` / `CaseGroup` aliases to architecture boundary docs. The E2E verifier keeps status table verdicts defined in the Verdict Scale table, and `scripts/docs_metadata_check` keeps `Last updated` metadata aligned with dated status/changelog table rows. |
| CI: ADR governance gate | shipped | `make adr-check` validates ADR index/file consistency, monotonic IDs, schema-valid front matter, allowed statuses, file/H1/front-matter identity consistency, non-empty `Consequences` sections, supersession reference closure, and accepted ADR body immutability when `ADR_BASE_REF` is available. A `supersedes: ADR-NNNN` reference must point to an existing ADR whose status is `superseded` and whose `superseded_by` back-pointer includes the superseding ADR; PRs must supersede accepted ADRs instead of editing their decision body in place. Root `scripts` tests cover positive and negative front-matter / Consequences / supersession / accepted-body immutability fixtures. |
| CI: markdown link gate | shipped | `make links-check` validates governed Markdown relative links and same-file/cross-file Markdown `#anchor` fragments, and rejects orphan Markdown files under `docs/` that are not reachable from `docs/README.md`. Anchor checks compare links against generated heading slugs and explicit HTML `id` / `name` anchors, with root `scripts` tests covering valid anchors, missing-anchor failures, and orphan-document failures. |
| CI: external link inventory | shipped | `make external-links-check` inventories governed Markdown HTTP(S) links without network access for local reproducibility, rejecting symlink and non-regular governed Markdown inputs plus symlinked `docs/` subtrees before reading. `.github/workflows/external-links.yml` runs the opt-in live liveness check weekly and manually on the default branch, outside PR-blocking CI, while reserved example domains remain inventoried but skipped during live checks. |
| CI: markdown structure lint | shipped | `make markdownlint` runs pinned `markdownlint-cli2` from `web/package-lock.json` using regular non-symlink config `docs/design/ci/markdownlint/.markdownlint-cli2.jsonc` with non-symlink, directory-only parents; the initial rule set enforces heading-level increments, unordered-list indentation, and prose line length on governed Markdown without turning the gate into a broad historical style rewrite. |
| CI: workflow parity policy | shipped | `make workflow-parity` rejects inline shell, undeclared make targets, mutable action refs, missing action version comments, missing PR concurrency, `pull_request` secret references, `pull_request_target` workflows without an explicit reviewer-policy marker, missing top-level bash defaults, unpinned runners, permission scopes broader than `contents: read` without inline `parity-allow` justification, missing job permissions/timeouts, M0-M2 job timeouts above 15 minutes, unregistered workflow files, and duplicate workflow names. `go test ./scripts` includes temporary-repository negative fixtures for registry drift, invalid workflow filenames, missing workflow names, permission-scope drift, and PR secrets boundaries. |
| CI: linear history policy | shipped | `make linear-history-check` runs in the existing pull-request DCO job, checking the PR event-provided commit range for merge commits so branch review history stays rebase-oriented without adding another top-level CI check. It is excluded from local `make ci` because the authoritative range comes from the PR event; `make linear-history-check-test` covers PR range selection, missing-base fetches, missing SHA failures, upstream/local fallbacks, single-commit inspection, and merge-commit rejection. |
| CI: PR title policy | shipped | `make pr-title-check` runs on pull requests with `github.event.pull_request.title`, requires the Conventional Commits header shape `type(scope)!: description`, and caps titles at 120 characters. It is intentionally PR-context-only and excluded from local `make ci`; root `scripts` tests cover valid titles, invalid headers, missing titles, multiline rejection, and 120/121-character boundary behavior. |
| CI: PR description policy | shipped | `make pr-description-check` runs on pull requests and reads `pull_request.body` from `GITHUB_EVENT_PATH`, requiring non-empty `## Risk` and `## Rollback` sections. It is intentionally PR-context-only and excluded from local `make ci`; root `scripts` tests cover local env input, GitHub event JSON input, missing sections, empty sections, empty bodies, and fenced-code false positives. |
| CI: PR file count policy | shipped | `make pr-file-count-check` runs inside the existing pull-request PR impact reference job, reads `pull_request.changed_files` and labels from `GITHUB_EVENT_PATH`, enforces a default 50-file review cap, and allows only the explicit maintainer label `large-pr-approved` to override the cap without adding another top-level CI check. It is intentionally PR-context-only and excluded from local `make ci`; `make pr-file-count-check-test` covers event JSON, env/local inputs, PR-range fallback, invalid config, over-limit rejection, and override acceptance. |
| CI: PR template policy | shipped | `make pr-template-check` runs inside the existing gate-hardening job and keeps `.github/pull_request_template.md` aligned with the PR description gate by requiring non-empty `## Summary`, `## Risk`, `## Rollback`, `## Local verification`, and `## DCO` sections without adding another top-level CI check. Root `scripts` tests cover missing, empty, and fenced-code false positives. |
| CI: issue template policy | shipped | `make issue-template-check` runs inside the existing gate-hardening job and validates `.github/ISSUE_TEMPLATE/bug_report.md` and `.github/ISSUE_TEMPLATE/feature_request.md` front matter plus required non-empty triage sections, so issue intake labels, title prefixes, and reproduction/acceptance prompts stay stable without adding another top-level CI check. Root `scripts` tests cover front-matter drift, missing sections, empty placeholders, and fenced-code false positives. |
| CI: DCO sign-off policy | shipped | `make dco-check` runs on pull requests and validates non-merge commits in the PR range. The gate now requires a `Signed-off-by:` trailer whose email matches the commit author email, rejects GitHub `users.noreply.github.com` author emails, rejects commit messages/trailers with AI tool branding, and makes local no-upstream fallback validate `HEAD` instead of silently checking an empty range. `go test ./scripts` includes temporary git repositories for matching, missing, mismatched, noreply-author, AI-branded, and local-fallback cases. |
| CI: manual target isolation | shipped | `make manual-target-isolation` runs inside the existing gate-hardening job and validates `docs/design/ci/manual-targets.tsv`, requiring live/manual smoke and retained-evidence make targets to stay explicitly registered, documented, marked as manual in Makefile help, and unreachable from `make ci` or any GitHub Actions `run: make <target>` entry point without adding another top-level CI check. |
| CI: dependency pinning policy | shipped | `make forbidden-latest` rejects mutable Go/npm dependency pins, requires critical first-import Go modules (Ent, Temporal SDK, OTel) to remain direct concrete root-module pins without `replace`, requires external Go `tool` directive package paths to resolve to concrete `require` version pins, rejects undocumented Go `replace` directives unless `docs/design/DEPENDENCIES.md` is a regular non-symlink policy file with non-symlink, directory-only parents and carries a matching `replace-allow: <module> => <target>` marker with owner and expiry, and rejects committed Dockerfile external base images unless they are pinned to `@sha256:<digest>`. The gate permits `scratch` and previously declared build stages, matching the current custom thin runner Dockerfile while preventing future tag-only sandbox/runtime base images. |
| CI: Go license compliance | shipped | `make go-licenses-check` runs pinned `go-licenses v1.6.0` with test dependencies over the root Go package set and `tools/openclarion-linter`, ignores first-party package prefixes while still checking third-party dependencies, accepts only the SPDX IDs listed by `go-license-allow:` in `docs/design/DEPENDENCIES.md` after rejecting symlink/non-regular policy files and symlinked or non-directory policy-file parents, and validates that the policy marker has non-empty owner, non-future reviewed date, and reason metadata. |
| CI: Go checks gate | shipped | `make go-checks` (generate, vet, build, race-enabled `go test -count=1`) |
| CI: Go coverage floor | shipped | `make go-coverage` runs `scripts/check_go_coverage.sh` over selected handwritten Go packages, excluding generated API/Ent code and root script-test aggregators, and requires every covered package with statements to meet `GO_COVERAGE_MIN` (default 40.0%). Root `scripts` tests prove package filtering, below-floor rejection, and invalid-threshold rejection. |
| CI: integration-test environment contract | shipped | `make testcontainers-contract` parses real Go test files with `go/parser` / `go/ast` and rejects packages that import `database/sql` without a same-package `testcontainers-go` import. The gate also rejects direct host/public network entry points in Go tests, including `net/http.Get`, `net/http.DefaultClient`, and `net.Dial`, so tests use `httptest`, injected clients/transports, or Testcontainers-backed endpoints. Analyzer/source fixtures under `testdata/` remain excluded. |
| CI: SQLite test gate | shipped | `make forbidden-sqlite` rejects SQLite driver imports and SQLite DSN markers in Go test files, fails closed on symlinked or otherwise non-regular `_test.go` paths before scanning, rejects symlinked first-party test search directories, and root `scripts` tests cover no-test skip, allowed tests, import/DSN rejections, non-test Go exclusion, indirect-file rejection, and indirect-tree rejection. |
| CI: generated file header gate | shipped | `make generated-headers` validates that committed OpenAPI Go, Ent, and frontend OpenAPI TypeScript generated files retain their generator-owned headers and direct-edit warnings, with root `scripts` tests covering missing-header failures. |
| CI: generation freshness gate | shipped | `make generate-fresh` snapshots tracked plus non-ignored untracked repository files, runs `make generate`, rejects any generated-file drift, then runs `make generate` again to prove generation idempotence. Root `scripts` tests cover stable generation, tracked and untracked generator diffs, and second-run non-determinism. |
| CI: repository size budget | shipped | `make repo-size-check` scans Git-visible tracked and non-ignored untracked working-tree files through `git ls-files`, rejects symlink/non-regular paths, enforces default 1 MiB per-file / 25 MiB total budgets, and blocks common archive, executable, object, packaged JVM, WebAssembly, and database artifact extensions so retained evidence, generated blobs, and accidental binary files cannot grow or enter review silently. `make repo-size-check-test` covers deterministic ordering, unsafe paths, symlinks, blocked artifact extensions, per-file and total budget failures, and invalid budgets. CI runs the gate in the existing PR budget job so the workflow does not add another top-level check. |
| CI: OpenAPI checks gate | shipped | `make openapi-checks` (`vacuum` lint + `openapi-fresh`) |
| CI: OpenAPI contract hardening | shipped | W4 migration gates: `make openapi-breaking` (oasdiff `v1.11.7`, soft-fail until 2026-06-10; owner: CI maintainers; root `scripts` tests cover pre-sunset soft-fail, on/after-sunset hard-fail, non-breaking success, and invalid date fail-closed behavior), `make openapi-fingerprint` (`docs/design/ci/locks/openapi-critical.lock`, duplicate-key/multi-document/anchor/merge-key/alias spec YAML rejection, strict JSON lock parsing), and `make doc-claims-check` |
| CI: gate hardening maturity checklist | shipped | `make gate-hardening-check` validates `docs/design/ci/GATE_HARDENING_CHECKLIST.md` against `docs/design/ci/README.md`'s Progressive Gate Schedule so every activated gate has exactly one maturity record with an allowed level, concrete evidence, and a concrete next-hardening note. The checker rejects symlink/non-regular schedule or checklist inputs, including symlinked parent directories, before parsing. |
| CI: Go toolchain version consistency | shipped | `make go-toolchain-check` keeps root and first-party submodule `go.mod` directives on the same patch-pinned Go version, parses `.golangci.yml` and workflow YAML as single-document config without duplicate keys, anchors, aliases, or merge keys before reading the version fields it owns, keeps `.golangci.yml` on the corresponding major.minor language version, and rejects hard-coded `actions/setup-go` versions so CI reads `go-version-file: go.mod` as the single toolchain source. |
| CI: source comment debt tracking | shipped | `make comment-debt-check` runs `scripts/comment_debt_check`, which scans supported source comments for tracked debt markers while avoiding string-literal, generated-code, testdata, and ignored-path false positives. Supported source paths must be regular files, so symlinks are rejected before reading. Accepted tracking references match the PR impact policy: `#123`, GitHub issue/PR URLs, `ADR-0001`, or concrete ADR paths such as `docs/adr/ADR-0001-postgresql-single-source.md`. |
| CI: deferred follow-up ledger gate | shipped | `make deferred-followups-check` validates regular-file `docs/design/DEFERRED_FOLLOWUPS.md` input so deferral IDs stay contiguous and unique, active/closed sections match canonical status, required fields remain concrete, dates are valid and non-future, and unknown table fields are rejected. |
| CI: branch protection required-check policy | shipped | `make branch-protection-check` validates `docs/design/ci/branch-protection.required-checks.json` as the repository policy source for `main` required status checks. The policy must be strict JSON with branch `main`, strict status checks, GitHub Actions as the expected source app, sorted duplicate-free contexts, and exact coverage of every unique job name in PR-triggered workflows. CI runs it in the existing workflow-parity job instead of adding another top-level workflow check. |
| CI: Go license compliance | shipped | `make go-licenses-check` runs pinned `go-licenses v1.6.0` with `--include_tests` over the root Go package set and `tools/openclarion-linter`, ignores first-party package prefixes while still checking third-party dependencies, and accepts only the SPDX IDs recorded by `go-license-allow:` in `docs/design/DEPENDENCIES.md` after rejecting symlink/non-regular policy files and symlinked or non-directory policy-file parents. |
| CI: npm OSV lockfile scan | shipped | `make osv-scan` runs pinned `osv-scanner v1.9.2` against committed `package-lock.json` files outside `node_modules`, fails when first-party npm manifests lack lockfiles, and currently scans `web/package-lock.json` with zero findings. |
| CI: Temporal workflow tests | shipped | `make temporal-workflow-tests`; focused CI job runs Temporal integration tests with race detector and replay-verifies a completed `DiagnosisWorkflow` history, including the snapshot-bound startup path |
| CI: security and lint gates | shipped | W1-W3 migration gates: `make secrets-scan`, `make govulncheck`, and `make go-lint` with the `openclarion-arch` custom analyzer plugin (`forbiddenimports`, `txboundary`, `runtimemock`, `structuredlogging`, `httpclient`, `sqldb`, `execboundary`, `filesystemboundary`, `clockboundary`, `envboundary`, `reflectionboundary`, `panicboundary`, `goroutineboundary`). `runtimemock` rejects any production `cmd/openclarion` import of `internal/providers/*/fake`, covering both metrics and LLM fakes. `structuredlogging` rejects direct `fmt.Print*` and standard-library `log.Print*` / `log.Fatal*` / `log.Panic*` calls in non-test production packages while allowing CLI/script/generated Ent code. `httpclient` rejects `http.DefaultClient` and bare `http.Get` / `http.Head` / `http.Post` / `http.PostForm` in non-test production packages, forcing outbound HTTP through request-scoped contexts and injected clients. `sqldb` rejects direct `database/sql.Open` calls outside the persistence repository / generated Ent boundary. `execboundary` rejects direct `os/exec.Command` / `os/exec.CommandContext` calls outside `cmd/`, repository `scripts/`, and the sandbox boundary, keeping production alert-analysis code behind Provider or sandbox-runtime execution paths. `filesystemboundary` rejects direct local filesystem reads, metadata/directory probes, writes, creates, removes, renames, links, temp paths, and local filesystem adapters in non-test domain/usecase packages. `clockboundary` rejects direct `time.Now()` calls in non-test domain/usecase packages so core alert-analysis logic remains replayable and receives time from inputs, workflow/activity boundaries, or injected clocks. `envboundary` rejects direct process-environment reads/mutations and process flag parsing in non-test domain/usecase packages so configuration stays in boundary wiring and injected DTOs. `reflectionboundary` rejects `reflect` and `unsafe` imports in handwritten production files while allowing tests and generated code, keeping escape hatches out of the alert-analysis control plane unless a reviewed infra boundary is introduced. `panicboundary` rejects handwritten production `panic()` calls outside `main`, `init`, or explicit recover boundaries; alert ingestion/grouping now return errors for canonical JSON failures instead of panicking. `goroutineboundary` rejects raw `go` statements in handwritten production files outside `internal/supervision`; `cmd/openclarion` now runs its HTTP server lifecycle through `errgroup.WithContext`. `make forbidden-agent-runtime` reads `docs/design/ci/agent-runtime-forbidden.tsv` and rejects both configured first-party control-plane agent-framework dependencies and configured non-test first-party source runtime-family hardcoding under `cmd/`, `internal/`, `scripts/`, and `web/src/` before the M4 runtime-selection evidence gate accepts a baseline. `make govulncheck` covers each first-party Go module, including `tools/openclarion-linter`; Dependabot watches `/`, `/tools/openclarion-linter`, and `/web`. W3-2b retired the legacy forbidden-imports bash guard after analyzer equivalence verification with one `analysistest` red fixture for every retired legacy deny-list prefix. |
| CI: report live proof gate | shipped | `make report-live-smoke-output-test` covers `scripts/report_live_smoke_output`, the offline checker for retained `make report-live-smoke` artifacts. The checker rejects symlink/non-regular proof files, duplicate JSON object keys, unknown proof fields, log-polluted JSON, missing, non-canonical, or future `checked_at` timestamps, non-canonical replay-window timestamps, invalid replay request metadata, workflow ID mismatches when requested, failed ingest/replay stats, replay stat/snapshot mismatches, empty or duplicate snapshots, non-contiguous snapshot group indexes, duplicate SubReport IDs, snapshot/SubReport count mismatches, missing or mismatched `notification_idempotency_key`, malformed provider message IDs when supplied, and notification statuses other than `accepted` or `delivered`. |
| CI: diagnosis-room proof gates | shipped | M5 focused gates include `make diagnosis-room-policy-test`, `make diagnosis-room-workflow-test`, `make diagnosis-auth-test`, `make diagnosis-chat-persistence-test`, and `make diagnosis-live-smoke-output-test`. Workflow tests now cover duplicate-key-free starter/workflow evidence input before room lifecycle execution and duplicate-key/trailing-value rejection for lifecycle audit payload JSON before any `DiagnosisTaskEvent` append. The last target validates `scripts/diagnosis_live_smoke_output` so real-stack browser evidence has a machine-checked JSON shape, including symlink/non-regular proof-file rejection, duplicate-key and unknown-field rejection, canonical scalar fields, request metadata, submitted-message SHA-256 digest binding, and structured browser observations, before it can support M5 acceptance. |
| Go module | shipped | `github.com/openclarion/openclarion`, Go 1.25.11, oapi-codegen-exp v0.1.0, vacuum (tools) |
| OpenAPI spec | shipped | `api/openapi.yaml` (3.1.0, healthz endpoint plus domain paths: `GET /api/v1/dashboard`, `GET /api/v1/alerts`, `GET /api/v1/evidence-snapshots`, `GET /api/v1/reports`, `GET /api/v1/reports/{report_id}`, `POST /api/v1/report-triggers/replay-window`, `POST /api/v1/diagnosis/rooms`, and `POST /api/v1/diagnosis/ws-ticket`) |
| `oapi-codegen-exp` toolchain | shipped | V3, std net/http ServerInterface, `go generate` chain |
| `vacuum` OpenAPI lint | shipped | `go tool` dependency, `--fail-severity error`, real blocking gate with project ruleset `docs/design/ci/vacuum/.vacuum.yaml` |
| HTTP API | shipped | `internal/transport/http/server.go` implements generated handlers for recent alert events, evidence snapshots, final report lists, final report detail, and replay-window report triggering. Read APIs use bounded `limit` query params (1..500, default 100); report detail responses include linked SubReports with `evidence_snapshot_id` for M3 evidence traceability. The report trigger endpoint accepts a replay window, optional workflow/correlation/scenario overrides, uses default alert grouping, returns replay stats and workflow handle, and returns 503 when trigger wiring is not configured. |
| Observability metrics | shipped (M3 backend foundation) | `internal/observability/metrics` owns an isolated Prometheus registry with Go/process collectors plus `openclarion_http_requests_total`, `openclarion_http_request_duration_seconds`, and `openclarion_http_in_flight_requests`. `cmd/openclarion` serves `GET /metrics` outside the OpenAPI contract and instruments generated API routes with one stable `handler="api"` label plus method/code labels, avoiding raw-path high cardinality. Tests verify scrape output and request metric emission. |
| Observability tracing | shipped (M3 backend foundation) | `internal/observability/tracing` initializes W3C TraceContext+Baggage propagation with a no-op default and opt-in OTLP HTTP exporter when `OTEL_EXPORTER_OTLP_ENDPOINT`, `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT`, or `OTEL_TRACES_EXPORTER=otlp` is configured. OTLP endpoint env values are prevalidated to reject URL userinfo before exporter construction. `cmd/openclarion` wraps generated API routes with `otelhttp` using ServeMux route-pattern span names such as `GET /api/v1/reports/{report_id}` so spans avoid raw-path cardinality. `HTTPTracing.Transport` and `HTTPTracing.HTTPClient` instrument outbound HTTP with the same tracer provider and propagator, while preserving bounded `X-Request-ID` propagation. Runtime Prometheus, OpenAI-compatible LLM, and Webhook clients use those wrappers when tracing is configured. Runtime Temporal clients install the Temporal OpenTelemetry tracing interceptor when tracing is enabled, reusing the same tracer provider and propagator for workflow/activity trace propagation. `OTEL_SERVICE_NAME` and `OPENCLARION_SERVICE_VERSION` populate resource attributes, and shutdown flushes the tracer provider. Tests use an in-memory SDK exporter without mutating global OTel state, prove outbound `traceparent` propagation, run an OTLP HTTP collector smoke that receives exported spans, reject userinfo-bearing OTLP endpoints, and prove workflow context carries a Temporal OTel span. |
| HTTP correlation and access logging | shipped (M3 backend foundation) | `internal/observability/correlation` provides generated-or-preserved bounded `X-Request-ID` handling, `LogAttrs(ctx)` for `request_id`, OTel `trace_id`, and `span_id`, plus a request-id `RoundTripper` for outbound HTTP clients. `internal/observability/accesslog` emits one structured `http access` log per generated API request with request/trace/span correlation, method, stable ServeMux route pattern, status, duration, and response bytes; it intentionally does not log the raw request target or query string. `cmd/openclarion` installs tracing outside correlation/access logging so access records can see the active server span and request ID. Prometheus, OpenAI-compatible LLM, and Webhook provider default clients propagate `X-Request-ID` from context. Tests cover valid/invalid request IDs, trace/log attributes, access log shape, no raw target leakage, provider header propagation, and HTTP error logging through context-aware `slog.LogAttrs`. |
| Docker Compose (PostgreSQL + Temporal) | shipped | tag-pinned (`postgres:18-alpine`, `temporalio/auto-setup:1.25.2`); digest pin deferred to M4 sandbox |
| Health endpoint | shipped | `GET /healthz` returns 200 with `{"status":"ok"}` |
| Ent toolchain | shipped (M1-PR1) | `entgo.io/ent v0.14.6` direct require + `tool` directive; `make ent-generate` / `make ent-fresh` |
| Atlas toolchain | shipped (M1-PR1) | `arigaio/atlas:1.2.0` Docker image pin; wrapper landed in `scripts/lib_atlas.sh` plus three thin entry scripts: host launches per-invocation `postgres:18-alpine` on a dedicated Docker network, Atlas container mounts host Go toolchain read-only at `/usr/local/go`, runs as `$(id -u):$(id -g)`, talks to dev DB via plain `postgres://`. `make atlas-migrate-diff` / `make atlas-drift` / `make atlas-smoke` targets shipped; `make atlas-smoke` verified locally 2026-05-22 (produced 2 files, clean cleanup); `make atlas-drift` runs in CI with `actions/setup-go` so the host Go toolchain is mounted into the Atlas container. |
| Ent schemas | shipped (M1-PR1) | 5 entities landed: `AlertEvent`, `AlertGroup`, `EvidenceSnapshot`, `DiagnosisTask`, `DiagnosisTaskEvent`. AlertEvent <-M2N-> AlertGroup; AlertGroup -1:N-> EvidenceSnapshot; EvidenceSnapshot -1:N-> DiagnosisTask; DiagnosisTask -1:N-> DiagnosisTaskEvent. All entities use bigserial PK; FK columns are surfaced as explicit `field.Int` (`alert_group_id` / `evidence_snapshot_id` / `task_id`) so composite-index column ordering matches docs intent. Constraint set after 2026-05-22 review: `EvidenceSnapshot.digest` is **per-group** unique on `(alert_group_id, digest)` (NOT cross-row global, since two distinct groups MAY produce identical canonical payloads); `DiagnosisTask` natural identity is `(workflow_id, run_id)` (NOT `workflow_id` alone), with `run_id` NOT NULL + immutable, so Temporal retries that spawn a new `run_id` are NEW rows. First migration `20260525060310_initial_schema.sql` committed; `make atlas-drift` reports synced. |
| Temporal Go SDK / workflows | shipped (M1) | `go.temporal.io/sdk v1.44.0` pinned via first-import rule. `DiagnosisWorkflowInput` now carries `TaskID` and `EvidenceSnapshotID`; new workflow histories require both to be non-zero, while `workflow.GetVersion("diagnosis-workflow-snapshot-bound-start", DefaultVersion, 1)` keeps older histories replayable. The workflow registers the `"record-event"` Update before startup completes, but the handler `workflow.Await`s the snapshot-bound start gate before it can append events. `StartDiagnosisTask` validates that the task row belongs to the input snapshot and idempotently transitions the task to `running`; mismatched or missing rows are non-retryable invariant failures. `RecordEventRequest` has no TaskID field, so writes remain bound to the workflow's task; its validator pre-history rejects empty Kind / empty `dedupe_key`. The `RecordDiagnosisEvent` activity runs the three-transaction idempotent producer chain (pre-check via `FindEventByTaskAndDedupeKey` -> attempt insert -> on `ErrAlreadyExists` re-fetch in a fresh tx). Permanent failures use `temporalsdk.NewNonRetryableApplicationError` with `RetryPolicy.NonRetryableErrorTypes={InvalidInput,InvariantViolation}`. `NewWorker` wires workflow + activities on the `"openclarion"` task queue; `cmd/openclarion/main.go` starts the Temporal client + worker alongside the HTTP server. Integration tests via `testsuite.StartDevServer` + testcontainers Postgres cover: snapshot-bound startup sets task status `running`; happy-path Update round-trip writes a `DiagnosisTaskEvent` row; validator rejects empty Kind / empty `dedupe_key` with no event row; workflow-bound task isolation; same-`dedupe_key` idempotency; zero `TaskID`; zero `EvidenceSnapshotID`; mismatched `EvidenceSnapshotID`; and completed-history replay with `worker.NewWorkflowReplayer`. |
| MetricsProvider + Prometheus + fake + ingestion library | shipped (M1-PR4) | `MetricsProvider` port + `ActiveAlert` DTO in `internal/usecases/ports/providers.go` (firing-only contract; provider implementations MAY import third-party SDKs, the port itself stays domain + stdlib only). `prometheus.NewProvider` in `internal/providers/metrics/prometheus/` wraps `github.com/prometheus/client_golang v1.23.2`'s `api/prometheus/v1` client, rejects userinfo-bearing endpoint URLs before constructing the upstream client, drops non-firing alerts before mapping `model.LabelSet -> map[string]string`, and exposes Bearer auth via `WithBearer` backed by `github.com/prometheus/common/config.NewAuthorizationCredentialsRoundTripper`. Its internal transport caps parsed response bodies at 4 MiB and rejects duplicate-key or trailing-value JSON before `client_golang` parses 2xx success envelopes or Prometheus API 400/422 error envelopes, while leaving other non-2xx handling to the upstream client. `fake.New` in `internal/providers/metrics/fake/` returns a double-deep-copied slice so test seeds and consumer mutations stay isolated. `alertingest.IngestOnce` (`internal/usecases/alertingest/`) queries the provider once, persists each `ActiveAlert` as a `domain.AlertEvent` inside its own `UnitOfWork` transaction (so `ErrAlreadyExists` rolls back the tx and is counted as `Duplicate`), aggregates remaining failures via `errors.Join`, and reports `Stats{Total,Saved,Duplicate,Failed}` with `Total = len(provider output)`. Fingerprints come from a sorted-JSON canonical form: `SourceFingerprint` and `CanonicalFingerprint` both use SHA-256 over the nil-normalised label map so empty-vs-nil labels collapse to the same key. Per-alert warning logs only carry `source` / `starts_at` / `canonical_fingerprint` / `error` (no labels / annotations / raw payload). Integration tests in `internal/usecases/alertingest/` cover save / duplicate-rollback / invariant-violation paths against testcontainers Postgres; `client_golang` httptest tests cover envelope decoding, endpoint userinfo rejection, strict parsed-envelope rejection, oversized parsed-response rejection, firing-state filter, Bearer-header injection, and 5xx / malformed-JSON wrapping. Runtime report trigger wiring now exists through the M2 HTTP replay endpoint and CLI one-shot command; scheduled workflow triggers remain future scope. |
| Deterministic grouping algorithm | shipped (M1-PR5) | Pure function `GroupEvents([]AlertEvent, Config) ([]AlertGroup, error)` in `internal/usecases/alertgrouping/`; groups by configurable dimension keys (default `alertname`), sha256 group key, max severity aggregation, stable output ordering. No persistence orchestration in this PR. |
| EvidenceSnapshot builder | shipped (M1-PR6) | Pure function `BuildSnapshot(Input) (EvidenceSnapshot, error)` in `internal/usecases/evidencebuild/`; deterministic payload JSON with canonical nested `json.RawMessage`, sha256 digest, static provenance (`openclarion.core`), status=complete. Expanded input validation covers zero IDs / empty strings / invalid times / severity / duplicate-key-free dimensions / duplicate-key-free RawPayload / out-of-range events / duplicate or set-mismatched event IDs; unit tests cover determinism, canonical JSON key-order/whitespace invariance, duplicate-key and trailing-value rejection, nil rendering, validation boundaries, provenance/status, and digest stability. No provider calls, no persistence orchestration. |
| Alert window replay harness | shipped (M1-PR7 local) | Orchestrator `ReplayWindow(ctx, provider, factory, Request) (Stats, error)` in `internal/usecases/alertreplay/`. Steps: (1) `alertingest.IngestOnce`; (2) short read tx `AlertRepository.ListEventsByStartsAtRange(WindowStart, WindowEnd, Limit+1)` over the half-open `[WindowStart, WindowEnd)` interval (Limit+1 is a safety valve -- `> Limit` rows aborts the run with `ErrInvariantViolation` after writing `Stats.EventsLoaded` so callers can diagnose); (3) `alertgrouping.GroupEvents`; (4) reconstruct each draft's events from a local id->event map (missing id -> invariant violation at orchestrator boundary); (5) per-group pipeline transaction running `FindGroupByNaturalKey` -> save vs refresh-vs-existing diff (severity / event_count / last_seen_at) -> `LinkEventsToGroup` -> `BuildSnapshot` (with `snapshotGroup.EventIDs = draft.EventIDs` so the group-vs-events ID set check runs) -> `FindByGroupAndDigest` -> `Save` only when not found -> `UpdateGroup(g.Close())` only when status was active. Per-group failures are joined via `errors.Join` and counted as `Failed`; they do NOT abort the rest of the run. `AlertRepository` ports gained two methods this PR: `ListEventsByStartsAtRange` and `FindGroupByNaturalKey`; both reject zero/blank inputs and (for the range method) compare end > start AFTER `NormalizeUTCMicro` so a sub-microsecond gap that the normaliser collapses cannot slip past as a valid window. D7 closed->refresh tension: refresh allows updating mutable fields of an already-closed group when the same `(group_key, first_seen_at)` natural key recurs, but status is preserved (no reopen); a window whose `first_seen_at` differs creates a NEW row by design. Integration tests via testcontainers Postgres cover happy path / rerun idempotency / out-of-window exclusion / multi-alertname grouping / close-after-snapshot / refresh-on-new-event-in-existing-group / Limit safety valve / request validation table (nil provider/factory, zero/inverted/normalised-collapse window, Limit <= 0, Limit == math.MaxInt) / empty grouping config rejected when events exist / empty window short-circuit. M2 HTTP and CLI one-shot triggers now consume this harness; scheduled workflow triggers remain future scope. |
| LLMProvider | shipped (M2 local contract) | Provider-neutral structured JSON DTOs and `LLMProvider.GenerateJSON(ctx, LLMRequest)` live in `internal/usecases/ports/providers.go`. The contract carries messages, output schema bytes, schema ID, idempotency key, raw JSON content, finish_reason, refusal, output mode (`json_schema` vs `json_object`), and model metadata so report-generation code can validate/refuse/truncation-check before persistence. Deterministic scripted fake provider lives in `internal/providers/llm/fake`, deep-copies scripted responses and returns, rejects missing idempotency keys, supports per-key retry scripts, and is covered by unit tests. Report Activities consume the port through worker injection; `cmd/openclarion` wires the OpenAI-compatible provider when `OPENCLARION_LLM_MODEL` is set, with optional `OPENCLARION_LLM_BASE_URL` and `OPENCLARION_LLM_API_KEY`. The provider accepts only absolute HTTP(S) base URLs and rejects URL userinfo so API credentials stay on the explicit API-key field. |
| OpenAI-compatible LLM provider | shipped (M2 local provider) | `internal/providers/llm/openai/` implements a thin Chat Completions HTTP client for `LLMProvider`. It sends `response_format: json_schema` with `strict:true` when configured for strict structured outputs, falls back to `json_object` when startup capability probing receives an unsupported strict-schema error, maps `choices[0].message.content`, `message.refusal`, `finish_reason`, model, and selected output mode into `ports.LLMResponse`, and sets `X-Client-Request-Id` from the request idempotency key. The provider bounds successful response bodies at 4 MiB, rejects duplicate-key or trailing-value response envelopes before unmarshalling, rejects ambiguous JSON API error envelopes before capability fallback decisions, and still ignores unknown provider metadata for compatibility. The provider does not validate output, retry, persist, or notify. `httptest` coverage verifies strict request shape, JSON mode fallback request shape, refusal mapping, API error wrapping, ambiguous/oversized response-envelope rejection, plain-text API errors, invalid request preflight including non-HTTP(S) and userinfo-bearing base URLs, capability probe success/fallback, ambiguous capability-error rejection, and non-capability probe error propagation. Runtime wiring in `cmd/openclarion` uses capability detection before injecting the provider into Temporal Activities. |
| LLM output validation | shipped (M2 local framework) | `internal/usecases/llmoutput/` compiles caller-supplied JSON Schema with `github.com/santhosh-tekuri/jsonschema/v6 v6.0.2` (Draft 2020-12 default), rejects missing/invalid schemas, unsupported output modes, model refusals, non-`stop` finish reasons (including truncation), empty/invalid JSON content, and schema violations, then returns an accepted raw JSON copy plus parsed instance. Rejection errors carry a reason and retryability flag consumed by `internal/usecases/llmretry/`. Unit tests cover strict `json_schema`, `json_object` fallback, refusal, truncation, invalid JSON, schema violations, invalid schema metadata, unsupported output mode, and copy isolation. |
| LLM retry orchestration | shipped (M2 local framework) | `internal/usecases/llmretry/` runs bounded provider attempts around `LLMProvider.GenerateJSON` and `llmoutput.Validate`. Default max attempts is 3; validation failures marked retryable append a user feedback message containing the validation error before the next attempt. Non-retryable refusal / invalid request / unsupported provider metadata, provider call errors, and context cancellation stop immediately. Attempt history records response, reason, and error for later Activity error mapping. |
| Report draft contracts | shipped (M2 local framework) | `internal/usecases/reportdraft/` defines typed SubReport and FinalReport draft contracts plus OpenAI strict structured-output compatible JSON Schemas. Parse helpers validate provider responses through `llmoutput`, reject extra/missing fields and invalid enums, return defensive schema copies, and apply local semantic bounds for string length and list cardinality before Ent persistence accepts a report payload. Tests prove valid drafts parse, invalid drafts reject as retryable schema violations, schemas keep `additionalProperties:false` with all properties required, root schemas are objects, and unsupported strict-schema keywords do not enter the provider-facing schema. |
| Report prompt templates | shipped (M2 local framework) | `internal/usecases/reportprompt/` builds provider-neutral `LLMRequest` values for SubReport and FinalReport generation. SubReport templates cover `single_alert`, `cascade`, and `alert_storm`; every request carries explicit JSON-only instructions for `json_object` fallback safety, the correct draft schema copy, and a deterministic idempotency key shaped as `snapshot:<id>/group:<index>/sub_report`. FinalReport prompts reduce validated SubReports behind a caller-supplied correlation key. Structural tests validate prompt/request shape and fixture outputs against the report draft schemas without calling a real provider. |
| Report persistence | shipped (M2 local persistence) | `internal/domain/report.go` defines SubReport/FinalReport domain constructors, report severity/confidence vocabularies, notification delivery lifecycle states, and duplicate-key-free retained JSON invariant checks for report content and delivery metadata. Ent schemas `internal/persistence/ent/schema/subreport.go`, `internal/persistence/ent/schema/finalreport.go`, and `internal/persistence/ent/schema/reportnotificationdelivery.go` persist immutable report rows, the `final_report_sub_reports` fan-in edge, and `report_notification_deliveries` rows keyed by global notification idempotency key. Atlas migrations `internal/persistence/migrations/20260528022647_report_schemas.sql` and `internal/persistence/migrations/20260528034745_report_notification_deliveries.sql` create the report tables. `internal/usecases/ports/persistence.go` exposes `ReportRepository`, and `internal/persistence/repository/report_repo.go` implements per-snapshot SubReport idempotency, global FinalReport idempotency, notification delivery save/update/query, linked SubReport listing, and newest-first report listing under UoW `Reports()`. Tests in `internal/domain/report_test.go` and `internal/persistence/repository/report_repo_test.go` cover constructors, duplicate-key mapping, edge links, delivery lifecycle persistence, ordering, retained JSON duplicate-key/trailing-value rejection, and boundary invariant errors. |
| Report Temporal workflows | shipped (M2 local orchestration) | `internal/orchestrator/temporal/report_workflow.go` adds `ReportFanOutWorkflow`, `ReportBatchWorkflow`, and `FinalReportWorkflow`; `internal/orchestrator/temporal/activities.go` adds `GenerateSubReport`, `GenerateFinalReport`, and `SendReportNotification`. Generation Activities load persisted evidence/subreports, build provider-neutral prompts, run `llmretry.GenerateValidated`, parse accepted drafts through `reportdraft`, persist immutable report rows through `ReportRepository`, and pre-check idempotency keys before any repeat LLM call. `SendReportNotification` creates or reuses one `ReportNotificationDelivery` row before calling IMProvider, marks it delivered/failed afterwards, and skips the provider when a delivered row already exists. `ReportBatchWorkflow` starts one `ReportFanOutWorkflow` child per EvidenceSnapshot, preserves input order when collecting SubReport IDs, and then starts `FinalReportWorkflow`; `FinalReportWorkflow` calls `SendReportNotification` only after `GenerateFinalReport` returns a persisted `FinalReportID`. `internal/orchestrator/temporal/worker.go` registers all report workflows and supports `WithLLMProvider` / `WithIMProvider` for report-capable workers; `cmd/openclarion` wires those providers from env when configured. Tests in `internal/orchestrator/temporal/report_workflow_test.go` cover Activity persistence/idempotency, delivery-log success/failure behaviour, notification ordering input, parent-child batch fan-out/fan-in, workflow Activity scheduling, and completed-history replay for ReportFanOutWorkflow, FinalReportWorkflow, and ReportBatchWorkflow via `worker.NewWorkflowReplayer`. Scheduled trigger surface remains pending. |
| Report trigger framework | shipped (M2 local trigger framework) | `alertreplay.ReplayWindowForReport` returns `Result{Stats,Snapshots}` so downstream dispatch can use persisted EvidenceSnapshot IDs while `ReplayWindow` remains counter-only for existing callers. `internal/usecases/ports/orchestration.go` defines `ReportWorkflowStarter` and start DTOs without importing Temporal into usecases. `internal/orchestrator/temporal/report_starter.go` maps those DTOs to `ReportBatchWorkflowInput` and starts `ReportBatchWorkflow` with stable workflow ID, task queue, timeouts, `WORKFLOW_ID_REUSE_POLICY_REJECT_DUPLICATE`, and `WORKFLOW_ID_CONFLICT_POLICY_USE_EXISTING` so duplicate trigger attempts return a workflow handle instead of blindly starting another run. `internal/usecases/reporttrigger` builds deterministic correlation/workflow IDs from replay windows, validates scenarios and snapshot refs, and exposes `ReplayAndStart`. `POST /api/v1/report-triggers/replay-window` calls this service when `cmd/openclarion` configures `OPENCLARION_PROMETHEUS_URL`; optional `OPENCLARION_PROMETHEUS_BEARER_TOKEN` is passed to the Prometheus provider. `openclarion report-replay` exposes the same one-shot path for operators: it accepts `--window-start`, `--window-end`, optional `--limit`, `--correlation-key`, `--workflow-id`, `--scenario`, `--wait`, and `--wait-timeout`, then prints `checked_at`, sanitized replay request metadata, replay stats, snapshot refs, Temporal workflow handle JSON, and, when waiting, the completed ReportBatchWorkflow result including `notification_idempotency_key`. `make report-live-smoke` wraps this command for manual live verification and runs `scripts/report_live_smoke_output` to require canonical UTC timestamps, a non-future `checked_at`, a valid replay window not after `checked_at`, supported scenario, positive limit, explicit wait intent, snapshot refs, SubReport IDs, a persisted FinalReport ID, a matching `final_report:<id>/notification` idempotency key, a clean provider message ID when upstream supplies one, and a successful notification status (`accepted` or `delivered`). Unit tests cover saved and duplicate snapshot refs, request mapping, default IDs, validation, Temporal start options, HTTP trigger behavior, CLI argument/request/JSON/wait-result mapping, live-smoke output validation, and env-driven trigger wiring. Scheduled triggers and live Prometheus->Temporal->Webhook E2E execution evidence remain pending. |
| Report HTTP APIs | shipped (M2 local read + trigger path) | `api/openapi.yaml` and generated `api/openapi.gen.go` now define `GET /api/v1/reports`, `GET /api/v1/reports/{report_id}`, and `POST /api/v1/report-triggers/replay-window`. `internal/transport/http/server.go` maps `ReportRepository.ListFinalReports`, `FindFinalReportByID`, and `ListSubReportsForFinalReport` into generated response types, and maps trigger requests into `reporttrigger.Request`. Final report detail includes operator-facing fields, original accepted JSON content, and linked SubReports with `evidence_snapshot_id`; trigger responses include replay stats, EvidenceSnapshot refs, and workflow handle. HTTP tests cover list, detail, 404, bounded-list validation, trigger success mapping, invalid trigger request rejection, and unconfigured 503 behavior. |
| IMProvider + Webhook | shipped (M2 local provider, reused by M5) | `internal/usecases/ports/providers.go` now defines `IMProvider`, `IMNotification`, `IMDelivery`, and retry-classified `IMError`. `IMNotification` supports FinalReport and DiagnosisTask notification subjects so M2 report notifications and M5 diagnosis-room close notifications share the same provider without forcing diagnosis rooms through report-specific IDs. Deterministic fake provider lives in `internal/providers/im/fake` with per-idempotency-key scripts and request recording. Webhook provider lives in `internal/providers/im/webhook`; it posts JSON with `X-OpenClarion-Idempotency-Key`, `X-OpenClarion-Final-Report-Id` when a FinalReport subject is present, and `X-OpenClarion-Diagnosis-Task-Id` when a DiagnosisTask subject is present. It supports optional Bearer auth, rejects URL userinfo in the webhook endpoint, uses context-bound `http.NewRequestWithContext`, maps 2xx responses to deliveries, bounds response bodies at 1 MiB with overflow detection, rejects non-object, duplicate-key, or trailing-value JSON success responses before retaining provider raw metadata, preserves unknown success metadata for compatibility, and classifies 429/5xx/network failures as retryable. `cmd/openclarion` injects this provider when `OPENCLARION_IM_WEBHOOK_URL` is set, with optional `OPENCLARION_IM_WEBHOOK_BEARER_TOKEN`. Tests cover header/body shape, delivery response parsing, ambiguous/oversized response rejection, status retryability, userinfo-bearing webhook URL rejection, invalid config/input, fake provider copy isolation, runtime env wiring, and Activity-level delivery-log persistence. |
| Report local E2E | shipped (M2 local verification) | `internal/e2e/report_pipeline_test.go` starts testcontainers Postgres, a Temporal dev server, a real OpenClarion Temporal worker, and generated HTTP handlers. It drives `POST /api/v1/report-triggers/replay-window` against a Prometheus-compatible `httptest` server, uses the OpenAI-compatible and Webhook provider implementations against `httptest` endpoints, waits for `ReportBatchWorkflow` completion, then asserts webhook delivery plus persisted FinalReport/SubReport/ReportNotificationDelivery rows. Live external Prometheus->Temporal->Webhook verification remains pending. |
| Report live smoke gate | shipped (M2 manual verification harness) | `scripts/run_report_live_smoke.sh` and `make report-live-smoke` provide a manual acceptance gate for real environments. Required inputs are `DATABASE_URL`, `TEMPORAL_HOST_PORT`, `OPENCLARION_PROMETHEUS_URL`, `REPORT_WINDOW_START`, and `REPORT_WINDOW_END`; the script also requires local LLM/Webhook env evidence or `REPORT_LIVE_SMOKE_ASSUME_WORKER_READY=1` for externally managed workers. `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=report-live-smoke` now rejects malformed Prometheus/Webhook URLs, non-canonical or future replay windows, unsupported scenarios, non-positive limits, invalid wait timeouts, and unsafe optional workflow/correlation IDs without printing values. The live smoke calls `openclarion report-replay --wait`, writes JSON to `REPORT_LIVE_SMOKE_OUTPUT` or a temp file, and `scripts/report_live_smoke_output` rejects output that lacks canonical UTC timestamps, a non-future `checked_at`, valid replay request metadata, snapshot refs, waited workflow result, SubReport IDs, FinalReport ID, matching notification idempotency key, or a successful notification status (`accepted` or `delivered`); provider message IDs are checked for clean formatting when the upstream supplies one. This is not a CI gate and does not close live external E2E until a real run is captured. |
| Frontend (Next.js) | shipped (M3 read-only operations slice + M5 diagnosis-room route) | `web/` is a Next.js 16.2.6 / React 19.2.6 App Router project. OpenAPI TypeScript types are generated into `web/src/lib/api/openapi.ts` from `api/openapi.yaml`; feature code imports those generated schema types instead of hand-written DTOs. Thin route pages under `web/src/app/` render `/dashboard`, `/reports`, `/reports/[reportId]`, and `/diagnosis-room` through feature modules. The dashboard shows recent alert counters and report delivery success rate from `GET /api/v1/dashboard`; report detail includes linked SubReports with `evidence_snapshot_id` for evidence traceability. The diagnosis room route keeps ticket issuance and WebSocket frame handling inside `web/src/features/diagnosis-room/`, uses generated OpenAPI types for `POST /api/v1/diagnosis/ws-ticket`, and keeps non-OpenAPI WebSocket frames local to that feature. Frontend CI targets now cover `npm ci`, TypeScript, ESLint, Vitest unit tests, production build, Playwright route smoke against a mocked OpenClarion API/WebSocket endpoint, Knip dead-code scan, `npm audit --audit-level=high`, and OpenAPI TS freshness. `make diagnosis-live-browser-smoke` is available as a manual live browser proof harness for a real backend/worker stack; its Playwright test writes structured browser observations that the retained JSON proof validator checks before reporting success. Fuller UI polish and real-stack live proof remain pending. |
| ContainerProvider sandbox | partial (M4 contract + security spec + Engine lifecycle) | `internal/usecases/ports/container.go` defines the provider-neutral `ContainerProvider.Run(ctx, ContainerRunRequest)` boundary shared by M4 batch sandbox analysis and M5 per-turn diagnosis. The contract fixes ADR-0013 workspace paths (`/workspace/evidence.json`, `/workspace/conversation.json`, `/workspace/message.json`, `/workspace/agent_config`, `/workspace/out/output.json`), applies default/max timeout and output-size caps, validates invocation IDs, agent names, duplicate-key-free evidence JSON, duplicate-key-free optional M5 turn files, network policy shape, exact `host[:port]` allowlist targets, short-lived credential shape, and raw result lifecycle/output invariants, including duplicate-key and trailing JSON rejection, before schema-specific callers accept output. Deterministic fake provider coverage lives in `internal/providers/container/fake`. `internal/providers/container/docker/spec.go` translates that contract into a security-reviewed Docker runtime spec and `make sandbox-security` verifies digest-pinned images, non-root user, readonly rootfs, `no-new-privileges`, unprivileged execution, dropped capabilities, memory/CPU/PID limits, readonly `/workspace/*` input mounts, writable `/workspace/out` output mount, output-size cap, and Docker socket mount rejection. `internal/providers/container/docker/provider.go` now implements the Docker Engine lifecycle behind an injectable EngineClient: per-invocation input workspace creation, readonly input mount mapping, private writable output mount mapping, create/start/wait, fixed timeout stop/kill, forced remove, `CopyFromContainer` output extraction, output archive member validation that accepts only a top-level regular `output.json` file, output-size/JSON validation, short-lived credential expiry validation before Docker create, env injection only for accepted credentials, and cleanup tests without a local daemon. Provider-created input files are readable by the non-root sandbox user while the host temp parent remains private; `make container-provider-smoke` runs the Docker-backed Provider through a real local daemon with digest-pinned default BusyBox image resolution, ADR-0013 mounts, output archive copy, invocation-label leak check, and duplicate-key/trailing JSON rejection for provider output; `make container-provider-timeout-smoke` proves timeout cleanup leaves no labelled container behind; `make container-provider-output-cap-smoke` proves oversized output fails and still cleans up. Allowlist network mode fails closed unless an injected EgressEnforcer verifies the provider-specific network boundary before container creation; `StaticAllowlistEnforcer` additionally rejects requests that are not a subset of an externally provisioned allowlist network/proxy boundary. `make egress-allowdeny-smoke` proves the concrete Docker internal-network + proxy topology locally: allowed target succeeds through the proxy, denied target returns 403, and direct bypass from the sandbox client to upstream fails. `docs/design/agent-runtime-selection.md` records the adapter-first OpenClaw/Hermes/custom thin runner selection gate, `make forbidden-agent-runtime` prevents accidental control-plane framework dependencies before a sandbox runtime baseline is accepted, `make agent-runtime-smoke` provides the manual candidate-image smoke harness for ADR-0013 file I/O plus Docker security posture with duplicate-key/trailing JSON rejection on candidate output, and `make custom-thin-runner-smoke` proves the local scratch custom runner through both the runtime harness and Docker Provider harness using a real digest-pinned localhost-registry image reference. `scripts/agent_tool_metric_query` and `scripts/agent_tool_topology_lookup` provide the first read-only metric/topology helper contracts, covered by `make agent-tool-scripts-test`; the custom thin runner image now packages both helper binaries and proves packaged topology-helper execution with an alternate entrypoint. `scripts/sandbox_baseline_audit` emits a code-level JSON proof for fixed file paths, network-none defaults, M5 turn mounts, Docker security posture, resource limits, allowlist subset enforcement, and strict request/raw-output JSON validation. `scripts/sandbox_quality_compare` provides the offline direct-vs-sandbox SubReport comparison harness, manifest-mode batch evidence support with `sample_basis`, per-case alert scenario labels, canonical `snapshot:<positive-id>` EvidenceSnapshot refs, scenario coverage output, and production parser validation before conservative per-case and aggregate deltas are emitted. `scripts/sandbox_m4_decision` provides the offline proceed/iterate/defer evidence combiner for baseline audit output, manifest-mode quality comparison output, runtime smoke evidence, and human review evidence; quality evidence must cover `single_alert`, `cascade`, and `alert_storm`, each case must retain a canonical snapshot evidence ref, review evidence must name the same `sample_basis` as the quality comparison, each reviewed case must repeat the quality case scenario and required evidence refs exactly, and runtime smoke sources must match the canonical `make` target for each required smoke. `scripts/sandbox_m4_evidence_packet` assembles those outputs plus copied quality manifest/direct/sandbox SubReport inputs, copied review evidence, copied runtime-smoke artifacts, and decision output into one empty artifact directory for audit handoff; it rejects quality manifests that do not match the generated comparison output, rejects copied SubReports that fail the production parser or miss declared required refs, rejects copied review evidence whose `sample_basis` or reviewed-case scenario/evidence-ref bindings do not match the generated quality comparison, records packet-local paths plus digests, and can reverify a retained packet offline via `--verify-packet` without rerunning helpers. OpenClaw/Hermes framework candidate results, production egress wiring into a candidate runtime, credential issuance/rotation wiring, real quality comparison, and a recorded M4 decision from real evidence remain pending. |
| Docker daemon access boundary | documented | `docs/design/docker-daemon-boundary.md` records the V1 host-socket boundary, treats Docker socket access as host-admin equivalent, forbids mounting the socket into agent sandboxes, rejects plaintext remote daemon access, and limits any future remote Docker mode to TLS-verified client-authenticated connections. Post-V1 options are rootless Docker, a dedicated sandbox host, or a Kubernetes Job provider behind the same `ContainerProvider` contract. |
| AuthProvider/RBAC/WS ticket boundary | shipped (M5 local framework) | `internal/usecases/ports/providers.go` defines the provider-neutral bearer authentication port and `AuthPrincipal` / owner-admin role DTOs. `internal/providers/auth/oidc` implements signed OIDC ID-token verification through issuer discovery/JWKS, configured client ID audience enforcement, and role-claim mapping into owner/admin roles; `internal/providers/auth/fake` provides scripted deterministic auth for tests. `internal/usecases/diagnosisauth` enforces V1 owner/admin RBAC, keeps leader-tier access rejected, issues cryptographically random URL-safe WebSocket tickets with TTL <= 30s only after RBAC passes, consumes tickets atomically once, rejects expired or wrong-session tickets, and includes a defensive in-memory store for tests/local development. `internal/persistence/ent/schema/diagnosisauthticket.go`, migration `internal/persistence/migrations/20260528122257_diagnosis_auth_tickets.sql`, and `internal/persistence/repository/diagnosis_auth_ticket_store.go` add PostgreSQL-backed ticket storage that persists only `sha256(token)` and uses a conditional consume update so concurrent consumers produce one winner. OpenAPI includes `POST /api/v1/diagnosis/ws-ticket`; `internal/transport/http` registers authenticated ticket issuance plus the ticket-required `GET /ws/diagnosis` upgrade route, then relays authenticated `submit_turn` and `query_state` frames through the provider-neutral `ports.DiagnosisRoomWorkflowClient`. `make diagnosis-auth-test` is part of CI and covers handshake, relay, timeout, and disconnect boundaries. |
| AuthProvider OIDC | shipped (M5 local provider) | `internal/providers/auth/oidc` verifies signed OIDC ID tokens with `github.com/coreos/go-oidc/v3`, rejects issuer URL userinfo before discovery, enforces issuer/audience/expiry/signature checks before custom claim handling, rejects malformed or trailing claim payload JSON during verification, rejects duplicate claim names after verification and before role mapping, extracts claims without persisting raw tokens, supports configurable role claim/value mapping, and is covered by in-process OIDC `oidctest` verification tests. |
| Diagnosis room policy boundary | shipped (M5 local framework) | `internal/usecases/diagnosisroom` owns pure short-conversation policy checks before transport handoff: bounded turn count, session lifetime, idle timeout, per-turn timeout bounds, message/context byte caps, duplicate `message_id` rejection, in-flight turn rejection, caller-supplied time checks, duplicate-key-free evidence JSON with trailing-value rejection before deterministic context byte accounting for ADR-0013 M5 input files, a basic unsafe-instruction denylist, and the strict V1 sandbox `output.json` schema parser. `make diagnosis-room-policy-test` is part of CI. |
| Diagnosis room workflow | shipped (M5 local control plane + sandbox + transcript persistence + lifecycle audit + close notification) | `internal/orchestrator/temporal/diagnosis_room_workflow.go` registers `submit-turn` Update with a `diagnosisroom.ValidateSubmitTurn` Validator, exposes `state` Query for reconnect/read state, consumes `close` and `cancel` Signals, and uses durable workflow timers for idle timeout and fixed session lifetime. HTTP-created rooms start from a frozen EvidenceSnapshot through `DiagnosisRoomStarter`, which now requires duplicate-key-free JSON-object evidence and rejects trailing values before constructing workflow input; workflow startup repeats the same evidence validation before calling `EnsureDiagnosisRoomSession`, which creates/reuses the workflow-bound `DiagnosisTask` and `ChatSession` using Temporal workflow/run IDs and records an idempotent `diagnosis_room.opened` event. Existing task-bound histories still use `EnsureDiagnosisChatSession`. Accepted Updates call `RunDiagnosisTurn` in `internal/orchestrator/temporal/diagnosis_room_activity.go`, which builds the ADR-0013 `ContainerRunRequest` for the `diagnosis-assistant` agent, mounts evidence/prior conversation/latest message, runs `ContainerProvider.Run`, and accepts only strict-JSON and schema-valid diagnosis-turn output. The workflow then calls `PersistDiagnosisTurn`, which writes the user+assistant ChatTurn pair, advances `ChatSession.turn_count`, and records an idempotent `diagnosis_room.turn_persisted` event before workflow state is mutated. On terminal close, the workflow calls `CloseDiagnosisChatSession`, which persists `ChatSession.closed_at` / `close_reason` and records an idempotent `diagnosis_room.closed` event, then calls `SendDiagnosisRoomCloseNotification`, which reuses `IMProvider` with a diagnosis-task-scoped idempotency key and records `diagnosis_room.close_notification_sent`. Lifecycle audit payloads reject duplicate object keys and trailing JSON before append-only `DiagnosisTaskEvent` persistence. `internal/orchestrator/temporal/diagnosis_room_client.go` adapts Temporal `UpdateWorkflow` with `WorkflowUpdateStageCompleted` and `QueryWorkflow` to the provider-neutral transport port. `make diagnosis-room-workflow-test` covers accepted persisted user+assistant turn state, startup task/session creation, strict starter/workflow evidence input, strict turn-output acceptance, direct persistence/lifecycle/notification Activity idempotency, ambiguous lifecycle payload rejection, duplicate `message_id` rejection, concurrent Update rejection while an Activity is in flight, configured max-turn rejection, unsafe-message rejection, Query output, close/cancel signal termination, timer close reasons, and Temporal client start/Update/Query option mapping. |
| Diagnosis room | partial (M5 local framework + one-turn local live proof) | AuthProvider/OIDC/RBAC, `POST /api/v1/diagnosis/rooms` room creation from frozen EvidenceSnapshot, ticket-based WebSocket handshake, authenticated WebSocket submit/query relay, pure room policy checks, Temporal room workflow plus per-turn sandbox Activity boundary, workflow transcript persistence, lifecycle audit events, final close notification, the `/diagnosis-room` Next.js route, the manual live browser smoke harness, runtime env wiring for OIDC/room-starter/ticket/relay/browser-origin/Docker sandbox dependencies, and ChatSession/ChatTurn schema/repository persistence have landed. `ChatSession` remains anchored to `DiagnosisTask` so the project stays intelligent-alert-analysis first; `session_key` is the external room id for browser tickets/reconnect. `ChatTurn` is append-only with per-session `message_id` and `sequence` uniqueness for retry idempotency and canonical transcript replay. A retained local 2026-06-04 `make diagnosis-live-browser-smoke` proof now covers one create-room -> browser connect/state -> submit_turn -> turn_result path against a real local backend/worker stack. The harness can optionally close the room with `OPENCLARION_LIVE_REQUIRE_CLOSE_NOTIFICATION=1` and validate a `close_notification` proof block, including workflow `final_conclusion` and `diagnosis_room.closed` event `final_conclusion` equality for the latest assistant turn; `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=diagnosis-live-browser-smoke` now preflights the extra local PostgreSQL/Temporal and close-notification worker IM prerequisites without printing their values. A retained live close-notification IM delivery proof and production agent runtime selection remain pending. |

## Active ADRs (Quick Index)

| ADR | Status | Subject |
|-----|--------|---------|
| 0001 | proposed | PostgreSQL single source of truth |
| 0002 | proposed | AI agent black-box principle |
| 0003 | proposed | Provider extension interfaces |
| 0004 | proposed | Temporal workflow engine (driven by M5) |
| 0005 | proposed | Ephemeral container security |
| 0006 | proposed | Feasibility and MVP cutline (M5 = V1 required, short-conversation scope) |
| 0007 | proposed | OpenAPI 3.1 native toolchain (`oapi-codegen-exp` V3) |
| 0008 | proposed | Monorepo repository structure |
| 0009 | proposed | Go control plane scheduling |
| 0010 | proposed | Frontend architecture |
| 0011 | proposed | CI governance |
| 0012 | proposed | Technology stack validation (Gin removed, Node.js 24.x LTS, Next.js 16, generic agent sandbox) |

## Open Validation Items Before M1

* **Atlas wrapper smoke verified locally on 2026-05-22.** The wrapper
  (`scripts/lib_atlas.sh` plus three thin entry scripts) ran
  `make atlas-smoke` end-to-end against host docker and produced 2
  files; cleanup left no residual `.atlas-*` directories,
  `atlas-pg-*` containers, or `atlas-net-*` networks. The CI
  `atlas-drift` job already runs `actions/setup-go` so the host Go
  toolchain is available when the wrapper mounts it into the Atlas
  container. See [database/migrations.md](database/migrations.md) for
  the full wrapper contract and the empirical findings table that
  drove the redesign.
* **Remaining four Ent schemas landed on 2026-05-22** (`AlertGroup`,
  `EvidenceSnapshot`, `DiagnosisTask`, `DiagnosisTaskEvent`); first
  migration cut via `make atlas-migrate-diff NAME=initial_schema`
  (`20260525060310_initial_schema.sql`, recut after the same-day
  review that tightened `EvidenceSnapshot` to per-group unique digest
  and `DiagnosisTask` to `(workflow_id, run_id)` natural identity);
  `make atlas-drift` reports
  synced.
* Temporal Go SDK first-import pin (`go.temporal.io/sdk v1.44.0`) and Update
  round-trip integration test landed in **M1-PR3** as the `DiagnosisWorkflow`
  shell + `RecordDiagnosisEvent` activity (three-transaction idempotent
  producer chain). Workflow integration tests via `testsuite.StartDevServer`
  + testcontainers Postgres cover: happy-path Update writes
  `DiagnosisTaskEvent` to DB; validator rejects empty Kind / empty
  `dedupe_key` with no DB write; Update on workflow A never produces events
  on a peer task B (workflow-bound task isolation); duplicate Update with
  the same `dedupe_key` returns the original EventID and leaves a single
  DB row (idempotency); zero `input.TaskID` is rejected at workflow start.

## Non-Blocking Cross-Checks

* optional spike comparing Temporal vs River+sqlc on M2 fan-out/fan-in
  (cross-check ADR-0004 if the M1-PR3 Temporal setup feels disproportionate)

## Update Discipline

* This file is updated in the same PR that changes implementation status.
* Status entries use: `not started`, `in progress`, `shipped`, `deferred`.
* When a row moves to `deferred`, a corresponding entry must appear in
  [DEFERRED_FOLLOWUPS.md](DEFERRED_FOLLOWUPS.md).

## Changelog

| Date | Author | Change |
|------|--------|--------|
| 2026-06-05 | jindyzhao | Manual evidence readiness sequencing landed locally: `make manual-evidence-readiness` now annotates each remaining live/evidence target with milestone, sequence, evidence goal, and dependency hints, and the summary reports the next blocked target by milestone sequence. This makes the remaining M2/M5/M4 proof order explicit without adding a CI gate, running external services, selecting samples, comparing quality, or claiming any new retained evidence. |
| 2026-06-05 | jindyzhao | M2 report-live-smoke readiness preflight hardened locally: `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=report-live-smoke` now validates Prometheus/Webhook URL shape, canonical UTC replay windows, supported report scenarios, positive replay limits, positive Go-duration wait timeouts, and bounded whitespace-free workflow/correlation IDs without printing configured values. This reduces operator setup errors for the pending real Prometheus->Temporal->Webhook retained proof without adding a CI gate or claiming live external E2E completion. |
| 2026-06-05 | jindyzhao | M4 evidence-chain readiness now performs semantic packet verification when the canonical retained artifact set is present: after checking direct artifact presence, strict JSON shape, digest-ref format, and SHA-256 digests, `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=sandbox-m4-evidence-chain` invokes the retained packet verifier and reports a sanitized `packet_semantic_verification` status. This catches stale or hand-edited packets before an operator treats the evidence root as ready, without adding a CI job, selecting representative samples, running Docker, comparing quality, or accepting a runtime baseline. |
| 2026-06-04 | jindyzhao | M4 evidence-chain readiness preflight landed locally: `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=sandbox-m4-evidence-chain` now checks `OPENCLARION_M4_EVIDENCE_ROOT` for the canonical retained M4 artifact chain, including baseline audit, runtime-smoke artifacts, digest-ref handoff, direct/sandbox sample directories, quality manifest, quality comparison, review evidence, and packet summary. Existing JSON artifacts are checked for duplicate object keys and trailing JSON values before SHA-256 digests are reported. This is an operator gap audit only; representative quality comparison, human review, and the M4 proceed/iterate/defer decision remain pending. |
| 2026-05-19 | jindyzhao | Initial snapshot at pre-M0 state |
| 2026-05-22 | jindyzhao | M0 bootstrap complete: Go module, Docker Compose, OpenAPI, oapi-codegen-exp, health endpoint, CI gates |
| 2026-05-22 | jindyzhao | Post-review fixes: PostgreSQL `postgres:18-alpine` tag (digest scrapped); `vacuum` promoted to real blocking gate via `go tool`; Ent / Atlas / Temporal SDK pin moved to M1 per first-import rule; ADR-0012 amended to move Temporal Update validation to M1 |
| 2026-05-22 | jindyzhao | M1-PR1 start: persistence foundation. `entgo.io/ent v0.14.6` pinned (direct require + tool directive); Atlas pinned to `arigaio/atlas:1.2.0` Docker image (original Plan A draft: Dockerized Atlas with mounted Docker socket -- later superseded by the wrapper redesign in the same milestone window); `AlertEvent` Ent schema landed as smoke target; `make ent-fresh` and `make atlas-drift` gates landed in CI; `make atlas-smoke` and remaining four schemas pending host-docker validation |
| 2026-05-22 | jindyzhao | M1-PR1 Atlas wrapper redesign landed: dropped `--dev-url docker://...` + Docker socket approach (Atlas image lacks Docker CLI and Go runtime); new wrapper (`scripts/lib_atlas.sh` plus three thin entry scripts) launches dev Postgres from host on a dedicated Docker network, mounts host Go toolchain read-only into Atlas container, runs as `$(id -u):$(id -g)`, dev-url is plain `postgres://`; `raw_payload` corrected from `bytea` to JSONB; CI `atlas-drift` job acquired `actions/setup-go`; AlertEvent primary key locked as Ent default `bigserial` (UUID reserved for security-sensitive single-use tokens only) |
| 2026-05-22 | jindyzhao | M1-PR1 Atlas smoke gate verified on host docker: `make atlas-smoke` produced 2 files end-to-end via the redesigned wrapper; cleanup left no residual `.atlas-*` dirs, `atlas-pg-*` containers, or `atlas-net-*` networks. Remaining four Ent schemas and the first migration are next. |
| 2026-05-22 | jindyzhao | M1-PR1 schemas + first migration landed: `AlertGroup`, `EvidenceSnapshot`, `DiagnosisTask`, `DiagnosisTaskEvent` Ent schemas committed (M2N AlertEvent<->AlertGroup; 1:N down the chain; all bigserial PKs); FK columns surfaced as explicit `field.Int` so composite-index column ordering matches docs intent (`(alert_group_id, created_at)`, `(task_id, dedupe_key) UNIQUE`, `(task_id, occurred_at)`); first migration cut via `make atlas-migrate-diff NAME=initial_schema`; `make atlas-drift` reports synced. |
| 2026-05-22 | jindyzhao | M1-PR1 schema review fixes pre-baseline: (1) `EvidenceSnapshot.digest` no longer table-wide UNIQUE -- replaced with composite `UNIQUE (alert_group_id, digest)` because the model is `AlertGroup -1:N-> EvidenceSnapshot` and two groups MAY produce identical canonical payloads; (2) `DiagnosisTask` identity changed from `UNIQUE(workflow_id)` + optional latest-`run_id` to natural `UNIQUE(workflow_id, run_id)` with `run_id` NOT NULL + immutable, plus a non-unique `workflow_id` chain index, so Temporal retries that spawn a new `run_id` are NEW rows (matches Temporal's own `(workflow_id, run_id)` event-history boundary); (3) `docs/design/DEPENDENCIES.md` and `DEFERRED_FOLLOWUPS.md` cleaned of leftover `--dev-url docker://...` / mounted-Docker-socket descriptions (those describe a plan that was abandoned during the same-day wrapper redesign); (4) `ATLAS_IMAGE` and `ENT_SCHEMA_URL` are now propagated explicitly from `Makefile` to wrapper scripts via per-target `ATLAS_IMAGE="$(ATLAS_IMAGE)" bash ...` recipes -- the lib_atlas.sh defaults are now an explicit fallback for direct script debugging only. Initial migration recut as `20260525060310_initial_schema.sql`; `make atlas-drift` reports synced; `make pr` all gates passed. |
| 2026-05-22 | jindyzhao | M1-PR1 shipped post-merge as `b7233c7`. Stage moves to **M1-PR2** (domain layer + persistence repository contracts + `UnitOfWork` + DI wiring). Temporal Go SDK first-import pin (>= 1.21) and Update round-trip integration test scoped to **M1-PR3** with the `DiagnosisWorkflow` shell, per ADR-0012 amendment and the first-import rule (dependencies enter `go.mod` only when production code first imports them). `DEPENDENCIES.md`, `ADR-0004`, and `END_TO_END_VERIFICATION.md` aligned with this scope split. |
| 2026-05-25 | jindyzhao | M1-PR2 shipped post-merge as `d0c67bb`. M1-PR3 ships Temporal Go SDK (`go.temporal.io/sdk v1.44.0`) via first-import pin, `DiagnosisWorkflow` shell with workflow-entry zero-TaskID rejection + `"record-event"` Update handler (validator rejects empty Kind / empty `dedupe_key` pre-history) + `"complete"` Signal, `RecordDiagnosisEvent` activity (three-transaction idempotent producer chain backed by `DiagnosisRepository.FindEventByTaskAndDedupeKey`), explicit `RetryPolicy.NonRetryableErrorTypes` aligned with `temporalsdk.NewNonRetryableApplicationError`, `NewWorker` factory, and `cmd/openclarion/main.go` Temporal client + worker wiring. Integration tests cover happy-path Update round-trip, validator-rejects-empty-Kind / -empty-DedupeKey (no DB write, error text asserted by substring), workflow-bound task isolation, same-`dedupe_key` idempotency, and zero-TaskID workflow-start rejection. D6 Temporal SDK portion closed. |
| 2026-05-26 | jindyzhao | M1-PR3 shipped post-merge as `3874f44`. M1-PR4 in progress: `MetricsProvider` port + `ActiveAlert` DTO landed in `internal/usecases/ports/providers.go` (firing-only contract; ports depend only on domain + stdlib, concrete providers MAY import third-party SDKs). Prometheus implementation in `internal/providers/metrics/prometheus/` pins `github.com/prometheus/client_golang v1.23.2` via first-import rule, drops non-firing alerts before mapping `model.LabelSet -> map[string]string`, and exposes Bearer auth via `WithBearer` -> `github.com/prometheus/common/config.NewAuthorizationCredentialsRoundTripper`. `fake.New` deep-copies the seed at construction AND at every read so test-vs-consumer mutations stay isolated (covered by unit tests). `alertingest.IngestOnce` queries the provider once, persists each alert in its own `UnitOfWork` transaction so `ErrAlreadyExists` rolls back and counts as `Duplicate`, aggregates remaining failures with `errors.Join`, and reports `Stats{Total = len(alerts), Saved, Duplicate, Failed}`. Sorted-JSON canonical labels feed SourceFingerprint and CanonicalFingerprint using SHA-256; nil labels normalise to `{}` before hashing. Per-alert warning logs only carry `source` / `starts_at` / `canonical_fingerprint` / `error`. testcontainers Postgres integration tests cover save / duplicate-rollback / invariant-violation; `client_golang` httptest tests cover envelope decoding, firing-state filter, Bearer-header injection, and 5xx / malformed-JSON wrapping. Trigger wiring is intentionally deferred to a later PR alongside the grouping algorithm, so `cmd/openclarion/main.go` is intentionally untouched. |
| 2026-05-26 | jindyzhao | M1-PR5 shipped. M1-PR6 in progress: EvidenceSnapshot deterministic builder. Pure function `BuildSnapshot(Input) (domain.EvidenceSnapshot, error)` in `internal/usecases/evidencebuild/` -- packs `AlertGroup` + `[]AlertEvent` into a canonical snapshot payload (typed structs for field-order stability, all nested `json.RawMessage` canonicalized via unmarshal+remarshal, nil labels/annotations normalized to `{}`, `*json.RawMessage` for null rendering). Digest = sha256 of the marshalled canonical payload bytes. Provenance = static `openclarion.core` with `status:ok, inputs:[alert_group, alert_events]`. Status = complete, MissingFields = nil. Expanded input validation rejects zero IDs, empty strings, zero/invalid times, LastSeenAt < FirstSeenAt, invalid severity, invalid/non-object dimensions, non-valid RawPayload, event outside time range, duplicate event IDs, and EventIDs set mismatch. `CreatedByWorkflow` flows to the domain struct but is NOT part of payload/digest. All times normalized via `domain.NormalizeUTCMicro` before comparison and formatting. Unit tests cover determinism, canonical JSON key-order/whitespace invariance, nil rendering, validation boundaries, provenance/status, and digest stability. No provider calls, no persistence orchestration, no changes to `cmd/openclarion/main.go`. |
| 2026-05-26 | jindyzhao | M1-PR6 shipped post-merge as `22428fb`. M1-PR7 in progress: Alert window replay harness. New package `internal/usecases/alertreplay/` exposes `ReplayWindow(ctx, provider, factory, Request) (Stats, error)` which (1) calls `alertingest.IngestOnce`, (2) reads the half-open window `[WindowStart, WindowEnd)` from the DB with `Limit+1` as a safety valve (`> Limit` aborts the run with `ErrInvariantViolation` after setting `Stats.EventsLoaded` so callers can diagnose), (3) runs deterministic grouping, (4) reconstructs each draft's events from an id->event map (missing id is rejected at the orchestrator boundary as an invariant violation), and (5) runs a per-group pipeline transaction: `FindGroupByNaturalKey` decides save vs refresh (severity / event_count / last_seen_at diff) vs existing -> `LinkEventsToGroup` -> `BuildSnapshot` with `snapshotGroup.EventIDs = draft.EventIDs` so the group-vs-events ID set check runs -> `FindByGroupAndDigest` -> `Evidence.Save` only when not found -> `UpdateGroup(g.Close())` only when status was active. Per-group failures join via `errors.Join` and increment `Stats.Failed` without aborting the rest of the run. `AlertRepository` ports gained two methods: `ListEventsByStartsAtRange(start, end, limit)` (half-open interval, ordered by `(starts_at ASC, id ASC)`, end > start compared AFTER `NormalizeUTCMicro` to defend against sub-microsecond windows the normaliser would erase) and `FindGroupByNaturalKey(groupKey, firstSeenAt)`; both reject zero/blank inputs with wrapped `ErrInvariantViolation`. NotFound branches strictly use `errors.Is(err, domain.ErrNotFound)` so a non-NotFound error never silently routes through the save path. D7 closed->refresh tension: refresh allows updating mutable fields of an already-closed group when the same `(group_key, first_seen_at)` natural key recurs, but status is preserved (no reopen) -- a window whose `first_seen_at` differs creates a NEW row by design. testcontainers Postgres integration tests cover the happy path, rerun idempotency (Stats reshape Saved -> Duplicate / Existing), out-of-window event exclusion (IngestOnce still writes 30 rows; replay only sees 20), multi-alertname grouping with distinct snapshot digests, close-after-snapshot, refresh-on-new-event-in-existing-group (closed group refreshes mutable fields, status stays closed, second snapshot row written because digest differs), Limit safety valve, request validation table (nil provider / nil factory / zero or inverted window / sub-microsecond normalised-collapse / Limit <= 0 / Limit == math.MaxInt), empty grouping config rejected when events exist (Step 3 fail does NOT count as `Failed`), and empty-window short-circuit. Repository-level tests cover the new methods' boundary self-defence (zero start/end, limit <= 0, end == start after normalisation, normalised collapse, NotFound semantics). `cmd/openclarion/main.go` is intentionally untouched -- triggers (CLI / scheduled workflow / HTTP) land in a later PR. |
| 2026-05-27 | jindyzhao | Shepherd gate migration W1-W3 landed locally: gitleaks, govulncheck, golangci-lint baseline, Dependabot policy, and `tools/openclarion-linter` custom golangci-lint module plugin with forbidden-imports, Ent transaction-boundary, and runtime fake-provider analyzers. W3-2b retired `scripts/check_no_forbidden_imports.sh` after local equivalence proof. |
| 2026-05-29 | jindyzhao | W3-2b forbidden-import analyzer equivalence evidence hardened locally: `tools/openclarion-linter/analyzer/forbiddenimports` now carries `analysistest` red fixtures for every retired legacy deny-list prefix, plus provider-boundary red/green fixtures and a retained deny-list drift test. |
| 2026-05-27 | jindyzhao | First domain API read paths landed: `GET /api/v1/alerts` and `GET /api/v1/evidence-snapshots`, backed by newest-first repository list methods and generated `oapi-codegen-exp` handlers. W4 OpenAPI contract gates landed in the same change: oasdiff breaking check with soft-fail sunset 2026-06-10, critical-node fingerprint lock, and shipped-doc-claims consistency check. W5-W6 remain trigger-blocked: there is no `web/` tree and the public/release window has not opened. |
| 2026-05-27 | jindyzhao | Temporal workflow quality gate promoted from pending to landed: `make temporal-workflow-tests` runs focused race-enabled workflow tests in CI, and the happy-path `DiagnosisWorkflow` test now fetches completed history from the Temporal dev server and replays it with `worker.NewWorkflowReplayer` to catch non-deterministic changes. |
| 2026-05-28 | jindyzhao | `DiagnosisWorkflow` now starts from an EvidenceSnapshot-bound task: new histories require `TaskID` + `EvidenceSnapshotID`, `StartDiagnosisTask` validates the binding and marks the task running before event updates can write, and `workflow.GetVersion` preserves replay compatibility for older histories. Tests cover zero/mismatched snapshot inputs plus completed-history replay. |
| 2026-05-28 | jindyzhao | `workflow-parity` M1 hardening landed: PR workflows now require top-level concurrency cancellation, top-level `defaults.run.shell: bash`, fixed Ubuntu runner labels, action SHA version comments, and per-job timeout cap enforcement. |
| 2026-05-29 | jindyzhao | `workflow-parity` M2 workflow registry hardening landed locally: committed workflow files must now follow the `ci.yml` / `<gate>.yml` naming convention, appear in `docs/design/ci/README.md`'s Workflow File Registry, and carry a unique top-level workflow `name:` so branch-protection and failure names remain unambiguous. The root `scripts` Go test package now builds temporary repos to prove duplicate/unregistered workflows, invalid filenames, and missing workflow names fail. |
| 2026-05-29 | jindyzhao | `workflow-parity` permission-scope hardening landed locally: workflow-level and job-level `permissions:` blocks must stay at `contents: read`; broader entries require an inline `# parity-allow: <reason>` justification and are covered by temporary-repository Go fixtures. |
| 2026-05-30 | jindyzhao | Source comment debt tracking landed locally: `make comment-debt-check` now enforces the coding-style rule that debt comments carry an issue or ADR reference, using parser-backed Go comment scanning plus focused non-Go comment-prefix scanning so strings, generated code, testdata, ignored paths, and supported-path symlinks do not create false positives or indirect reads. |
| 2026-05-30 | jindyzhao | Deferred follow-up ledger gate landed locally: `make deferred-followups-check` now validates regular-file `docs/design/DEFERRED_FOLLOWUPS.md` input and entries for contiguous IDs, status/section consistency, required concrete fields, date hygiene, and unknown fields; closed D6/D11 entries were moved under Closed Deferrals. |
| 2026-05-29 | jindyzhao | `dco-check` author-email hardening landed locally: each accepted `Signed-off-by:` trailer must include an email matching the commit author email, mismatches are reported separately from missing trailers, and the local no-upstream fallback now validates `HEAD`. Temporary git-repository tests cover all three paths. |
| 2026-05-29 | jindyzhao | Markdown anchor validation landed locally: `scripts/check_markdown_links.sh` now validates Markdown `#anchor` fragments for same-file and cross-file links against generated heading slugs or explicit HTML anchors; `go test ./scripts` carries positive and negative temporary Markdown fixtures. |
| 2026-05-28 | jindyzhao | M2 provider contract started: `LLMProvider` and structured JSON request/response DTOs landed with a deterministic scripted fake provider. The fake-provider runtime analyzer was generalized from metrics-only to all `internal/providers/*/fake` packages so new fakes cannot leak into production `cmd/openclarion` wiring. |
| 2026-05-28 | jindyzhao | M2 LLM output validation framework landed: provider JSON is accepted only after refusal / finish_reason / truncation checks and JSON Schema validation. `github.com/santhosh-tekuri/jsonschema/v6 v6.0.2` is now a direct dependency recorded in DEPENDENCIES.md. |
| 2026-05-30 | jindyzhao | M2 LLM output strict JSON boundary hardened locally: `internal/usecases/llmoutput.Validate` now rejects duplicate object keys and trailing JSON values in both provider content and output schemas before JSON Schema validation. Provider-content ambiguity remains retryable `invalid_json`; schema ambiguity is non-retryable `invalid_request`. |
| 2026-05-28 | jindyzhao | M2 LLM retry framework landed: validation errors marked retryable are fed back to the provider for up to three attempts; refusal, invalid request metadata, provider errors, and context cancellation stop without blind retries. |
| 2026-05-28 | jindyzhao | M2 report draft contracts landed: `internal/usecases/reportdraft` now owns SubReport/FinalReport typed drafts and strict structured-output compatible JSON Schemas, with schema compatibility tests and local semantic bounds. Workflow integration remained pending at this point. |
| 2026-05-28 | jindyzhao | M2 report prompt templates landed: `internal/usecases/reportprompt` now builds SubReport requests for single-alert, cascade, and alert-storm scenarios plus FinalReport fan-in requests, with structure-only golden tests that validate request shape and schema-compatible fixture outputs. Temporal Activities/workflows remained pending at this point. |
| 2026-05-28 | jindyzhao | M2 OpenAI-compatible provider landed locally: `internal/providers/llm/openai` calls Chat Completions, supports strict `json_schema` and `json_object` fallback modes, probes strict structured-output capability at startup, maps refusal/finish/model metadata into `ports.LLMResponse`, and is covered by `httptest`. Activity/workflow wiring and notification remain pending. |
| 2026-05-28 | jindyzhao | M2 report persistence landed locally: `SubReport` and `FinalReport` domain constructors, Ent schemas, generated client code, migration `20260528022647_report_schemas.sql`, `ReportRepository`, and UoW `Reports()` now exist. Repository integration tests cover SubReport per-snapshot idempotency, FinalReport global idempotency, M2M fan-in edge listing, and bounded newest-first report queries. Temporal report workflows, IMProvider/Webhook, notification flow, and report APIs were still pending at that point. |
| 2026-05-28 | jindyzhao | M2 report Temporal workflows landed locally: `ReportFanOutWorkflow` and `FinalReportWorkflow` now execute LLM-backed generation Activities that validate drafts and persist reports before returning IDs. Activity retry idempotency pre-checks existing SubReport/FinalReport rows before any repeated LLM call. Worker registration includes both workflows, while production LLM provider injection, IMProvider/Webhook, notification flow, and report APIs were still pending at that point. |
| 2026-05-28 | jindyzhao | M2 IM provider contract landed locally: `IMProvider` and Webhook implementation now exist with deterministic fake coverage, outbound idempotency/report headers, optional Bearer auth, context-bound HTTP requests, and retryability classification. Notification Activity/workflow sequencing and delivery persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 notification flow landed locally: `FinalReportWorkflow` sequences `GenerateFinalReport` before `SendReportNotification`, and the notification Activity reloads the persisted FinalReport, uses idempotency key `final_report:<id>/notification`, calls `IMProvider`, and maps non-retryable IM failures to Temporal non-retryable application errors. Delivery-log persistence, production provider injection, end-to-end alert trigger wiring, and report APIs were still pending at that point. |
| 2026-05-28 | jindyzhao | M2 report HTTP APIs landed locally: `GET /api/v1/reports` lists recent FinalReports, and `GET /api/v1/reports/{report_id}` returns detail plus linked SubReports carrying `evidence_snapshot_id` for M3 traceability. The OpenAPI generated server types and critical-node fingerprint lock were refreshed; HTTP tests cover list/detail/not-found behavior. End-to-end alert trigger wiring, production provider injection, and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 report batch workflow landed locally: `ReportBatchWorkflow` now validates a batch, runs one `ReportFanOutWorkflow` child per EvidenceSnapshot, collects SubReport IDs in input order, and starts `FinalReportWorkflow` for fan-in persistence plus notification. Worker registration includes the new parent workflow; Temporal tests cover the child workflow graph. External trigger dispatch, production provider injection, and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 runtime provider injection landed locally: `cmd/openclarion` now builds report Activity options from env. `OPENCLARION_LLM_MODEL` enables the OpenAI-compatible LLM provider with startup strict-schema capability detection; `OPENCLARION_IM_WEBHOOK_URL` enables the Webhook IM provider. Partial config is rejected at startup, and tests cover configured/unconfigured/partial cases. External report trigger dispatch and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 HTTP report trigger landed locally: `POST /api/v1/report-triggers/replay-window` decodes a replay window, uses default alert grouping, calls `reporttrigger.ReplayAndStart`, and returns replay stats plus Temporal workflow handle. `cmd/openclarion` wires this endpoint when `OPENCLARION_PROMETHEUS_URL` is configured. CLI/scheduled triggers, live Prometheus->Temporal->Webhook E2E verification, and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 notification delivery log landed locally: `report_notification_deliveries` now records one row per notification idempotency key with pending/delivered/failed lifecycle state, provider message/status metadata, raw provider/failure JSON, and delivered_at. `SendReportNotification` persists pending before the IMProvider call, updates the same row on success/failure, and skips duplicate provider calls once a delivered row exists. Live Prometheus->Temporal->Webhook E2E verification remains pending. |
| 2026-05-28 | jindyzhao | M2 local report E2E landed: `go test ./internal/e2e` now exercises HTTP replay trigger -> alert replay -> ReportBatchWorkflow -> OpenAI-compatible SubReport/FinalReport generation -> Webhook notification -> report and delivery-log persistence using real OpenClarion components with protocol-level `httptest` providers. Live external Prometheus->Temporal->Webhook verification and CLI/scheduled triggers remain pending. |
| 2026-05-28 | jindyzhao | M2 CLI one-shot report trigger landed locally: `openclarion report-replay` uses the same report trigger usecase as the HTTP endpoint, reads PostgreSQL/Temporal/Prometheus connection settings from env, validates RFC3339 replay-window flags, emits workflow/replay JSON, and supports `--wait` / `--wait-timeout` to block until ReportBatchWorkflow completion for live external verification runs. Scheduled reports and live external Prometheus->Temporal->Webhook verification remain pending. |
| 2026-05-28 | jindyzhao | M2 manual live smoke gate landed locally: `make report-live-smoke` wraps `openclarion report-replay --wait` for real Prometheus/Temporal/PostgreSQL/Webhook environments and validates the JSON proof with `scripts/report_live_smoke_output`. The validator now also checks replay stats, failed counts, snapshot/SubReport consistency, matching notification idempotency key, and successful notification status through `make report-live-smoke-output-test`. Live external E2E remains unchecked until the manual gate is run against real services and its output is retained. |
| 2026-05-29 | jindyzhao | M2 report live proof timestamp hardening landed locally: `openclarion report-replay` now emits a UTC `checked_at` field in its retained JSON artifact, and `scripts/report_live_smoke_output` rejects missing, malformed, whitespace-padded, or future `checked_at` values before accepting a live external E2E proof. |
| 2026-05-29 | jindyzhao | M2 report live proof request binding hardened locally: `openclarion report-replay` now records sanitized replay request metadata in its JSON proof, and `scripts/report_live_smoke_output` rejects proofs without a valid alert window, positive limit, supported scenario, explicit wait intent, parseable positive wait timeout, or a window end after `checked_at`. |
| 2026-05-29 | jindyzhao | Retained live proof timestamp canonicalization hardened locally: `scripts/report_live_smoke_output` now rejects non-UTC or non-canonical `checked_at`, `request.window_start`, and `request.window_end` timestamps, and `scripts/diagnosis_live_smoke_output` rejects non-UTC or non-canonical `checked_at` timestamps. This keeps retained M2 and M5 live evidence stable for diffing and audit. |
| 2026-05-29 | jindyzhao | M2 report live proof identity binding hardened locally: `scripts/report_live_smoke_output` now rejects requested workflow ID mismatches, whitespace-padded workflow/run IDs, duplicate snapshot IDs, non-contiguous snapshot group indexes, and duplicate SubReport IDs so retained intelligent-alert report evidence cannot inflate one snapshot or SubReport into multiple proof items. |
| 2026-05-29 | jindyzhao | M2 report live proof identifier hygiene hardened locally: `scripts/report_live_smoke_output` now bounds retained workflow IDs and run IDs to 256 bytes, rejects whitespace inside those IDs, bounds optional request correlation keys, bounds notification idempotency keys, and rejects multiline/tabbed or over-512-byte provider message IDs before a live Prometheus->Temporal->Webhook proof can support acceptance. |
| 2026-05-29 | jindyzhao | M2 report live proof replay-stat consistency hardened locally: `scripts/report_live_smoke_output` now rejects retained `make report-live-smoke` artifacts when `events_loaded` exceeds the requested limit, group outcome counts do not add up to `groups_built`, snapshot saved/duplicate counters do not match retained snapshot refs, `groups_built` does not match snapshot count, or retained snapshot `event_count` values do not sum back to `events_loaded`. Live external Prometheus->Temporal->Webhook evidence remains pending until a real validator-checked artifact is retained. |
| 2026-05-28 | jindyzhao | M3 Prometheus metrics endpoint landed locally: `/metrics` is served from a custom registry with Go/process collectors and low-cardinality HTTP request counter/duration/in-flight metrics for generated API routes. OTel traces, correlation IDs, frontend pages, and dashboards remain pending. |
| 2026-05-28 | jindyzhao | M3 OpenTelemetry HTTP tracing foundation landed locally: generated API routes are wrapped with `otelhttp`, span names use stable ServeMux route patterns, W3C TraceContext/Baggage propagation is wired, OTLP HTTP export is opt-in through standard OTel env vars, and tests verify spans with an in-memory SDK exporter. Collector smoke verification, outbound trace propagation, structured correlation IDs, frontend pages, and dashboards remain pending. |
| 2026-05-28 | jindyzhao | M3 HTTP correlation logging foundation landed locally: generated API routes now receive bounded `X-Request-ID` values, HTTP transport error logs include request/trace/span correlation attributes when present, and tests verify request ID sanitisation plus trace-context log attributes. Access logs and cross-boundary propagation remain pending. |
| 2026-05-28 | jindyzhao | M3 frontend skeleton landed locally: `web/` now has a Next.js App Router project, exact-pinned npm dependencies, generated OpenAPI TypeScript types, report list/detail pages backed by generated types, and Makefile/CI gates for typecheck, lint, unit tests, build, dead-code scan, audit, and OpenAPI TS freshness. Dashboard, Playwright smoke, and broader frontend polish remain pending. |
| 2026-05-28 | jindyzhao | M3 basic dashboard landed locally: `GET /api/v1/dashboard` aggregates recent alert states and latest report notification-delivery outcomes; `/dashboard` is the default web route and renders metric cards plus alert/report breakdowns through generated OpenAPI TypeScript types. |
| 2026-05-28 | jindyzhao | M4 ContainerProvider contract landed locally: `internal/usecases/ports/container.go` fixes the shared sandbox file-contract paths, timeout/output caps, network policy shape, duplicate-key-free request/result validation, and `ContainerProvider.Run(ctx, req)` interface for M4/M5; `internal/providers/container/fake` supplies deterministic scripted fake coverage. Docker runtime and security enforcement gates remain pending. |
| 2026-05-28 | jindyzhao | M4 sandbox security spec gate landed locally: `internal/providers/container/docker/spec.go` validates Docker runtime specs before any Engine API call, and `make sandbox-security` is now in local/CI bundles to prove digest-pinned image refs, non-root execution, readonly rootfs, `no-new-privileges`, dropped capabilities, memory/CPU limits, readonly input mounts, single writable output mount, output-size cap, and Docker socket mount rejection. Candidate runtime proof and production egress enforcement remain pending. |
| 2026-05-28 | jindyzhao | M4 agent runtime selection gate landed locally: `docs/design/agent-runtime-selection.md` keeps OpenClaw/Hermes/custom thin runner evaluation inside the sandbox-image boundary, and `make forbidden-agent-runtime` rejects accidental control-plane framework dependencies before the runtime baseline is accepted. |
| 2026-05-29 | jindyzhao | M4 agent-runtime dependency boundary widened locally after runtime-boundary review: `make forbidden-agent-runtime` now rejects common framework families beyond the initial OpenClaw/Hermes/LangChain list, including LangGraph, LlamaIndex, Semantic Kernel, Pydantic AI, Agno, and Mastra, while avoiding a false positive on diagnosis-oriented package names. The custom thin runner remains a file-contract proof only, not an accepted agent runtime baseline. |
| 2026-05-29 | jindyzhao | M4 agent-runtime hardcoding boundary hardened locally: `make forbidden-agent-runtime` now also scans non-test Go control-plane source under `cmd`, `internal`, and `scripts`, rejecting hard-coded runtime family names so candidate runtime identities stay in docs, evidence packets, and sandbox image contexts until a baseline is accepted. |
| 2026-05-29 | jindyzhao | M4 agent-runtime hardcoding policy externalized locally: `make forbidden-agent-runtime` now reads `docs/design/ci/agent-runtime-forbidden.tsv`, so runtime-family names such as current framework candidates remain auditable governance data rather than shell-script or production-code bindings. Tests prove an arbitrary configured candidate family is rejected without changing the script. |
| 2026-05-29 | jindyzhao | M4 agent-runtime hardcoding policy hygiene hardened locally: `make forbidden-agent-runtime` now rejects duplicate policy rows, whitespace-padded scopes/patterns, malformed rows, and policies missing either `manifest` or `code` coverage before scanning source. This keeps the config-driven runtime-family blocklist reviewable without hard-coding candidate tools into control-plane code. |
| 2026-06-01 | jindyzhao | M4 agent-runtime hardcoding source scope hardened locally: `make forbidden-agent-runtime` now scans non-test shell and frontend JavaScript/TypeScript source under `scripts/` and `web/src/` in addition to Go control-plane source, while docs and tests remain valid places for candidate evidence. This prevents runtime-family names from becoming script/UI branches before a retained M4 decision accepts a baseline. |
| 2026-06-02 | jindyzhao | M4 agent-runtime policy checker hardened locally: `make forbidden-agent-runtime` now delegates to a structured Go checker. It rejects symlink/non-regular policy or manifest files, parses `go.mod` require/replace module paths with `golang.org/x/mod/modfile`, reads only `package.json` dependency sections, parses non-test Go control-plane import paths, string literals, and identifiers with `go/parser` / `go/ast`, and retains non-test shell/frontend source scanning so `scripts/` and `web/src/` runtime branches still fail before a retained M4 decision accepts a baseline. |
| 2026-05-28 | jindyzhao | M4 manual runtime adapter smoke harness landed locally: `make agent-runtime-smoke` validates digest-pinned candidate sandbox images under network-none, non-root, readonly-rootfs, dropped-capability, resource-limited Docker settings and requires a duplicate-key-free JSON object at `/workspace/out/output.json`. Actual runtime candidate results remain pending. |
| 2026-05-29 | jindyzhao | M4 runtime smoke output proof hardened locally: `scripts/agent_runtime_smoke_output` now rejects symlink/non-regular proof paths, bounds reads to the configured output cap plus one byte, rejects duplicate-key/trailing JSON, and rejects empty `{}` output objects. `make agent-runtime-smoke` therefore requires a candidate runtime to emit at least one direct-file machine-readable field while staying framework-neutral for OpenClaw, Hermes Agent, or custom thin runner images. This remains a file-contract smoke only; report-quality baseline acceptance still requires representative quality comparison and the M4 decision gate. |
| 2026-05-28 | jindyzhao | Alert-first signal extension boundary landed locally: `docs/design/alert-first-signal-extension.md` records that OpenClarion remains an intelligent alert analysis product, while the architecture avoids hard binding evidence, provider, workflow, report, and runtime contracts to a single monitoring system or a single alert domain. |
| 2026-05-29 | jindyzhao | Insight Pipeline / Agent Workspace boundary documented locally: `docs/design/insight-pipeline-agent-workspace.md` records that automated alert insight reports and human diagnosis/workspace interaction are separate logical subsystems inside one OpenClarion product. They share evidence, reports, providers, auth, audit, OpenAPI, PostgreSQL, Temporal infrastructure, and observability, but do not share workflow lifecycle, output schemas, conversation state, or operation authority. No repository or deployment split is planned until scale, ownership, or isolation evidence requires it. |
| 2026-05-28 | jindyzhao | M4 Docker Engine provider lifecycle landed locally: `internal/providers/container/docker/provider.go` implements the `ContainerProvider.Run` path through an injectable Docker Engine client, with unit coverage for secure create options, readonly evidence/conversation/message mounts, timeout stop/kill/remove cleanup, output-copy failure cleanup, missing agent-config rejection, and capped JSON output extraction. Candidate runtime smoke, concrete egress enforcement, and quality comparison remain pending. |
| 2026-05-28 | jindyzhao | M4 Docker egress fail-closed guard landed locally: allowlist network requests now require an injected `EgressEnforcer` to validate the provider-specific network boundary before Docker Engine create. Unit tests prove missing or rejecting enforcers prevent container creation, while accepted enforcers see the requested host allowlist and dedicated network mode. Concrete proxy/firewall enforcement remains pending. |
| 2026-05-28 | jindyzhao | M4 egress policy hardening landed locally: allowlist targets are normalized as exact `host[:port]` entries and reject URLs, paths, wildcards, whitespace, invalid ports, IPv6 literal syntax, and duplicates. Docker `StaticAllowlistEnforcer` validates requested targets against an externally provisioned allowlist boundary before create. Concrete Envoy/Squid/iptables allow-deny smoke remains pending. |
| 2026-05-28 | jindyzhao | M4 egress allow-deny smoke harness landed and passed locally: `make egress-allowdeny-smoke` builds `scripts/egress_allowdeny_smoke`, creates sandbox/upstream Docker networks, starts allowed and denied upstream services plus an allowlist proxy, and proves allowed-via-proxy succeeds, denied-via-proxy returns 403, and direct sandbox bypass fails. Candidate runtime integration remains pending. |
| 2026-05-28 | jindyzhao | M4 short-lived credential boundary landed locally: `ContainerRunRequest` accepts one-invocation credentials with strict env-name validation, duplicate rejection, non-empty values, and required expiry. Docker provider validation rejects expired credentials and credentials whose expiry exceeds the effective container timeout immediately before create, then injects accepted values through `container.Config.Env`; tests prove invalid credential lifetimes prevent container creation and errors never include the credential value. Credential issuance/rotation remains pending runtime wiring. |
| 2026-05-28 | jindyzhao | M4 Docker daemon access boundary documented: `docs/design/docker-daemon-boundary.md` scopes V1 host-socket use, documents the no-socket-in-agent-container rule, rejects plaintext remote Docker access, and records rootless Docker / dedicated sandbox host / Kubernetes Job as post-V1 runtime options behind the same provider contract. |
| 2026-05-28 | jindyzhao | M4 Docker Provider live smoke landed locally: provider-created input files are now readable by the non-root sandbox user, the Provider now uses a private writable output bind mount because Docker archive copy cannot extract files from tmpfs mounts, and `make container-provider-smoke` runs `ContainerProvider.Run` through a real local Docker daemon using a digest-pinned image ref, network-none, readonly rootfs, ADR-0013 readonly inputs, output `fsize` cap, Docker archive output copy, invocation-label leak check, and duplicate-key-free JSON-object output validation. Real OpenClaw/Hermes/custom candidate execution through the Provider remains pending. |
| 2026-05-28 | jindyzhao | M4 Docker Provider timeout cleanup smoke landed locally: `make container-provider-timeout-smoke` runs `ContainerProvider.Run` with a two-second timeout and long-running command, expects a timeout error, and then checks Docker labels to prove the container was removed. |
| 2026-05-28 | jindyzhao | M4 Docker Provider output cap smoke landed locally: `make container-provider-output-cap-smoke` runs `ContainerProvider.Run` with a 64-byte output cap and a command that writes more than the cap, accepts either `fsize` process failure or Provider output-size rejection, and checks Docker labels to prove cleanup. |
| 2026-05-28 | jindyzhao | M4 custom thin runner candidate smoke landed locally: `scripts/custom_thin_runner` is a minimal ADR-0013 file-contract runner packaged in a scratch image, and `make custom-thin-runner-smoke` pushes it to an ephemeral localhost registry for a real `repo@sha256` reference before running both `make agent-runtime-smoke` and `make container-provider-smoke`. This is a contract/lifecycle proof only; OpenClaw/Hermes framework evaluation and report-quality comparison remain pending. |
| 2026-05-28 | jindyzhao | M4 sandbox tool helper contracts landed locally: `scripts/agent_tool_metric_query` implements a bounded read-only Prometheus instant query helper and `scripts/agent_tool_topology_lookup` implements a bounded static JSON topology lookup helper. Both emit one JSON object on success and are covered by `make agent-tool-scripts-test`, which is now part of `make ci`. |
| 2026-05-28 | jindyzhao | M4 custom thin runner helper packaging proof expanded locally: `make custom-thin-runner-smoke` now compiles `agent_tool_metric_query` and `agent_tool_topology_lookup` into the scratch candidate image and proves packaged topology-helper execution from the digest-pinned image with an alternate entrypoint before running the existing runtime and Provider smokes. |
| 2026-05-28 | jindyzhao | M4 offline sandbox quality comparison harness landed locally: `scripts/sandbox_quality_compare` validates direct and sandbox SubReport JSON through the production report draft parser, emits machine-readable metric deltas, supports manifest-mode batch evidence, supports fail-on-regression mode, and is covered by `make sandbox-quality-compare-test`. Manifest evidence now carries `sample_basis`, per-case alert scenario labels, and scenario coverage output. Duplicate JSON object keys and unknown manifest fields are rejected before manifest parsing; SubReport files reject duplicate keys before production schema validation. This is a harness only; representative quality comparison remains pending. |
| 2026-05-28 | jindyzhao | M4 sandbox baseline audit landed locally: `scripts/sandbox_baseline_audit` builds provider-neutral requests and Docker runtime specs to prove fixed ADR-0013 paths, network-none defaults, M5 turn mounts, security posture, resource limits, allowlist subset enforcement, and strict request/raw-output JSON validation without requiring Docker. `make sandbox-baseline-audit` is now part of `make ci`; manual Docker smokes remain required for live daemon cleanup evidence. |
| 2026-05-28 | jindyzhao | M4 minimum sandbox baseline for M5 accepted locally: `make sandbox-baseline-audit`, `make custom-thin-runner-smoke`, `make container-provider-timeout-smoke`, `make container-provider-output-cap-smoke`, and `make egress-allowdeny-smoke` passed against the current tree, proving auditable file I/O, packaged helper execution, timeout cleanup, output cap enforcement, and proxy allow/deny behavior. Real report-quality comparison and the M4 proceed/iterate/defer decision remain pending. |
| 2026-05-28 | jindyzhao | M4 quality evidence manifest mode landed locally: `scripts/sandbox_quality_compare --manifest <file>` validates multiple direct/sandbox SubReport pairs, resolves relative report paths from the manifest directory, emits per-case deltas plus aggregate recommendation counts, and fails on any regressed case with `--fail-on-regression`. Representative sample capture remains pending. |
| 2026-05-28 | jindyzhao | M4 sandbox decision evidence gate landed locally: `scripts/sandbox_m4_decision` combines `sandbox_baseline_audit` JSON, manifest-mode `sandbox_quality_compare` JSON, runtime smoke evidence, and human review evidence into a conservative proceed/iterate/defer JSON decision. `make sandbox-m4-decision-test` is part of CI; manual `make sandbox-m4-decision` remains pending real candidate evidence. |
| 2026-06-04 | jindyzhao | M4 retained runtime-smoke artifact collection landed locally: `make sandbox-m4-runtime-smoke-artifacts` runs the existing agent runtime, Docker Provider lifecycle, Provider timeout cleanup, Provider output-cap, and egress allow/deny smoke harnesses and writes the five canonical JSON proof filenames expected by the review-evidence template. The target is manual, requires Docker and a digest-pinned candidate image, and does not accept a runtime baseline or replace representative quality comparison. |
| 2026-06-04 | jindyzhao | M4 custom thin runner retained-artifact path landed locally: `make custom-thin-runner-smoke` can optionally write its resolved digest ref and run `make sandbox-m4-runtime-smoke-artifacts` while the ephemeral localhost registry is still alive. This connects the existing custom runner proof to the review-evidence and packet helpers without adding a CI gate, accepting a runtime baseline, or replacing representative quality comparison. |
| 2026-06-04 | jindyzhao | M4 review-evidence draft generation landed locally: `scripts/sandbox_m4_review_evidence_template` builds a fail-closed `sandbox_m4_review_evidence` draft from manifest-mode quality comparison output and retained runtime-smoke artifacts, copying quality sample/case IDs, binding canonical runtime-smoke sources, reading artifact statuses, and recording smoke artifact SHA-256 digests. `make sandbox-m4-review-evidence-template` is manual and does not accept a runtime baseline; real representative quality review and the M4 proceed/iterate/defer decision remain pending. |
| 2026-06-05 | jindyzhao | M4 reviewed-case evidence binding hardened locally: `scripts/sandbox_m4_review_evidence_template` now carries quality case `scenario` and `required_evidence_refs` into every generated `reviewed_cases[]` item, while `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` require those reviewed-case fields to match the frozen quality comparison exactly. This prevents stale human-review evidence from reusing a matching case ID for a different alert scenario or EvidenceSnapshot ref; representative quality comparison and the recorded M4 decision remain pending. |
| 2026-06-04 | jindyzhao | M4 custom-runner digest handoff landed locally: `make sandbox-m4-review-evidence-template` can read `RUNTIME_CANDIDATE_FILE=<digest-ref.txt>` instead of requiring operators to copy the custom thin runner digest into `RUNTIME_CANDIDATE`. The helper and readiness preflight still require a direct regular file containing exactly one immutable image reference and remain fail-closed until operator review, representative quality comparison, and M4 decision evidence are complete. |
| 2026-06-04 | jindyzhao | M4 retained quality comparison output path landed locally: `make sandbox-m4-quality-compare` runs manifest-mode `scripts/sandbox_quality_compare` with fail-on-regression and writes a new retained comparison JSON file without overwriting existing evidence. Real representative direct/sandbox report capture, human review, and the M4 proceed/iterate/defer decision remain pending. |
| 2026-06-04 | jindyzhao | M4 retained baseline audit output path landed locally: `make sandbox-m4-baseline-audit` writes the existing code-level sandbox baseline audit proof to a new retained JSON file without overwriting existing evidence, so the manual M4 decision chain no longer depends on shell redirection for `BASELINE_AUDIT`. Real representative quality comparison, human review, and the M4 proceed/iterate/defer decision remain pending. |
| 2026-05-28 | jindyzhao | M4 sandbox evidence packet assembler landed locally: `scripts/sandbox_m4_evidence_packet` runs the baseline audit, runs manifest-mode quality comparison with fail-on-regression, copies review evidence, runs the decision gate, and writes `baseline-audit.json`, `quality-comparison.json`, `review-evidence.json`, `decision.json`, and `packet.json` into one empty output directory. `make sandbox-m4-evidence-packet-test` is part of CI; manual packet creation still requires real representative evidence. |
| 2026-05-29 | jindyzhao | M4 sandbox evidence packet artifact validation hardened locally: `scripts/sandbox_m4_evidence_packet` now validates generated helper stdout before writing artifacts, requiring baseline pass checks, manifest-mode quality sample/scenario evidence, and valid decision enum/reasons so fake or stale helper output cannot support an audit packet. |
| 2026-05-29 | jindyzhao | M4 sandbox evidence packet review evidence validation hardened locally: `scripts/sandbox_m4_evidence_packet` now validates copied review evidence before writing it, requiring a dated representative sample basis, runtime candidate, all required runtime smokes with canonical `make` sources, and human reviewer/status metadata. |
| 2026-05-29 | jindyzhao | M4 baseline audit check uniqueness hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now reject duplicate baseline audit check names before retained M4 evidence can drive a decision or packet, preventing map-overwrite behavior from hiding a failed or missing required baseline check. |
| 2026-05-29 | jindyzhao | M4 sandbox decision evidence source validation hardened locally: `scripts/sandbox_m4_decision` now requires every runtime-smoke source in review evidence to match its canonical `make` target, so free-form notes cannot satisfy candidate runtime, provider lifecycle, timeout, output-cap, or egress smoke evidence. |
| 2026-05-29 | jindyzhao | M4 quality evidence scenario coverage hardened locally: `scripts/sandbox_quality_compare` now requires manifest `sample_basis` plus per-case alert prompt scenario labels and emits `scenario_coverage`; `scripts/sandbox_m4_decision` now defers any proceed decision unless the quality comparison covers `single_alert`, `cascade`, and `alert_storm`. |
| 2026-05-29 | jindyzhao | M4 sample-basis consistency hardened locally: `scripts/sandbox_m4_decision` now defers when human review evidence names a different `sample_basis` than the manifest-mode quality comparison, and `scripts/sandbox_m4_evidence_packet` rejects copied review evidence before writing when its `sample_basis` does not match the generated quality comparison. |
| 2026-05-29 | jindyzhao | M4 evidence schema/status validation hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now require quality comparison artifacts to declare the current SubReport schema, require every quality case to keep `review_required: true`, reject future-dated review evidence, and restrict runtime/human review statuses to `pass` or `fail`. This strengthens the evidence gate only; real representative quality comparison and the M4 proceed/iterate/defer decision remain pending. |
| 2026-05-29 | jindyzhao | M4 quality summary consistency hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now recompute quality summary counts from `cases[].recommendation`, reject mismatched top-level summaries, and reject duplicate quality case IDs before packet quality artifacts are written. This keeps retained alert-analysis quality evidence case-derived instead of hand-editable. |
| 2026-05-29 | jindyzhao | M4 evidence packet digest binding hardened locally: `scripts/sandbox_m4_evidence_packet` now records SHA-256 digests for `baseline-audit.json`, `quality-comparison.json`, `review-evidence.json`, and `decision.json` in `packet.json`, so retained runtime-baseline evidence is bound to exact artifact contents rather than only local paths. |
| 2026-05-29 | jindyzhao | M4 decision evidence packet consistency hardened locally: `scripts/sandbox_m4_evidence_packet` now rejects `decision.json` when its evidence summary does not match the same packet's baseline audit status, quality recommendation, case count, scenario coverage, sample basis, runtime candidate, runtime smoke pass count, representative-sample flag, human review status, or `--min-cases` input. |
| 2026-05-29 | jindyzhao | M4 quality manifest portability hardened locally: `scripts/sandbox_quality_compare --manifest` now rejects absolute paths, drive/URL-style paths, parent-directory traversal, cases where direct and sandbox reports normalize to the same file, and repeated normalized report paths across cases. Manifest report paths must stay slash-separated, relative to the manifest directory, and unique so retained evidence packets are portable and one report cannot inflate the representative sample size. |
| 2026-05-29 | jindyzhao | M4 quality evidence binding hardened locally: each `scripts/sandbox_quality_compare --manifest` case now requires `required_evidence_refs`, and the helper rejects the case unless both direct and sandbox SubReports contain every declared ref. The quality output, decision gate, and evidence-packet validator retain and require those refs so representative runtime evidence cannot compare mismatched alert samples by filename alone. |
| 2026-05-29 | jindyzhao | M4 quality EvidenceSnapshot binding hardened locally: manifest-mode quality cases, M4 decision input, and M4 evidence-packet quality artifacts now require at least one canonical `snapshot:<positive-id>` ref in `required_evidence_refs`. This keeps representative runtime quality evidence tied to frozen EvidenceSnapshot IDs, while runtime candidate identities remain evidence-supplied and are not hard-coded into the control plane. |
| 2026-05-29 | jindyzhao | M4 quality manifest sample-basis hygiene hardened locally: `scripts/sandbox_quality_compare --manifest` now rejects missing, whitespace-padded, multiline/tabbed, or over-2048-byte `sample_basis` values before emitting retained quality comparison output. This keeps the quality evidence basis stable before the M4 decision and evidence-packet gates consume it. |
| 2026-05-31 | jindyzhao | M4 decision and packet quality sample-basis boundary hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now revalidate retained quality comparison `sample_basis` values as required, single-line, unpadded, and bounded to 2048 bytes before combining or freezing baseline, runtime-smoke, and human-review evidence. This prevents edited or stale retained quality evidence from bypassing the manifest helper's alert-analysis evidence hygiene. |
| 2026-05-29 | jindyzhao | M4 quality manifest identity/path hygiene hardened locally: `scripts/sandbox_quality_compare --manifest` now rejects multiline/tabbed or overlong case IDs, evidence refs, and report paths before comparing reports or emitting retained quality output. Runtime candidates remain generic evidence values; the manifest hardening only stabilizes representative alert-analysis evidence. |
| 2026-05-29 | jindyzhao | M4 runtime candidate evidence immutability hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now reject review or decision evidence whose `runtime_candidate` is not an immutable `name@sha256:<64-hex-digest>` image reference, preventing tag-only sandbox runtime claims from supporting a proceed/iterate/defer decision or retained evidence packet. |
| 2026-05-29 | jindyzhao | M4 human review evidence explanation hardened locally: `scripts/sandbox_m4_decision` now defers when review evidence lacks `human_review.notes`, and `scripts/sandbox_m4_evidence_packet` rejects copied review evidence before writing if notes are missing or whitespace-padded, so retained human pass/fail claims carry an auditable reviewer explanation. |
| 2026-05-29 | jindyzhao | M4 review evidence case binding hardened locally: `scripts/sandbox_m4_decision` now requires review evidence `reviewed_cases` to match quality-comparison case IDs exactly. Missing or stale case IDs keep the decision at `defer`, case-level review failures force `iterate`, and `scripts/sandbox_m4_evidence_packet` validates `reviewed_case_count` against both review and quality artifacts before freezing a packet. |
| 2026-05-29 | jindyzhao | M4 evidence packet review-case identity matching hardened locally: `scripts/sandbox_m4_evidence_packet` now independently compares frozen review `reviewed_cases[].id` values with frozen quality `cases[].id` values before writing `packet.json`, rejecting packets that only match counts but contain missing or stale reviewed case IDs. |
| 2026-05-29 | jindyzhao | M4 decision reason retention hardened locally: `scripts/sandbox_m4_decision` keeps `defer` as the conservative decision when evidence is incomplete, but its retained `reasons` now also include any detected iterate-class failures such as non-canonical runtime-smoke evidence, regressions, or failed case review. This prevents incomplete-evidence decisions from hiding explicit runtime or quality failures. |
| 2026-05-29 | jindyzhao | M4 evidence packet decision-reason consistency hardened locally: `scripts/sandbox_m4_evidence_packet` now rejects decision artifacts whose `reasons` are blank, whitespace-padded, duplicated, inconsistent with `proceed`, or incorrectly reuse the proceed success reason for `iterate` / `defer`. This keeps retained packet decisions aligned with the frozen decision semantics rather than accepting arbitrary helper text. |
| 2026-05-29 | jindyzhao | M4 runtime candidate evaluation evidence generalized locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now require review evidence to name a generic `selected_candidate` and matching `candidate_evaluations[]` entry with status `pass`, while candidate IDs remain operator-supplied evidence values rather than code-defined OpenClaw/Hermes/custom enums. This keeps the runtime gate auditable without hard-binding OpenClarion to any particular agent framework. |
| 2026-05-29 | jindyzhao | M4 runtime candidate wording boundary clarified locally: runtime smoke and architecture docs now describe candidates as operator-supplied evidence and accepted sandbox adapters, not platform enums, product defaults, or Go control-plane branches. OpenClaw/Hermes-style names remain examples in docs, governance policy, and red-test fixtures only. |
| 2026-05-29 | jindyzhao | M4 selected-candidate runtime reference binding hardened locally: pass-status `candidate_evaluations[]` entries now require an immutable `runtime_candidate` image ref, and the selected candidate's evaluation ref must match the top-level review-evidence `runtime_candidate`. `scripts/sandbox_m4_decision` defers on missing or mismatched selected refs, while `scripts/sandbox_m4_evidence_packet` rejects such review evidence before freezing a packet. |
| 2026-05-29 | jindyzhao | M4 selected-candidate runtime smoke reference binding hardened locally: pass-status `candidate_evaluations[]` entries now require `runtime_smoke_refs` covering every required runtime smoke name, so a selected generic runtime candidate cannot claim pass status without explicitly citing the canonical runtime contract, Provider lifecycle, timeout cleanup, output-cap, and egress smoke evidence. |
| 2026-05-29 | jindyzhao | M4 runtime smoke evidence vocabulary hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now reject unknown `runtime_smokes[].name` values, so retained review evidence can only contain the exact required runtime contract, Provider lifecycle, timeout cleanup, output-cap, and egress smoke names rather than ad hoc pass claims. |
| 2026-05-29 | jindyzhao | M4 runtime smoke artifact retention hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now require every retained `runtime_smokes[]` entry to carry a distinct bounded single-line `evidence_ref` and a 64-character lowercase `evidence_sha256` digest, so future candidate acceptance cannot rely only on hand-written pass/status claims without pointing at retained smoke output. |
| 2026-05-29 | jindyzhao | M4 runtime smoke artifact-ref portability hardened locally: retained `runtime_smokes[].evidence_ref` values must now be normalized relative artifact paths; absolute paths, traversal segments, URI-style refs, backslashes, spaces, and duplicate refs are rejected before a sandbox decision or evidence packet can be accepted. |
| 2026-05-29 | jindyzhao | M4 evidence packet runtime-smoke artifact freezing hardened locally: `scripts/sandbox_m4_evidence_packet` now resolves retained runtime-smoke `evidence_ref` values under `--runtime-smoke-artifacts-root` or the review-evidence directory, verifies the source files match `evidence_sha256`, copies them into `runtime-smoke-artifacts/`, and records packet-local copied paths plus digests in `packet.json`. |
| 2026-05-29 | jindyzhao | M4 evidence packet path portability hardened locally: retained `packet.json` now records `out_dir` as `.` and stores generated artifact plus helper output paths as slash-separated packet-local paths, while the helper still uses filesystem paths internally for execution and validation. |
| 2026-05-29 | jindyzhao | M4 evidence packet quality-input freezing hardened locally: `scripts/sandbox_m4_evidence_packet` now copies the quality manifest and every manifest-named direct/sandbox SubReport into packet-local `quality-inputs/` paths, validates manifest/output identity, validates copied reports with the production SubReport parser, requires declared refs to appear in both reports, localizes the retained quality helper manifest argument, and records copied input SHA-256 digests in `packet.json`. |
| 2026-05-29 | jindyzhao | M4 evidence packet offline verification hardened locally: `scripts/sandbox_m4_evidence_packet --verify-packet <packet-dir-or-json>` now revalidates retained packet-local paths, helper command metadata, closed packet file inventory, core artifact SHA-256 digests, quality-input copies, runtime-smoke artifact copies, and decision evidence consistency without rerunning helper commands. `make sandbox-m4-evidence-packet-verify` exposes this as the manual retained-packet audit command. |
| 2026-05-29 | jindyzhao | M4 evidence packet source-file regularity hardened locally: `scripts/sandbox_m4_evidence_packet` now rejects symlinks and other non-regular files before reading, hashing, or copying quality manifests, review evidence, direct/sandbox SubReports, and runtime-smoke artifacts. This prevents retained runtime-baseline packets from being assembled through indirect links to files outside the reviewed evidence tree. |
| 2026-05-29 | jindyzhao | M4 standalone evidence source-file regularity hardened locally: `scripts/sandbox_quality_compare` now rejects symlink/non-regular manifests and direct/sandbox SubReports before parsing, and `scripts/sandbox_m4_decision` rejects symlink/non-regular baseline, quality, and review evidence inputs before parsing. This gives the manual comparison and decision targets the same direct-file evidence boundary as the retained packet assembler. |
| 2026-05-29 | jindyzhao | M4 review evidence text hygiene hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now reject multiline, tabbed, whitespace-padded, empty, or over-2048-byte human-authored review evidence text in sample basis, reviewer notes, candidate evaluation source/notes, and reviewed-case notes. This keeps retained M4 runtime decision packets diffable and audit-friendly. |
| 2026-05-29 | jindyzhao | M4 decision/packet identity hygiene hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now reject whitespace-padded `selected_candidate` values plus multiline/tabbed or overlong quality/review case IDs and required evidence refs before making a decision or freezing a packet. Candidate IDs remain generic evidence values, not code-defined runtime names. |
| 2026-05-29 | jindyzhao | Retained JSON evidence strictness hardened locally: `internal/strictjson` rejects duplicate object keys and unknown struct fields before Go unmarshalling, and M2 report live proof, M4 quality comparison, M4 decision, M4 evidence packet, and M5 diagnosis live proof validators now use it for operator-supplied or helper-emitted JSON artifacts. This prevents later duplicate keys from silently overriding earlier evidence fields and prevents stale/extra proof fields from being mistaken as accepted evidence. |
| 2026-05-29 | jindyzhao | OpenAPI fingerprint lock strictness hardened locally: `scripts/check_openapi_fingerprint.go` now rejects duplicate object keys and trailing JSON in `docs/design/ci/locks/openapi-critical.lock` before comparing critical-node hashes, and root `scripts` tests cover weak-lock rejection. This keeps API contract evidence from relying on Go map overwrite behavior. |
| 2026-05-30 | jindyzhao | OpenAPI fingerprint input regularity hardened locally: `scripts/check_openapi_fingerprint.go` now rejects symlink and other non-regular `api/openapi.yaml` / `docs/design/ci/locks/openapi-critical.lock` inputs before parsing, and refuses to update an existing indirect lock file. This keeps critical API contract evidence tied to direct repository files rather than filesystem aliases. |
| 2026-05-29 | jindyzhao | M4 topology helper input strictness hardened locally: `scripts/agent_tool_topology_lookup` now rejects symlink/non-regular mounted topology files before opening them and reuses `internal/strictjson` so mounted static topology JSON rejects duplicate object keys and trailing values in addition to unknown fields, oversized files, duplicate service names, and empty service names. This keeps sandbox helper output auditable before any runtime baseline is accepted. |
| 2026-05-29 | jindyzhao | M4 metric helper response strictness hardened locally: `scripts/agent_tool_metric_query` now reuses `internal/strictjson` for Prometheus response envelopes and nested query data so duplicate object keys, unknown fields, and trailing JSON values are rejected before sandbox helper output is emitted. |
| 2026-05-30 | jindyzhao | Prometheus MetricsProvider response strictness hardened locally: `internal/providers/metrics/prometheus` now validates Prometheus JSON envelopes through a bounded transport before `client_golang` parses them, rejecting duplicate object keys, trailing JSON values, and oversized parsed response bodies for 2xx success and 400/422 API error responses while preserving other upstream non-2xx error handling. Focused provider tests cover ambiguous top-level and nested success fields, ambiguous API error envelopes, and the 4 MiB cap. |
| 2026-05-29 | jindyzhao | ContainerProvider strict JSON boundary hardened locally: `internal/usecases/ports.ContainerRunRequest.Validate` now rejects duplicate object keys and trailing JSON in evidence, conversation, and latest-message input files, and `ValidateContainerRunResult` applies the same strict parser to raw sandbox output before schema-specific callers accept it. The shared helper now lives in `internal/strictjson` so retained proof validators and production sandbox boundaries cannot drift. |
| 2026-05-30 | jindyzhao | Docker Provider output archive strictness hardened locally: `internal/providers/container/docker.readOutputArchive` now accepts only a normalized top-level regular `output.json` tar member from `CopyFromContainer`, rejecting nested paths, traversal paths, symlinks, and hardlinks before size and JSON validation. This keeps sandbox output ingestion direct and auditable without adding runtime-specific agent logic. |
| 2026-05-29 | jindyzhao | M4 custom thin runner input strictness hardened locally: `scripts/custom_thin_runner` now rejects symlink/non-regular ADR-0013 evidence, conversation, and message JSON paths before reading and reuses `internal/strictjson` to reject duplicate object keys plus trailing JSON before hashing inputs or writing its contract proof. This aligns the candidate image proof with the ContainerProvider boundary while keeping the product direction focused on intelligent alert analysis. |
| 2026-05-29 | jindyzhao | M4 runtime-agnostic Docker sandbox baseline closed locally after revalidation. `make sandbox-security`, `make sandbox-baseline-audit`, `make container-provider-smoke`, `make container-provider-timeout-smoke`, `make container-provider-output-cap-smoke`, and `make egress-allowdeny-smoke` passed against Docker 29.1.5, proving non-root readonly execution, resource limits, timeout cleanup, output-cap handling, network-none defaults, allowlist subset enforcement, and proxy allow/deny behavior. Remaining M4 work is representative intelligent-alert quality comparison and the recorded proceed/iterate/defer decision. |
| 2026-05-29 | jindyzhao | PR wall-clock budget hardening landed locally: `make pr` now invokes `scripts/pr_budget` around `make ci` with a default 15-minute enforced budget and terminates the child command on timeout. `PR_BUDGET_MODE=warn` keeps a temporary local diagnostic path, while `make pr-budget-test` and the `pr-budget` CI job cover wrapper parsing, warn mode, timeout termination, over-budget failure, and child exit-code preservation without recursively running the full PR bundle inside CI. |
| 2026-05-30 | jindyzhao | Allowlist discipline source-file regularity hardened locally: `make allowlist-discipline` now rejects symlink and other non-regular `.gitleaks.toml` inputs plus symlinked or non-directory parent paths before checking allowlist metadata. This keeps false-positive suppressions tied to the reviewed repository file instead of an indirect path. |
| 2026-05-29 | jindyzhao | Allowlist discipline hardening landed locally: `make allowlist-discipline` now validates `.gitleaks.toml` allowlist entries have adjacent Owner, Expires, and Removal trigger metadata, and `make allowlist-discipline-test` covers missing metadata, detached metadata, expired entries, and read-error handling. The gitleaks false-positive suppressions now carry explicit 2026-08-31 review expiry dates. |
| 2026-05-30 | jindyzhao | Gitleaks allowlist review-window hardening landed locally: `make allowlist-discipline` now rejects `.gitleaks.toml` allowlist entries whose `Expires` date is more than 120 days in the future, while still accepting same-day and horizon-boundary expiries. This keeps secret-scanning false-positive suppressions bounded to active review windows. |
| 2026-05-30 | jindyzhao | Branch protection required-check policy landed locally: `make branch-protection-check` now validates the repository source-of-truth JSON for `main` required status checks against all PR-triggered GitHub Actions job names, keeping future branch-protection settings auditable and aligned with CI job renames. |
| 2026-05-29 | jindyzhao | Workflow change isolation hardening landed locally: PR-only `make workflow-change-guard` now rejects pull requests that change more than one `.github/workflows/*.yml` / `.yaml` file, while still allowing companion script, Makefile, and documentation edits for a single workflow gate change. `make workflow-change-guard-test` covers path normalization, duplicate handling, CI diff-range fetching, local fallback, and violation reporting. |
| 2026-06-03 | jindyzhao | Linear history guard landed locally without adding a top-level CI job: `make linear-history-check` now runs inside the existing PR-only DCO job and rejects merge commits introduced by pull request ranges, keeping branch review history rebase-oriented while leaving repository merge strategy governance as a future settings-as-code concern. `make linear-history-check-test` covers event-range resolution, missing-base fetches, missing SHA failures, upstream/local fallback, single-commit inspection, and merge-commit rejection. |
| 2026-05-29 | jindyzhao | PR impact reference hardening landed locally: PR-only `make pr-impact-reference-check` now requires PR bodies to link an issue or ADR when the changed-file set touches `docs/adr/` or future `internal/sandbox/` paths. Accepted references include `#123`, GitHub issue/PR URLs, `ADR-0001`, or concrete `docs/adr/ADR-0001-*.md` paths, and tests cover path triggers, event JSON body loading, diff-range fetching, and no-impact bypass. |
| 2026-05-30 | jindyzhao | PR impact reference event-file regularity hardened locally: `scripts/pr_impact_reference` now rejects symlink/non-regular `GITHUB_EVENT_PATH` files and symlinked or non-directory parent paths before parsing PR body JSON, keeping high-impact PR traceability evidence tied to the direct GitHub event payload. |
| 2026-05-29 | jindyzhao | Docker base-image pinning hardening landed locally: `make forbidden-latest` now scans committed Dockerfiles and rejects external `FROM` images without `@sha256:<digest>`, while allowing `scratch` and previous build stages. `go test ./scripts` carries positive and negative Dockerfile fixtures. |
| 2026-05-29 | jindyzhao | ADR Consequences governance hardening landed locally: all existing ADRs now include a non-empty `Consequences` section, and `make adr-check` rejects future ADRs with missing or empty Consequences sections through fixture-backed root `scripts` tests. |
| 2026-05-29 | jindyzhao | DCO AI-branding hardening landed locally: `make dco-check` now rejects commit messages or trailers that contain `Generated-by:`, AI co-author trailers, or common model/tool names, with temporary git-repository fixtures proving the failure modes. |
| 2026-05-30 | jindyzhao | DCO noreply-author hardening landed locally: `make dco-check` now rejects GitHub `users.noreply.github.com` author emails even when the `Signed-off-by:` trailer matches, and `DCO.md` documents that GitHub noreply author addresses are rejected. Root `scripts` tests prove the rejection with a temporary git repository fixture. |
| 2026-05-29 | jindyzhao | Go coverage floor landed locally: `make go-coverage` now runs `scripts/check_go_coverage.sh`, filters generated API/Ent packages and root script-test aggregators, and requires selected handwritten Go packages with statements to stay at or above the default 40.0% package coverage floor. The former future-import coverage non-regression note is retired in favor of this executable gate, with base-branch delta comparison left as a later hardening step. |
| 2026-05-30 | jindyzhao | Webhook IM provider response strictness hardened locally: response bodies are now read through a 1 MiB plus-one cap so oversized upstream responses are detected instead of silently truncated, and 2xx JSON delivery envelopes reject non-object payloads, duplicate keys, or trailing values before raw provider metadata is retained. Unknown success metadata remains preserved for provider compatibility, with focused coverage in `go test -race -count=1 ./internal/providers/im/webhook`. |
| 2026-06-03 | jindyzhao | Go linting hardened locally for stale code paths: `.golangci.yml` now enables the built-in `unused` and `ineffassign` linters inside `make go-lint`, the existing unused package-scope helpers in alert ingest, evidence snapshot building, and M4 evidence-packet validation were removed, and a fake metrics provider test now asserts the intentionally grown seed slice instead of keeping an ineffectual assignment. This keeps future framework and evidence-gate code from retaining stale helper paths or ignored assignments that are not exercised by tests or production wiring. |
| 2026-05-29 | jindyzhao | Generated file header gate landed locally: `make generated-headers` now rejects OpenAPI Go, Ent, or frontend OpenAPI TypeScript generated outputs that lose their generator-owned header/direct-edit warning, with fixture-backed root `scripts` tests. |
| 2026-05-30 | jindyzhao | Generated file header source-file regularity hardened locally: `make generated-headers` now rejects symlink and other non-regular OpenAPI Go, Ent, or frontend OpenAPI TypeScript generated outputs plus symlinked or non-directory generated path parents before reading header markers. Root `scripts` tests cover explicit OpenAPI symlinks, Ent symlinks discovered through the generated tree, generated path parent rejection, and non-regular frontend generated paths. |
| 2026-05-29 | jindyzhao | ADR front-matter schema hardening landed locally: every ADR now declares machine-readable `id` and `title`, and `make adr-check` rejects missing required front-matter keys, unknown keys, invalid dates/statuses, and file/H1/front-matter identity drift. |
| 2026-05-29 | jindyzhao | Critical Go module pinning hardening landed locally: `make forbidden-latest` now rejects missing, indirect, non-concrete, or replaced root-module pins for critical first-import modules covering Ent, Temporal SDK, and OpenTelemetry. |
| 2026-05-30 | jindyzhao | Dependency policy source-file regularity hardened locally: `make forbidden-latest` now rejects symlink and other non-regular `docs/design/DEPENDENCIES.md` inputs plus symlinked or non-directory parent paths before accepting Go `replace` allowlists, and `make go-licenses-check` applies the same policy-file and parent-path boundary before reading the SPDX allowlist. |
| 2026-05-29 | jindyzhao | Go tool directive pinning hardened locally: `make forbidden-latest` now parses Go 1.24+ `tool` directives and requires each external tool package path to resolve to a concrete `require` version pin for the module that provides it. Root `scripts` tests cover valid external tool pins, missing backing requires, non-concrete tool module versions, and longest-prefix module resolution. |
| 2026-05-29 | jindyzhao | Integration-test host-network boundary hardened locally: `make testcontainers-contract` now parses full Go test ASTs, keeps the existing Testcontainers database harness requirement, and rejects direct package-level host/public network entry points such as `net/http.Get`, `net/http.DefaultClient`, and `net.Dial` in executable Go tests. `httptest.NewServer` with injected clients/transports remains allowed. |
| 2026-05-30 | jindyzhao | Markdownlint config source-file regularity hardened locally: `make markdownlint` now rejects symlink and other non-regular `docs/design/ci/markdownlint/.markdownlint-cli2.jsonc` inputs plus symlinked or non-directory parent paths before invoking the pinned local markdownlint binary. |
| 2026-05-29 | jindyzhao | Project terminology gate landed locally: `make docs-hygiene` now runs `scripts/check_project_terminology.sh` against `docs/design/ci/terminology.tsv`, keeping OpenClarion positioned as intelligent alert analysis while allowing signal-capable architecture aliases only in the documented boundary docs. |
| 2026-05-30 | jindyzhao | Project terminology policy source-file regularity hardened locally: `make docs-hygiene` now rejects symlink and other non-regular `docs/design/ci/terminology.tsv` inputs plus symlinked or non-directory parent paths before scanning governed docs. This keeps product-positioning governance tied to the reviewed terminology policy file instead of an indirect path. |
| 2026-05-28 | jindyzhao | M5 diagnosis room policy boundary landed locally: `internal/usecases/diagnosisroom` validates configured short-conversation bounds, duplicate message IDs, in-flight turn rejection, session/idle timeout checks with caller-supplied `now`, context byte budget for evidence/conversation/message mounts, and unsafe denylist matches. `make diagnosis-room-policy-test` is part of CI; WebSocket/Temporal/persistence integration remains pending. |
| 2026-05-28 | jindyzhao | M5 auth/RBAC/WS ticket boundary landed locally: `internal/usecases/ports/providers.go` and `diagnosisauth` define provider-neutral principals, owner/admin RBAC, cryptographically random short-lived WebSocket tickets, atomic single-use consumption, TTL/wrong-session rejection, and a defensive in-memory store for tests/local development. `make diagnosis-auth-test` is part of CI; OIDC, PostgreSQL ticket storage, and WebSocket wiring remain pending. |
| 2026-05-28 | jindyzhao | M5 PostgreSQL WS ticket store landed locally: `internal/persistence/ent/schema/diagnosisauthticket.go` and migration `internal/persistence/migrations/20260528122257_diagnosis_auth_tickets.sql` add `diagnosis_auth_tickets`, while `internal/persistence/repository/diagnosis_auth_ticket_store.go` stores only `sha256(token)`, normalizes UTC microsecond times, maps duplicate token hashes to `ErrAlreadyExists`, rejects expired/unknown tickets, and uses a conditional update so concurrent consumers have exactly one successful winner. `make diagnosis-auth-test` now covers the persistence path; OIDC and WebSocket handler wiring remain pending. |
| 2026-05-28 | jindyzhao | M5 OIDC AuthProvider landed locally: `internal/providers/auth/oidc` verifies signed OIDC ID tokens via discovery/JWKS with client ID audience checks, extracts provider-neutral principals, maps configured role claims to owner/admin roles, rejects malformed bearer values, invalid audiences, expired tokens, and malformed role claims. `internal/providers/auth/fake` provides deterministic scripted auth for future transport tests. `make diagnosis-auth-test` now covers OIDC, fake auth, usecase RBAC/ticket rules, and PostgreSQL ticket storage; WebSocket ticket endpoint/upgrade wiring remains pending. |
| 2026-05-28 | jindyzhao | M5 WebSocket auth transport boundary landed locally: OpenAPI now includes `POST /api/v1/diagnosis/ws-ticket`, `internal/transport/http` authenticates OIDC Bearer tokens through `AuthProvider`, resolves session ownership through an injected resolver, issues short-lived usecase tickets, and registers `GET /ws/diagnosis` as a non-OpenAPI upgrade route that checks browser origin before consuming a ticket. Tests cover ticket issuance, malformed/unauthorized/not-found requests, ticket-required upgrade, redacted ticket handoff, and origin rejection without burning the ticket, including userinfo-bearing, path-bearing, query-bearing, fragment-bearing, non-http(s), and cross-host Origin values. Full chat relay, DiagnosisRoomWorkflow, ChatSession/ChatTurn persistence, audit events, and final notification remain pending. |
| 2026-05-28 | jindyzhao | M5 ChatSession/ChatTurn persistence boundary landed locally: `internal/domain/chat.go`, Ent schemas, Atlas migration `20260528132100_chat_sessions_turns.sql`, and `DiagnosisRepository` now persist one diagnosis-room session per `DiagnosisTask`, immutable owner/session identity, lifecycle close metadata, and append-only transcript turns with per-session `message_id` and `sequence` uniqueness. `make diagnosis-chat-persistence-test` is in CI; workflow/WS relay and final close notification remain pending. |
| 2026-05-28 | jindyzhao | M5 DiagnosisRoomWorkflow transcript persistence landed locally: `DiagnosisRoomWorkflow` now owns Update/Query/Signal/timer state transitions, calls the diagnosis-room policy Validator before accepting user turns, ensures the `ChatSession`, runs `RunDiagnosisTurn` through `ContainerProvider.Run`, validates V1 diagnosis-turn `output.json`, persists the user+assistant `ChatTurn` pair idempotently through `PersistDiagnosisTurn`, then appends the pair to workflow state. `make diagnosis-room-workflow-test` is in CI; WS relay, lifecycle audit events, and final close notification remain pending. |
| 2026-05-28 | jindyzhao | M5 WebSocket relay landed locally: `ports.DiagnosisRoomWorkflowClient` isolates transport from Temporal, `DiagnosisRoomClient` maps session ids to stable room workflow ids and uses `UpdateWorkflow(... WorkflowUpdateStageCompleted)` plus `QueryWorkflow`, and `DiagnosisWebSocketRelay` forwards authenticated `submit_turn` / `query_state` frames after ticket consumption. Relay tests cover ready, turn result, state query, bounded Update timeout with "turn is still processing", and disconnect behavior that does not cancel the submitted Update. Lifecycle audit events, final close notification, and frontend route remain pending. |
| 2026-05-28 | jindyzhao | M5 diagnosis-room lifecycle audit landed locally: `EnsureDiagnosisChatSession`, `PersistDiagnosisTurn`, and `CloseDiagnosisChatSession` now record idempotent `DiagnosisTaskEvent` rows for room opened, turn persisted, and room closed. The close Activity also persists terminal `ChatSession.closed_at` / `close_reason`. `make diagnosis-room-workflow-test` covers direct Activity idempotency and workflow close scheduling; final close notification and frontend route remain pending. |
| 2026-05-28 | jindyzhao | M5 diagnosis-room close notification landed locally: `SendDiagnosisRoomCloseNotification` runs after terminal close persistence, reuses `IMProvider` with a diagnosis-task-scoped idempotency key, sends the alert-group close notification, and records `diagnosis_room.close_notification_sent` so retries skip duplicate provider calls. `make diagnosis-room-workflow-test` covers workflow scheduling and direct Activity idempotency; frontend route remains pending. |
| 2026-05-28 | jindyzhao | M5 diagnosis-room frontend route landed locally: `/diagnosis-room` is a thin App Router page over `web/src/features/diagnosis-room`; ticket issuance uses generated OpenAPI types, WebSocket logic is isolated to the feature module, and Playwright route smoke now proves ticket bootstrap, ready/state frames, and one submit/turn-result exchange against a mocked API/WebSocket endpoint in a production Next server. Live browser acceptance against a real backend/worker stack remains pending. |
| 2026-05-28 | jindyzhao | M5 live diagnosis-room browser smoke harness landed locally: `make diagnosis-live-browser-smoke` runs production Next.js plus `web/playwright.live.config.ts` unless `OPENCLARION_LIVE_WEB_BASE_URL` points to an existing frontend, requires live API/session/token env, verifies one browser connect -> state -> submit_turn -> turn_result path, and writes a JSON proof. Real-stack execution remains pending. |
| 2026-05-28 | jindyzhao | M5 diagnosis-room runtime wiring landed locally: `cmd/openclarion` now builds diagnosis Activity and HTTP server options from env. `OPENCLARION_SANDBOX_IMAGE_REF` plus `OPENCLARION_SANDBOX_AGENT_CONFIG_ROOT` enables the Docker-backed per-turn `ContainerProvider`; `OPENCLARION_DIAGNOSIS_OIDC_ISSUER_URL` plus `OPENCLARION_DIAGNOSIS_OIDC_CLIENT_ID` enables OIDC auth, PostgreSQL WebSocket tickets, ChatSession session resolution, Temporal `DiagnosisRoomClient`, and optional `OPENCLARION_DIAGNOSIS_ALLOWED_ORIGINS` CORS/WebSocket origin policy. Tests cover configured/unconfigured/partial startup paths and reject userinfo-bearing allowed-origin values before normalization. Real-stack browser evidence remains pending. |
| 2026-05-29 | jindyzhao | M5 diagnosis-room creation path landed locally: `POST /api/v1/diagnosis/rooms` authenticates the OIDC bearer principal, loads a frozen EvidenceSnapshot, starts `DiagnosisRoomWorkflow` through `DiagnosisRoomStarter`, waits until workflow startup has persisted its `DiagnosisTask` and `ChatSession`, then returns the session/workflow handle. `make diagnosis-live-browser-smoke` can now create a room from `OPENCLARION_LIVE_EVIDENCE_SNAPSHOT_ID` instead of requiring a pre-existing session. Real-stack browser evidence remains pending. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof validator landed locally: `scripts/diagnosis_live_smoke_output` rejects malformed, trailing-log-polluted, non-HTTP(S), missing-session, zero-message, weak-evidence, and mismatched room-creation JSON proofs. It now also requires request metadata plus structured browser observations proving state load, `turn_result`, submitted-message visibility, connected status after the turn, assistant-turn count increment, user+assistant transcript pair increment, completed-turn log consistency with the assistant count, and transcript count consistency with the pair model. `make diagnosis-live-smoke-output-test` is wired into CI, and `make diagnosis-live-browser-smoke` now runs the validator before reporting success. Real-stack browser execution evidence remains pending. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof URL/time validation hardened locally: `scripts/diagnosis_live_smoke_output` now rejects future `checked_at` timestamps and rejects `web_base_url` / `api_base_url` values carrying user info, query strings, or fragments, so retained browser-smoke proofs cannot hide credentials or non-base URL state in the proof metadata. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof request binding hardened locally: `make diagnosis-live-browser-smoke` now records the exercised request mode, session id, evidence snapshot id, and message length in the retained JSON proof, and `scripts/diagnosis_live_smoke_output` rejects request/session/message/evidence mismatches before the artifact can support live M5 acceptance. Real-stack browser execution evidence remains pending. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof scalar canonicalization hardened locally: `scripts/diagnosis_live_smoke_output` now rejects whitespace-padded `checked_at`, string evidence snapshot IDs, completed-turn text, and created-room run IDs instead of trimming them during validation. Retained browser-smoke proofs must therefore carry stable machine-readable scalar values before supporting live M5 acceptance. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room evidence snapshot ID canonicalization reconciled locally: `scripts/run_diagnosis_live_browser_smoke.sh` now writes numeric proof IDs, `scripts/diagnosis_live_smoke_output` accepts only `null` or positive JSON integer values for top-level and request `evidence_snapshot_id` fields, and the created-room proof fixtures now use numeric IDs instead of quoted numbers. This aligns the proof producer, validator, and tests with the documented canonical-scalar requirement before any real-stack browser proof can support M5 acceptance. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room submitted-message proof binding hardened locally: the live Playwright proof now records the browser-submitted message length and SHA-256 digest, the smoke harness writes the trimmed message length and digest into retained request metadata, and `scripts/diagnosis_live_smoke_output` rejects mismatches between browser observation and top-level proof metadata. This prevents retained live browser proof from claiming a different submitted message than the one exercised in the UI without retaining message plaintext. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof identity hygiene hardened locally: `scripts/diagnosis_live_smoke_output` now rejects whitespace-containing or overlong top-level/request session IDs plus created-room workflow/run IDs before accepting a retained browser proof. This keeps real-stack proof identifiers stable for WebSocket, Temporal, and audit correlation before final M5 live acceptance. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof evidence text hygiene hardened locally: `scripts/diagnosis_live_smoke_output` now rejects missing, whitespace-padded, multiline/tabbed, or over-512-byte retained `evidence` summaries before accepting browser proof. The summary must remain a bounded single-line `turn_result` claim rather than arbitrary logs. |
| 2026-06-04 | jindyzhao | M5 local real-stack diagnosis-room browser proof captured: `make diagnosis-live-browser-smoke` passed against local PostgreSQL, Temporal, `openclarion serve`, OIDC, production Next.js, Playwright Chromium, and a digest-pinned temporary diagnosis-turn runner. The retained proof is `artifacts/m5/manual-2026-06-04/diagnosis-live-browser-smoke-local.json` (SHA-256 `4fa36ef42ba744721172d818f09453c572839c52210da789ea34c10c88c92be8`) and shows create-room mode, one browser connect -> state -> submit_turn -> turn_result round trip, one persisted user+assistant `ChatTurn` pair, and validator-accepted browser observations. The smoke harness now normalizes `OPENCLARION_LIVE_BEARER_TOKEN` so callers may pass either a raw token or a `Bearer ...` header, while the browser still receives the raw token, and it emits canonical UTC `checked_at` timestamps accepted by `scripts/diagnosis_live_smoke_output`. This proves the local browser/API/WebSocket/Temporal/Docker-sandbox/persistence path for one turn, but it does not select a production agent runtime or prove live close-notification IM delivery. |
| 2026-06-04 | jindyzhao | M5 optional live close-notification proof path landed locally: `openclarion diagnosis-room-close` signals `DiagnosisRoomCloseSignal`, waits for the Temporal workflow completion, loads `diagnosis_room.closed` and `diagnosis_room.close_notification_sent` lifecycle events from PostgreSQL, and emits a strict JSON proof. `make diagnosis-live-browser-smoke` can include this proof when `OPENCLARION_LIVE_REQUIRE_CLOSE_NOTIFICATION=1`; `scripts/diagnosis_live_smoke_output` validates session/workflow/run binding, closed workflow state, browser turn-count consistency, event kinds/timestamps, diagnosis-task-scoped close-notification idempotency key format, and accepted/delivered IM provider status. This is harness and validator support only; retained live close-notification IM delivery evidence is still pending. |
| 2026-06-04 | jindyzhao | M5 close-notification live-smoke readiness preflight landed locally: `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=diagnosis-live-browser-smoke` now validates live API/web URL shape, bearer-token shape, positive evidence snapshot IDs, and, when close-notification proof is required, local `DATABASE_URL`, `TEMPORAL_HOST_PORT`, close timeout/reason shape, and either Webhook IM configuration or `DIAGNOSIS_LIVE_SMOKE_ASSUME_WORKER_READY=1`. The output keeps tokens, URLs, session ids, database URLs, Temporal addresses, and Webhook endpoints out of the retained readiness JSON. This improves operator setup for the pending live close-notification IM proof without claiming that proof. |
| 2026-06-04 | jindyzhao | M5 close audit conclusion snapshot landed locally: `CloseDiagnosisChatSession` now writes a structured `final_conclusion` block into the idempotent `diagnosis_room.closed` event. Empty rooms record `not_available`; rooms with accepted turns bind the close audit to the latest persisted assistant `ChatTurn`, including assistant message id, sequence, occurrence time, bounded content, confidence, and human-review flag. The same close result is returned to workflow state, exposed through the `state` Query / diagnosis WebSocket read model, and displayed in the diagnosis-room workbench after close. This stores the room-close conclusion in the append-only audit stream without adding a new table, new workflow, or new CI job. |
| 2026-06-05 | jindyzhao | M5 close-notification proof conclusion binding hardened locally: `openclarion diagnosis-room-close` now decodes the strict `diagnosis_room.closed` payload, emits the workflow and close-event `final_conclusion` blocks in the retained proof, and rejects mismatches between the Temporal completion result and the append-only audit event. `scripts/diagnosis_live_smoke_output` now requires optional close proofs to bind the latest assistant-turn conclusion fields, canonical assistant occurrence time, confidence, and human-review flag before a retained close-notification artifact can support M5 claims. This strengthens the manual proof path without adding a CI gate or claiming a live IM delivery run. |
| 2026-05-29 | jindyzhao | Retained live proof source-file regularity hardened locally: `scripts/report_live_smoke_output` and `scripts/diagnosis_live_smoke_output` now reject symlink and other non-regular proof-file paths before reading JSON. This gives M2 report smoke and M5 browser smoke retained evidence the same direct-file boundary already used by M4 runtime decision artifacts. |
| 2026-05-29 | jindyzhao | M4 runtime smoke output source-file regularity hardened locally: `scripts/agent_runtime_smoke_output` now rejects symlink and other non-regular output proof paths before opening them, then reads through a hard cap rather than trusting pre-read file size. This keeps candidate runtime smoke artifacts direct, bounded, and audit-friendly before any M4 baseline decision can cite them. |
| 2026-05-29 | jindyzhao | M4 topology helper source-file regularity hardened locally: `scripts/agent_tool_topology_lookup` now rejects symlink and other non-regular topology JSON paths before reading. This keeps future sandbox runtime helper inputs direct, bounded, and reviewable when helper output is used as evidence for report quality comparison. |
| 2026-05-29 | jindyzhao | M4 custom thin runner ADR-0013 source-file regularity hardened locally: the runner now rejects symlink and other non-regular evidence, conversation, and message JSON paths before reading and hashing them. This keeps the local contract proof from accepting indirect mounted inputs while still avoiding planner, memory, approval, or runtime-framework behavior in Go. |
| 2026-05-30 | jindyzhao | SQLite-in-tests gate hardening landed locally: `make forbidden-sqlite` now enumerates Go test paths, rejects symlink/non-regular `_test.go` entries before scanning, and carries root `scripts` fixtures for no-test skip, allowed Go tests, SQLite import/DSN rejection, non-test Go exclusion, and indirect-path rejection. |
| 2026-05-30 | jindyzhao | D2 oapi strict-adapter deferral closed locally: the pinned `oapi-codegen-exp v0.1.0` output still exposes a small generated `ServerInterface` surface, `internal/transport/http.Server` asserts that interface directly, and endpoint tests cover the current handler paths. V1 keeps the direct generated interface instead of adding a handwritten strict adapter unless upstream support or measured boilerplate changes the evidence. |
| 2026-05-30 | jindyzhao | D10 phase-checklist split deferral re-evaluated after M2/M5 growth. `docs/design/CHECKLIST.md` remains a single 232-line milestone checklist because detailed implementation history is already split across `CURRENT_STATE.md`, `docs/roadmap/tasks.md`, phase docs, and M4 evidence documents. The deferral trigger now requires concrete size, ownership, or reviewer-friction evidence before splitting the checklist. |
| 2026-05-30 | jindyzhao | OpenAPI fingerprint spec-input strictness hardened locally: `make openapi-fingerprint` now rejects duplicate YAML keys, multi-document OpenAPI YAML, anchors, merge keys, and aliases before hashing critical nodes, complementing the existing strict JSON lock parser. |
| 2026-05-30 | jindyzhao | EvidenceSnapshot nested JSON strictness hardened locally: `internal/usecases/evidencebuild.BuildSnapshot` now rejects duplicate object keys and trailing JSON values in `AlertGroup.Dimensions` and `AlertEvent.RawPayload` before canonicalizing and hashing the frozen evidence payload. |
| 2026-05-30 | jindyzhao | M5 OIDC claim parsing hardened locally: OIDC verification now runs before custom claim handling; malformed or trailing claim payload JSON is rejected during `go-oidc` verification, and signed ID tokens whose JWT payload contains duplicate claim names are rejected after verification but before role-claim mapping, preventing ambiguous RBAC input from being accepted after standard JSON decoding would collapse duplicate claim content. |
| 2026-05-30 | jindyzhao | M2 report retained JSON strictness hardened locally: SubReport, FinalReport, and ReportNotificationDelivery domain constructors now reject duplicate object keys and trailing JSON values before retained JSON can be persisted for audit/replay. |
| 2026-05-30 | jindyzhao | OpenAI-compatible provider response-envelope strictness hardened locally: successful Chat Completions responses are now bounded to 4 MiB and reject duplicate object keys or trailing JSON values before unmarshalling, while unknown provider metadata remains ignored for compatibility. JSON API error envelopes use the same duplicate-key check before capability fallback decisions, so ambiguous upstream errors cannot silently switch the provider into `json_object` mode. `go test -race -count=1 ./internal/providers/llm/openai` covers ambiguous success envelopes, oversized success envelopes, plain-text API errors, and ambiguous capability-probe errors. |
| 2026-05-30 | jindyzhao | HTTP and diagnosis WebSocket input JSON strictness hardened locally: `internal/transport/http/json_decode.go` now routes JSON request bodies through `internal/strictjson` with duplicate-key, unknown-field, trailing-value, and bounded-size rejection before report replay, diagnosis room creation, or WebSocket ticket requests reach usecases. Diagnosis WebSocket client frames now use the same strict parser before workflow Update/Query dispatch, so ambiguous interactive frames cannot silently overwrite fields. |
| 2026-05-30 | jindyzhao | Prometheus provider endpoint configuration hardened locally: `prometheus.NewProvider` now rejects userinfo-bearing endpoint URLs before constructing the upstream client, keeping runtime endpoint credentials out of base URLs while leaving Bearer authentication on the explicit provider option. |
| 2026-05-30 | jindyzhao | Provider endpoint credential hygiene hardened locally: OpenAI-compatible LLM base URLs, IM Webhook URLs, and OIDC issuer URLs now reject URL userinfo so runtime credentials stay on explicit API-key or Bearer-token configuration fields. |
| 2026-05-30 | jindyzhao | OpenTelemetry OTLP endpoint credential hygiene hardened locally: tracing configuration now rejects `OTEL_EXPORTER_OTLP_ENDPOINT` and `OTEL_EXPORTER_OTLP_TRACES_ENDPOINT` values that include URL userinfo before constructing the OTLP HTTP exporter. |
| 2026-05-30 | jindyzhao | M5 diagnosis-room allowed-origin credential hygiene hardened locally: `normalizeBrowserOrigin` now rejects URL userinfo in `OPENCLARION_DIAGNOSIS_ALLOWED_ORIGINS` entries instead of silently canonicalizing `https://user@example.test` to `https://example.test`. Runtime wiring tests assert the env-level error names the allowed-origin setting and userinfo rejection. |
| 2026-05-30 | jindyzhao | M5 diagnosis-room policy evidence strictness hardened locally: `MountContextBytes` now rejects duplicate object keys and trailing JSON values in mounted evidence before context budgeting and sandbox handoff. This keeps the short-conversation control-plane boundary aligned with the stricter `ContainerProvider` evidence contract without adding any agent-runtime dependency or runtime-specific branch. |
| 2026-05-30 | jindyzhao | M5 diagnosis-room startup evidence strictness hardened locally: `DiagnosisRoomStarter` and `DiagnosisRoomWorkflow` now require room evidence to be a duplicate-key-free JSON object and reject trailing JSON values before task/session creation or sandbox handoff. This keeps the lifecycle boundary aligned with the strict ContainerProvider evidence contract without introducing any agent-runtime dependency or runtime-specific branch. |
| 2026-05-30 | jindyzhao | M5 diagnosis-room lifecycle audit payload strictness hardened locally: lifecycle event payloads now reject duplicate object keys and trailing JSON values before append-only `DiagnosisTaskEvent` persistence. This keeps retained room audit evidence unambiguous while preserving the runtime-neutral control-plane boundary. |
| 2026-05-30 | jindyzhao | M5 diagnosis-turn output strict JSON boundary hardened locally: `internal/usecases/diagnosisroom.ParseTurnOutput` now rejects duplicate object keys and trailing JSON values before JSON Schema validation, and the embedded V1 schema is strict-checked before compilation. This keeps per-turn sandbox output acceptance aligned with the shared `ContainerProvider` and retained-proof strict JSON boundary. |
| 2026-05-30 | jindyzhao | M2 SubReport evidence reference integrity hardened locally: `internal/usecases/reportdraft.ParseSubReport` now rejects duplicate top-level `evidence_refs` and rejects findings whose `evidence_id` is not listed in `evidence_refs`. This keeps persisted report drafts and later M4 sandbox quality evidence tied to an explicit, deduplicated audit index. |
| 2026-05-30 | jindyzhao | Go toolchain YAML parsing hardening landed locally: `make go-toolchain-check` now rejects duplicate keys, multi-document workflow YAML, anchors, aliases, and merge keys before comparing Go version declarations, while tolerating unrelated GitHub Actions / golangci-lint fields outside the gate-owned version metadata. |
| 2026-05-30 | jindyzhao | Gate hardening checklist source-file regularity hardened locally: `scripts/gate_hardening_check` now rejects symlink and non-regular CI schedule or maturity-checklist inputs, including symlinked parent directories, before parsing, keeping maturity evidence tied to direct repository Markdown files. |
| 2026-05-31 | jindyzhao | GitHub Actions semantic linting landed locally: `make actionlint` runs pinned `actionlint v1.7.12` over committed workflows and the CI bundle now includes a `GitHub Actions lint` job. This complements `workflow-parity` by checking workflow YAML syntax, expression semantics, and job graph references while keeping workflow steps bound to repository-owned `make` targets. |
| 2026-05-31 | jindyzhao | Shell script syntax validation landed locally: `make shell-syntax-check` discovers Git-tracked `.sh` files and shell-shebang scripts, requires `.sh` entries to be direct regular files, uses bounded first-line reads for shebang detection, and runs `bash -n` through a fixture-backed Go harness. The CI bundle now blocks syntactically invalid repository shell scripts before they can break local or remote gates. |
| 2026-05-31 | jindyzhao | YAML configuration syntax validation landed locally: `make yaml-syntax-check` parses every Git-tracked `.yml` / `.yaml` file with `go.yaml.in/yaml/v3`, rejects symlink/non-regular inputs, enforces a bounded file size, requires a single-document scalar-key YAML tree, and rejects duplicate keys, anchors, aliases, and merge keys so configuration mistakes cannot be silently overwritten or hidden behind parser-specific expansion behavior. |
| 2026-06-01 | jindyzhao | Core environment/config boundary analyzer hardened locally: `openclarion-arch` rejects direct process-environment reads/mutations and process flag parsing in non-test `internal/domain` and `internal/usecases` packages, keeping core alert-analysis logic driven by injected configuration rather than hidden process state. |
| 2026-06-02 | jindyzhao | Repository size budget gate landed locally: `make repo-size-check` now scans Git-visible tracked and non-ignored untracked working-tree files, rejects symlink/non-regular paths, and enforces default 1 MiB per-file / 25 MiB total budgets. The gate is wired into `make ci`; CI runs it in the existing PR budget job with `make repo-size-check-test`, preventing large retained evidence, generated blobs, or accidental binary files from entering review without adding another top-level workflow check. |
| 2026-06-03 | jindyzhao | Repository size budget artifact-extension policy hardened locally: `make repo-size-check` now also rejects common archive, executable, object, packaged JVM, WebAssembly, and database artifact extensions for Git-visible files, keeping the existing size-budget gate as the single repository artifact boundary instead of adding a duplicate committed-artifact workflow job. |
| 2026-05-30 | jindyzhao | M5 diagnosis WebSocket default Origin validation hardened locally: the default transport-level `CheckOrigin` now requires browser-shaped `http` or `https` origins with matching host and no userinfo, path, query, or fragment before a WebSocket ticket can be consumed. Transport tests prove rejected malformed or cross-host origins leave the single-use ticket reusable. |
| 2026-05-30 | jindyzhao | D1 workflow-engine spike deferral closed locally: shipped M2 report workflows, M5 diagnosis-room workflow coverage, local report E2E, and frontend diagnosis smoke now provide enough Temporal evidence that the old River+sqlc spike is no longer a pre-commit architecture cross-check. Reopening requires measured Temporal operations, delivery, or reliability pain. |
| 2026-05-30 | jindyzhao | Documentation metadata freshness hardened locally: `scripts/docs_metadata_check` now rejects governed Markdown files whose `Last updated` metadata is older than their latest dated status/changelog table row, and `make docs-hygiene` runs it alongside language and terminology checks. The first run fixed stale roadmap and deferred-follow-up headers. |
| 2026-06-04 | jindyzhao | M4 quality manifest preparation helper landed locally: `make sandbox-m4-quality-manifest-prepare` scans retained `direct/<scenario>/<case>.json` and `sandbox/<scenario>/<case>.json` SubReport pairs, requires all three alert-analysis scenarios, requires globally unique case IDs before downstream review binding, parses both sides through the production SubReport parser, and emits a portable quality manifest with shared `snapshot:<positive-id>` evidence refs. This reduces manual pairing and evidence-ref mistakes without adding a CI gate; real representative quality comparison, human review, and the recorded M4 decision remain pending. |
| 2026-06-04 | jindyzhao | M4 quality sample readiness preflight landed locally: `make manual-evidence-readiness` now includes the manual `sandbox-m4-quality-manifest-prepare` path, checking `ROOT`, `SAMPLE_BASIS`, `OUT`, direct/sandbox sample layout, paired-case counts, and required `single_alert` / `cascade` / `alert_storm` coverage without printing local paths or sample case IDs. This improves the real quality-comparison preparation path without adding a CI workflow or treating sample selection, human review, or the M4 decision as complete. |
| 2026-06-04 | jindyzhao | M4 persisted quality sample export landed locally: `make sandbox-m4-quality-sample-export` reads an operator-authored case selection file, loads persisted direct/sandbox SubReport rows from PostgreSQL, validates strict selection JSON, scenario matches, shared EvidenceSnapshot IDs, production SubReport JSON, and an empty output root, then writes the retained `direct/<scenario>/<case>.json` and `sandbox/<scenario>/<case>.json` layout for manifest preparation. `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=sandbox-m4-quality-sample-export` now preflights the database URL presence, selection file, and output root without printing their values. The export does not choose representative samples, compare quality, or accept a runtime candidate. |
| 2026-06-04 | jindyzhao | M4 runtime baseline decision hardening landed locally: `scripts/sandbox_m4_decision` now treats loopback or `localhost` registry runtime candidates as local smoke evidence only, forcing `defer` instead of allowing `proceed`; `scripts/sandbox_m4_evidence_packet` rejects hand-edited `proceed` decision outputs with loopback runtime refs before freezing a packet. This preserves the custom thin runner's ephemeral-registry proof path while preventing local-only image refs from being accepted as a publishable runtime baseline. |
| 2026-06-04 | jindyzhao | M4 review-evidence retained-output hardening landed locally: `make sandbox-m4-review-evidence-template OUT=...` now refuses existing output paths, including symlinks, and creates draft review evidence with exclusive file creation so retained human-review drafts cannot be silently overwritten in place. The helper remains fail-closed and still requires real representative samples plus operator review before any M4 decision can proceed. |
| 2026-06-04 | jindyzhao | M4 sandbox SubReport generation bridge landed locally: `make sandbox-m4-subreport-generate` loads one persisted `EvidenceSnapshot`, wraps its payload with snapshot ID, canonical `snapshot:<id>` ref, digest, scenario, and group metadata, runs the configured digest-pinned candidate image through the existing Docker `ContainerProvider`, validates `output.json` through the production SubReport parser, requires the snapshot ref in `evidence_refs`, and persists an idempotent sandbox SubReport row for later quality sample export. This closes candidate-output persistence for manual retained samples without comparing quality, selecting representative cases, adding a CI gate, or accepting a runtime. |
