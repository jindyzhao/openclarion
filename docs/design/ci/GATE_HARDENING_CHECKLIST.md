# Gate Hardening Checklist

This checklist audits the maturity of each activated Progressive Gate Schedule
row in [README.md](README.md). It is intentionally gate-focused: OpenClarion's
product direction remains intelligent alert analysis, while this document
keeps the CI and governance framework honest as the implementation broadens.

The companion checker, `make gate-hardening-check`, proves that every activated
Schedule row has exactly one record here and that records use concrete evidence
instead of placeholders.

## Maturity Levels

| Level | Meaning |
|-------|---------|
| baseline | The gate blocks its main drift mode but still has a narrow rule surface. |
| hardened | The gate has committed positive and negative coverage and is wired into local and remote validation. |
| mature | The gate covers the current production contract with focused tests, drift checks, and documented operating limits. |
| manual | The gate is a retained evidence or live-smoke workflow that cannot safely run in normal CI. |
| partial | The gate records useful evidence but cannot accept the architectural baseline yet. |
| replaced | The gate's original implementation was retired after equivalent or stronger coverage landed elsewhere. |

## Gate Maturity Records

| Gate | Maturity | Evidence | Next hardening |
|------|----------|----------|----------------|
| docs hygiene (English-only, terminology, proof-state, and metadata freshness checks) | hardened | `make docs-hygiene`; terminology TSV keeps product wording on intelligent alert analysis, the E2E verdict check keeps proof-state labels defined, and `scripts/docs_metadata_check` keeps `Last updated` metadata aligned with dated status/changelog rows. | Extend rule tables only when a real naming, evidence-taxonomy, or metadata drift appears. |
| ADR index, front matter, Consequences, supersession, and immutability validation | mature | `make adr-check`; fixture tests cover metadata, consequences, supersession, and base-aware immutability. | Add label-based governance timing only when external contributor flow exists. |
| Markdown link, anchor, and docs reachability validation | mature | `make links-check`; fixtures cover anchors, target paths, and orphan docs. | Keep external link liveness non-blocking if a scheduled workflow is added. |
| External HTTPS link inventory | manual | `make external-links-check` inventories links locally and rejects symlink/non-regular governed Markdown inputs plus symlinked `docs/` subtrees before reading; `.github/workflows/external-links.yml` runs `OPENCLARION_EXTERNAL_LINKS_LIVE=1 make external-links-check` on a weekly/manual default-branch workflow. Reserved example domains are skipped during live checks. | Keep live liveness non-PR-blocking and add an ignore file only with owner and expiry if unstable real links appear. |
| Markdown structure lint | hardened | `make markdownlint`; pinned local CLI and wrapper tests cover dependency availability plus regular-file/no-symlink/no-symlink-parent/no-non-directory-parent config behavior. | Expand rules only after style debt is first normalized. |
| Forbidden imports (Gin/Echo/Fiber/Redis/Mongo/vector) | replaced | Legacy shell gate retired after `openclarion-arch` analyzer equivalence fixtures. | Keep deny-list drift covered inside analyzer tests. |
| Forbidden mutable dependency pins (go.mod, package.json, Dockerfile) | mature | `make forbidden-latest`; tests cover Go critical pins, Go tool directive backing pins, npm exact pins, `@types/node` / CI Node major alignment, replace allowlists, dependency-policy regularity, and Dockerfile digest pinning. | Add release-age allowlists only with owner and expiry. |
| Forbidden oapi-codegen v2 / openapi.compat.yaml | baseline | `make forbidden-oapi-v2`; keeps ADR-0007 toolchain choice from regressing. | Retire only if ADR-0007 is superseded. |
| Forbidden SQLite in Go tests | hardened | `make forbidden-sqlite`; `go test ./scripts -run TestForbiddenSQLite` covers no-test skip, allowed Go tests, SQLite import/DSN rejection, non-test Go exclusion, symlink/non-regular `_test.go` fail-closed behavior, and symlinked first-party test search directories. | Broaden only if a second embedded database appears. |
| PR title Conventional Commit validation | hardened | `make pr-title-check`; PR-only CI job plus script fixtures, including 120/121-character boundary coverage. | Align allowed types with release automation when releases start. |
| PR description risk/rollback validation | hardened | `make pr-description-check`; event JSON and local env fixtures cover required sections. | Add path-sensitive required sections only after reviewer workflow stabilizes. |
| PR template required-section validation | hardened | `make pr-template-check` runs inside the existing gate-hardening job; fixtures cover required sections, missing rollback, empty placeholders, and fenced-code false positives. | Add path-sensitive template sections only after reviewer workflow stabilizes. |
| Issue template contract validation | hardened | `make issue-template-check` runs inside the existing gate-hardening job; fixtures cover front-matter drift, missing sections, empty placeholders, and fenced-code false positives. | Add issue forms only if Markdown templates stop carrying enough triage structure. |
| PR impact reference validation | hardened | PR-only `make pr-impact-reference-check`; tests cover ADR and sandbox path triggers, issue references, ADR references, event JSON body loading, PR diff-range fetching, missing env pairs, and no-impact bypass. | Add more high-impact path prefixes only when the architecture boundary adds them. |
| DCO sign-off validation | hardened | `make dco-check`; temporary git fixtures cover matching, missing, mismatched, noreply-author, branded, and local fallback cases. | Add commit signature verification only when the contributor base needs stronger identity proof. |
| Workflow / Makefile parity | mature | `make workflow-parity`; fixtures cover registry, workflow naming, permissions, PR secrets, and pinning drift. | Add path-filter policy for workflow edits when PR volume grows. |
| GitHub Actions workflow semantic lint | hardened | `make actionlint`; pinned actionlint checks committed workflow syntax, expressions, job graph references, and GitHub Actions semantics in addition to OpenClarion's custom workflow-policy gate. | Add local workflow fixtures only if workflow count grows beyond the current single primary CI workflow. |
| Manual target isolation | hardened | `make manual-target-isolation` runs inside the existing gate-hardening job; policy TSV and tests keep live/manual smoke and evidence targets unreachable from `make ci` and workflow `run: make` entry points. | Add owner/expiry metadata only if manual target exceptions become temporary rather than inherent. |
| Go module: generate, vet, build, tests | mature | `make go-checks`; root packages run generation, vet, build, and `go test -race -count=1`, while the isolated diagnosis runner is vetted, race-tested, and built separately. `make ci` includes the same nested-module check. | Split longer packages only when wall-clock data requires it. |
| Go coverage floor | hardened | `make go-coverage`; enforces a package-level coverage floor for selected handwritten Go packages while excluding generated API/Ent code. | Replace the fixed floor with base-branch per-package deltas when stable branch protection data exists. |
| Testcontainers integration-test contract | mature | `make testcontainers-contract`; Go parser/AST checker plus fixtures cover real DB test packages, `testdata` exclusions, allowed `httptest` usage, and direct host/public network rejection. | Add runtime egress-block verification only when CI can run sandboxed integration tests. |
| Generated file header validation | hardened | `make generated-headers`; fixtures cover OpenAPI Go, Ent, and frontend generated families. | Add new generated families when introduced. |
| OpenAPI lint (vacuum lint --fail-severity error) | hardened | `make openapi-lint`; pinned `go tool` vacuum with project ruleset. | Tighten ruleset after API naming debt is explicitly cleared. |
| OpenAPI generation freshness (make generate no diff) | hardened | `make openapi-fresh`; snapshots generated Go server code. | Retire only if covered by repository-wide generation freshness. |
| Repository generation freshness and idempotence | mature | `make generate-fresh`; fixtures cover tracked drift, untracked drift, stable runs, and second-run nondeterminism. | Add per-generator diagnostics if output becomes large. |
| oapi-codegen-exp released-version pin check | hardened | Covered by `make forbidden-latest` and `go.mod` tool pin. | Keep dependency docs updated on tool upgrades. |
| Ent generation freshness | hardened | `make ent-fresh`; generated Ent tree is checked after schema changes. | Add schema diff summaries if migrations become difficult to review. |
| Atlas migration drift check | hardened | `make atlas-drift`; pinned Atlas image checks migration drift. | Keep manual smoke for destructive migration review. |
| Secret scanning (gitleaks) | mature | `make secrets-scan`; scans history plus current tracked and untracked non-ignored files, while `make allowlist-discipline` requires gitleaks allowlist owner, removal trigger, non-expired `Expires`, and a maximum 120-day review window. | Shorten the review window only if false-positive churn stays low. |
| Operations configuration hygiene | hardened | `make operations-config-hygiene`; fixture-backed tests cover safe placeholder endpoints, non-placeholder host rejection, URL userinfo/query/fragment boundaries, and browser durable storage API rejection across the settings surface. | Broaden the scanned path set only when a new operations configuration surface is introduced. |
| Go vulnerability scan (govulncheck) | hardened | `make govulncheck`; scans the root, linter, and isolated diagnosis-runner modules. | Add npm OSV scanning for frontend lockfiles separately. |
| Go license compliance | hardened | `make go-licenses-check`; checks root, linter, and isolated diagnosis-runner dependencies, including tests, against the regular-file `DEPENDENCIES.md` SPDX allowlist with non-symlink, directory-only parents, and validates non-empty owner, non-future reviewed date, and reason metadata on the allowlist policy line. | Add per-dependency exceptions only with owner, review date, and reason. |
| OSV npm lockfile scan | hardened | `make osv-scan`; scans committed npm package-lock files outside `node_modules` and fails when npm manifests lack lockfiles. | Add additional lockfile ecosystems only when they enter the repository. |
| Go lint baseline (golangci-lint) | mature | `make go-lint`; pinned golangci-lint plus custom analyzer plugin, version parity check, `unused` dead-code detection, and `ineffassign` ineffectual-assignment detection. | Keep analyzer scope small and fixture-backed. |
| Dependency update policy (Dependabot) | hardened | `make dependabot-policy-check` plus `.github/dependabot.yml`; weekly grouped GitHub Actions, Go, and npm updates without blanket ignores; the checker rejects broad ignores while allowing only documented ordinary version-update suppressions for `golang.org/x/tools` linter parity, `/web` `@types/node` CI Node major alignment, and `/web` TypeScript/ESLint coordinated toolchain majors. Security-update groups are required for both guarded ecosystems. | Add branch-protection policy when repository settings are managed as code. |
| Custom analyzer scaffold + version parity | mature | `make go-lint`; custom plugin tests and `scripts/check_lint_version.sh`. | Add analyzers only for real architecture boundaries. |
| Provider/domain forbidden-import analyzer | mature | `openclarion-arch` fixtures prove domain/usecase and provider import boundaries. | Keep legacy deny-list parity test in place. |
| Legacy forbidden-import bash deletion | mature | Equivalence was proven before deleting `scripts/check_no_forbidden_imports.sh`. | Re-run analyzer fixture audit after any deny-list expansion. |
| Ent transaction boundary analyzer | mature | `openclarion-arch` resolves Ent transaction calls with `go/types`. | Add negative fixtures for any future transaction helper aliases. |
| Runtime fake-provider analyzer | hardened | `openclarion-arch` rejects production `cmd/openclarion` fake-provider imports. | Broaden only if production wiring moves outside `cmd/openclarion`. |
| Structured logging analyzer | hardened | `openclarion-arch` rejects ad-hoc production `fmt.Print*` and standard log calls. | Add slog attribute policy if log schema stabilizes. |
| HTTP client boundary analyzer | hardened | `openclarion-arch` rejects default/bare outbound HTTP calls in production packages. | Add tracing propagation analyzer only if wrappers drift. |
| SQL open boundary analyzer | hardened | `openclarion-arch` rejects direct `database/sql.Open` outside persistence boundary. | Pair with future pool lifecycle policy if runtime wiring expands. |
| Process execution boundary analyzer | hardened | `openclarion-arch` rejects ad-hoc subprocess execution outside allowed boundaries. | Revisit allowlist after sandbox runtime baseline is accepted. |
| Core filesystem boundary analyzer | hardened | `openclarion-arch` rejects direct local filesystem access in deterministic core packages. | Add fixture-backed exceptions only if a reviewed core file abstraction lands. |
| Core clock boundary analyzer | hardened | `openclarion-arch` rejects direct `time.Now()` in deterministic core packages. | Add injected-clock conventions if new domains appear. |
| Core context boundary analyzer | hardened | `openclarion-arch` rejects root and detached context creation in deterministic core packages. | Add allowed boundary fixtures only if a core orchestration package is introduced. |
| Core environment/config boundary analyzer | hardened | `openclarion-arch` rejects direct process-environment reads/mutations and process flag parsing in deterministic core packages. | Add new allowed boundary fixtures only if configuration orchestration moves. |
| Reflection / unsafe import boundary analyzer | hardened | `openclarion-arch` rejects `reflect` and `unsafe` in handwritten production files. | Require ADR coverage for any future exception. |
| Panic boundary analyzer | hardened | `openclarion-arch` rejects production panic outside accepted recover boundaries. | Add fixtures for any approved panic-wrapper pattern. |
| Goroutine supervision boundary analyzer | hardened | `openclarion-arch` rejects raw production goroutines outside supervision boundary. | Introduce `internal/supervision` contract before allowing new patterns. |
| Core mutable global state analyzer | hardened | `openclarion-arch` rejects package-level mutable collection state in handwritten deterministic core packages, including pointer/interface-backed collection initializers. | Add narrow exceptions only with executable proof that the state is immutable after initialization. |
| OpenAPI breaking-change diff | hardened | `make openapi-breaking`; oasdiff soft-fails only until the recorded date, with root `scripts` tests covering pre-sunset soft-fail, sunset hard-fail, non-breaking success, and invalid date fail-closed behavior. | Remove soft-fail language after the sunset has passed. |
| OpenAPI critical-node fingerprint lock | mature | `make openapi-fingerprint`; lock generated from real OpenAPI nodes, with duplicate-key/multi-document/anchor/merge-key/alias OpenAPI YAML rejection and duplicate-key/trailing-value JSON lock rejection covered by root `scripts` tests. | Add new critical nodes in the same PR as API surfaces. |
| Documentation shipped-claims consistency | hardened | `make doc-claims-check`; validates shipped path hints in `CURRENT_STATE.md`. | Extend to command claims only when command metadata is structured. |
| Gate hardening maturity checklist | hardened | `make gate-hardening-check`; validates this checklist against the Schedule table. | Add scoring thresholds if the checklist starts driving merge policy. |
| Tracked text file hygiene | hardened | `make text-file-hygiene`; checker tests cover policy file selection, skipped binary extensions, valid UTF-8/LF inputs, UTF-8 BOM, invalid UTF-8, CRLF, bare CR, NUL, ESC, DEL, line/column reporting, and real temporary Git index execution. | Add a reviewed policy file only if a future generated text family needs a narrow exception. |
| Git tracked file mode policy | hardened | `make file-mode-check`; checker tests cover regular files, allowed script entrypoints, executable-bit drift, tracked symlinks, submodules, unsupported modes, malformed `git ls-files -s -z` records, and real temporary Git index execution. | Add explicit exceptions only through a reviewed policy file if a future tool requires a non-script executable. |
| Go toolchain version consistency | hardened | `make go-toolchain-check`; checker tests cover aligned declarations, submodule drift, patchless root versions, golangci-lint language drift, duplicate YAML keys, multi-document workflow YAML, YAML anchor/alias/merge-key rejection, tolerance for unrelated YAML fields, and hard-coded setup-go versions. | Add `go.work` coverage only if the repository adopts a workspace file. |
| Shell script syntax validation | hardened | `make shell-syntax-check`; fixture-backed Go harness discovers Git-tracked shell scripts with bounded first-line shebang detection, requires `.sh` entries to be direct regular files, rejects shell-script symlinks, and runs `bash -n` over every candidate. | Add ShellCheck only if a pinned local installation path is accepted for normal PR validation. |
| YAML configuration syntax validation | hardened | `make yaml-syntax-check`; fixture-backed Go harness parses tracked YAML files, rejects symlink/non-regular inputs, bounds reads, requires single-document scalar-key YAML, and rejects duplicate keys, anchors, aliases, and merge keys. | Add schema-specific validators only for config families that have stable schemas. |
| Source comment debt tracking | hardened | `make comment-debt-check` and `make comment-debt-check-test`; fixtures cover Go parser-backed comment scanning, string-literal avoidance, generated/testdata/ignored-path skipping, supported-path symlink rejection, non-Go comment prefixes, marker boundaries, accepted tracking refs, and violation output. | Add inline-comment parsing only if repeated Makefile or shell debt appears. |
| Deferred follow-up ledger validation | hardened | `make deferred-followups-check` and `make deferred-followups-check-test`; fixtures cover complete ledgers, symlink input rejection, missing fields, section/status mismatches, duplicate and missing IDs, weak values, unknown fields, invalid dates, future dates, and pre-decision updates. | Add automatic cross-checks from deferred `CURRENT_STATE.md` rows only if such rows appear. |
| Allowlist discipline | hardened | `make allowlist-discipline`; checker tests cover missing owner, missing expiry, expired entries, overlong review windows, same-day expiry, horizon-boundary expiry, detached metadata, missing removal triggers, and read errors. | Add new allowlist file specs only when a real gate introduces them. |
| Branch protection required-check policy | hardened | `make branch-protection-check`; strict JSON policy plus checker tests prove required-check contexts match PR workflow job names, remain sorted and duplicate-free, reject stale/missing contexts, reject duplicate workflow job names, and reject non-regular policy inputs. | Compare the policy against live GitHub branch-protection settings when an admin-scoped token is available. |
| Workflow change isolation | hardened | PR-only `make workflow-change-guard`; tests cover one-workflow acceptance, multi-workflow rejection, duplicate paths, path normalization, CI diff-range fetching, missing env pairs, and local HEAD fallback. | Broaden only if reviewer policy needs path-sensitive companion-file limits. |
| Linear history guard | hardened | PR-only `make linear-history-check` runs inside the existing DCO job; tests cover PR range selection, missing-base fetch, missing SHA failure, local upstream fallback, single-HEAD inspection, and merge-commit rejection. | Broaden to main-push or repository-settings enforcement only after merge strategy is managed declaratively. |
| PR file count cap | hardened | PR-only `make pr-file-count-check` runs inside the existing PR impact reference job; tests cover GitHub event `changed_files`, label overrides, env/local inputs, PR-range git fallback, invalid config, over-limit rejection, and duplicate path counting. | Revisit the 50-file default only after reviewer throughput data supports a different threshold. |
| PR wall-clock budget | hardened | `make pr` enforces the default 15-minute local PR budget through `scripts/pr_budget`; `make pr-budget-test` covers budget parsing, warn mode, timeout termination, exit-code preservation, and over-budget failure. | Revisit the budget after stable CI p95 data exists. |
| Repository size budget | hardened | `make repo-size-check`; scans Git-visible tracked and non-ignored untracked working-tree files, rejects symlink/non-regular paths, enforces default per-file and total byte budgets, and rejects common archive/executable/object/database-style artifact extensions. `make repo-size-check-test` covers deterministic sorting, unsafe paths, symlinks, blocked artifact extensions, per-file failures, total failures, and invalid budgets. | Raise limits or add extension exceptions only with a review note that names the retained artifact family or generated output that needs the space. |
| Temporal workflow tests (snapshot-bound start + replay + signal completion) | mature | `make temporal-workflow-tests`; covers start, replay, and signal completion behavior. | Add history fixtures when workflow versioning expands. |
| LLM golden prompt structural tests | mature | `go test ./internal/usecases/reportprompt`; covers alert scenarios and FinalReport request shape. | Refresh fixtures only with deliberate prompt contract changes. |
| LLM output acceptance tests | mature | `go test ./internal/usecases/llmoutput`; covers refusal, truncation, strict JSON duplicate-key/trailing-value rejection, schema, and fallback validation. | Add provider-specific malformed outputs when observed. |
| LLM validation retry tests | mature | `go test ./internal/usecases/llmretry`; covers retry feedback, exhaustion, refusal, provider errors, and cancellation. | Add jitter/backoff tests if retry timing becomes configurable. |
| LLM report draft schema tests | mature | `go test ./internal/usecases/reportdraft`; covers schema compatibility, semantic draft validation, and SubReport evidence reference integrity. | Add schema versioning tests before changing persisted report shape. |
| OpenAI-compatible LLM provider tests | mature | `go test ./internal/providers/llm/openai`; covers strict schema, fallback, refusal, API errors, response-envelope strictness, and probe behavior. | Add Responses API provider only behind the provider interface. |
| Report persistence repository tests | mature | `go test ./internal/domain ./internal/persistence/repository`; covers idempotency, fan-in links, delivery logs, and list ordering. | Add migration rollback smoke when release process exists. |
| Report Temporal workflow tests | mature | `go test ./internal/orchestrator/temporal`; covers fan-out, fan-in, idempotency, notification scheduling, and completed-history replay for ReportFanOutWorkflow, FinalReportWorkflow, and ReportBatchWorkflow. | Add retained JSON history fixtures when report workflow versioning expands. |
| Report trigger tests | mature | `go test ./internal/usecases/alertreplay ./internal/usecases/reporttrigger ./internal/orchestrator/temporal ./internal/transport/http ./cmd/openclarion`; covers replay-to-workflow start paths. | Add scheduled trigger coverage once scheduler surface exists. |
| IM provider tests | mature | `go test ./internal/providers/im/...`; covers fake and webhook provider contracts, including strict Webhook success-response parsing. | Add provider-specific auth variants only when configured. |
| Report HTTP API tests | mature | `go test ./internal/transport/http`; covers report list/detail mappings, replay trigger request decoding, strict duplicate-key rejection, and not-found behavior. | Add auth policy tests when report APIs become protected. |
| Runtime provider wiring tests | mature | `go test ./cmd/openclarion`; covers env-driven provider wiring and fail-fast cases. | Add config-file wiring only if config files are introduced. |
| Report local E2E | mature | `go test ./internal/e2e`; runs generated HTTP trigger through Postgres, Temporal, LLM, webhook, and persistence. | Keep live external smoke separate from local deterministic E2E. |
| Diagnosis auth live smoke | manual | `make diagnosis-auth-live-smoke`; requires a running OpenClarion API and live LDAP or bearer credentials. The harness calls only the sanitized auth status/check endpoints, can require an expected backend mode such as `ldap`, and writes a private proof without LDAP passwords, bearer tokens, endpoint URLs, or raw provider errors. | Use before diagnosis-room browser or convergence proof when validating LDAP-backed operator access. |
| Notification channel live smoke | manual | `make notification-channel-live-smoke`; requires a running OpenClarion API, a persisted notification channel profile, and server-side notification secret-ref resolver wiring. The harness calls only the sanitized channel-test endpoint, can require an expected profile kind such as `wecom`, and can require either one expected content kind or the `ai_diagnosis_sample,diagnosis_close_sample` suite for AI update plus close proof. It writes a private proof without endpoint URLs, secret refs, tokens, or raw provider errors. | Use as a focused delivery preflight; retain full report or diagnosis proof before claiming workflow-level delivery. |
| Alertmanager auto-diagnosis live smoke | manual | `make alertmanager-auto-diagnosis-live-smoke`; requires a running OpenClarion API, an enabled Alertmanager source profile with an enabled `auto_room` policy, worker LLM delivery, and notification channel secret-ref resolver wiring. The harness posts a synthetic Alertmanager webhook, requires an auto-started diagnosis room, defaults to requiring both assistant-message and final-conclusion AI notification content proofs, can require a specific notification channel profile, and writes a private proof without webhook bearer tokens, endpoints, or raw diagnosis transcripts. | Use after notification-channel smoke when proving the full alert-ingest to AI notification path. |
| Report live smoke harness | manual | `make report-live-smoke`; requires real database, Temporal, Prometheus, worker, and alert window. `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=report-live-smoke` now preflights URL shape, canonical UTC replay window, scenario, limit, wait timeout, and optional workflow/correlation IDs without printing configured values. | Store retained proof before claiming live acceptance. |
| Alert operations live input readiness | manual | `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=alert-operations-live-inputs`; preflights local alert, LLM, and notification endpoint environment shape without connecting to services, sending notifications, or printing configured values. | Keep this as a preflight-only gate; retained proof still comes from report live-smoke, policy replay, schedule proof, and diagnosis browser smoke artifacts. |
| Report policy live smoke harness | manual | `make report-policy-live-smoke`; requires real database, Temporal, an enabled report workflow policy, a configured alert source profile, worker, and notification delivery configuration. `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=report-policy-live-smoke` preflights policy ID, canonical UTC replay window, limit, wait timeout, and optional workflow/correlation IDs without printing configured values. | Store retained proof before claiming profile-driven live acceptance. |
| Report schedule live proof harness | manual | `make report-schedule-live-smoke`; requires real database, Temporal, an enabled schedule/policy binding, worker, and notification delivery configuration. It preflights schedule ID, policy ID, optional Temporal Schedule ID shape, secret-ref JSON, worker-notification alternatives, observation timeout, and absent retained output path without printing configured values, then waits for a real Temporal Schedule action and validates retained report-delivery JSON. | Store retained proof from a real enabled Temporal Schedule firing before claiming scheduled-trigger live acceptance. |
| Report live proof validator | hardened | `make report-live-smoke-output-test`; rejects symlink/non-regular proof files and weak retained replay or scheduled-trigger proof JSON, including duplicate keys, unknown fields, missing, non-canonical, or future `checked_at`, invalid replay or schedule request metadata, workflow ID mismatches, non-canonical workflow/run IDs, unbounded or multiline provider message IDs, inconsistent replay stats, duplicate snapshot/SubReport IDs, disabled schedules, missing Temporal Schedule actions, launcher/report-batch workflow mismatches, missing FinalReport IDs, and unsuccessful notification statuses. | Add stronger provider observations only when the live harness emits them. |
| Prometheus MetricsProvider tests | mature | `go test ./internal/providers/metrics/prometheus`; covers strict parsed alert/API-error response validation, ambiguous envelope rejection, body-size caps, auth injection, and upstream error wrapping. | Add additional Prometheus API surfaces only when the provider contract grows. |
| Prometheus metrics endpoint tests | mature | `go test ./internal/observability/metrics`; covers isolated registry and low-cardinality metrics. | Add SLO dashboards outside PR-blocking CI. |
| OpenTelemetry HTTP + Temporal tracing tests | mature | `go test ./internal/observability/tracing`; covers propagators, outbound headers, OTLP smoke, and Temporal interceptors. | Add collector compatibility matrix only when deployment target is known. |
| HTTP correlation / access logging tests | mature | `go test ./internal/observability/correlation ./internal/observability/accesslog ./internal/transport/http ./internal/providers/metrics/prometheus ./internal/providers/llm/openai ./internal/providers/im/webhook`; covers request IDs and structured access logs. | Add redaction fixtures when new sensitive fields enter handlers. |
| Frontend typecheck / lint / unit / build / deadcode / audit | mature | `make frontend-checks`; runs npm install, typecheck, lint, unit, build, deadcode, and audit gates. | Split jobs only if local wall-clock requires it. |
| Frontend Playwright route smoke | mature | `make ci-frontend-smoke`; mocks OpenClarion API and WebSocket for production-route smoke. | Add accessibility checks once routes stabilize. |
| Diagnosis room live browser smoke harness | manual | `make diagnosis-live-browser-smoke`; requires real backend, token, and sandbox-capable worker. | Retain fresh proof JSON when backend, worker image, sandbox runtime, or routing changes. |
| Diagnosis room backend convergence smoke | manual | `make diagnosis-live-convergence-smoke`; requires a real backend, worker, bearer token, and existing or creatable diagnosis room. It drives the HTTP/WebSocket API directly without Playwright and retains backend-only convergence observations for multi-turn, evidence collection, supplemental evidence, and optional conclusion confirmation. | Keep this complementary to the browser smoke; do not use it alone to claim frontend readiness. |
| Alert consultation live setup | manual | `make alert-consultation-setup`; requires a running OpenClarion API, live Alertmanager endpoint metadata, and server-side Enterprise WeChat secret-ref resolver wiring. The helper creates or replaces source, notification-channel, grouping-policy, and auto-room workflow metadata, and writes only generated IDs plus sanitized setup proof under the operator-selected private output path. | Use before alertmanager auto-diagnosis smoke; never treat setup-only proof as delivery acceptance. |
| Diagnosis room live proof validator | hardened | `make diagnosis-live-smoke-output-test`; rejects symlink/non-regular proof files plus weak, duplicate-key, unknown-field, polluted, non-canonical scalar, non-UTC `checked_at`, non-canonical session/workflow/run IDs, unbounded or multiline proof summaries, or request/session/message/evidence-mismatched browser proof artifacts, including browser-submitted message length and SHA-256 digest mismatches. | Add new observations only when UI behavior changes. |
| Diagnosis dev OIDC issuer | manual | `make diagnosis-dev-oidc-issuer`; focused tests prove the issued token authenticates through the runtime OIDC provider, public discovery/JWKS shape, input rejection, and loopback-by-default binding. `make manual-target-isolation` keeps the helper out of CI. | Use only for local live-smoke setup; retain browser proof before claiming M5 acceptance. |
| OpenAPI -> TS type freshness | hardened | `make openapi-ts-fresh`; regenerates generated frontend API types from OpenAPI. | Keep generated client path locked if frontend modules move. |
| Temporal trace propagation tests | mature | Covered by tracing and runtime wiring tests. | Add replay fixture only if trace propagation enters workflow history assumptions. |
| Agent runtime dependency and hardcoding boundary | hardened | `make forbidden-agent-runtime`; structurally blocks configured Agent frameworks from control-plane manifests and non-test source, permits only Eino under the accepted diagnosis-runner module, and still rejects every other configured runtime family inside that module. | Revisit if a new runtime family replaces or supplements Eino. |
| ContainerProvider contract tests | mature | `go test ./internal/usecases/ports ./internal/providers/container/fake`; covers file contract, duplicate-key-free request/raw-result JSON validation, caps, network policy, and fake behavior. | Add new methods only with provider-neutral tests first. |
| Container sandbox security spec gate | mature | `make sandbox-security`; validates provider-neutral and Docker runtime security posture, including strict Docker output archive member handling. | Keep live Docker smoke as evidence for daemon behavior. |
| Agent tool helper contract tests | hardened | `make agent-tool-scripts-test`; covers metric query and topology lookup helpers, including strict Prometheus response JSON plus topology regular-file/no-symlink, duplicate-key, and trailing-value rejection. | Package helper smoke with runtime candidates. |
| Sandbox baseline audit | mature | `make sandbox-baseline-audit`; emits JSON proof for baseline sandbox checks. Manual `make sandbox-m4-baseline-audit OUT=...` retains the same proof without overwriting existing evidence. | Treat as code-level evidence, not report-quality acceptance. |
| Sandbox image audit | manual | `make sandbox-image-audit`; verifies an operator-selected digest-pinned sandbox image against the diagnosis assistant runtime audit contract. `make manual-target-isolation` keeps the Docker-backed image audit out of normal CI. | Retain audit proof only for real candidate images; do not treat it as report-quality acceptance. |
| Sandbox M4 SubReport generation | manual | `make sandbox-m4-subreport-generate`; focused tests cover sandbox evidence-envelope construction from a persisted EvidenceSnapshot, idempotent sandbox SubReport persistence, existing-row short-circuit behavior, strict candidate ID parsing, environment parsing, and rejection of candidate SubReport output that omits the canonical `snapshot:<id>` ref. `make manual-target-isolation` keeps the helper registered as manual evidence preparation. | Use real representative persisted snapshots and candidate images before exporting samples, comparing quality, or recording a runtime decision. |
| Sandbox M4 quality sample export | manual | `make sandbox-m4-quality-sample-export`; focused tests cover strict operator selection JSON, duplicate case/report ID rejection, persisted SubReport production-schema validation, scenario matching, shared EvidenceSnapshot ID checks, non-empty output-root rejection, and retained direct/sandbox sample layout output. `make manual-target-isolation` keeps the helper registered as manual evidence preparation. | Use real representative operator-selected rows before comparing quality or recording a runtime decision. |
| Sandbox quality comparison helper tests | hardened | `make sandbox-quality-compare-test`; validates offline direct-vs-sandbox comparison harness plus regular-file/no-symlink checks before reading manifests and report inputs, duplicate-key, unknown-field, single-line bounded manifest sample basis, case IDs, evidence refs, and report paths, duplicate manifest report-path rejection, per-case required evidence-ref binding across both reports, and canonical `snapshot:<positive-id>` EvidenceSnapshot refs. | Feed real representative outputs before accepting a runtime. |
| Sandbox M4 decision gate tests | hardened | `make sandbox-m4-decision-test`; validates proceed/iterate/defer evidence requirements, regular-file/no-symlink checks before parsing evidence files, generic unpadded selected-candidate evaluation evidence with selected runtime-ref and required runtime-smoke-ref binding, digest-pinned runtime candidates, exact required runtime-smoke-name set, distinct bounded normalized relative runtime-smoke `evidence_ref` values plus lowercase `evidence_sha256` digests, required single-line bounded quality `sample_basis`, required single-line bounded human review text, single-line bounded case-level `reviewed_cases` coverage matching quality case IDs, duplicate-key and unknown-field rejection, duplicate baseline check rejection, non-empty single-line bounded per-case required evidence refs with canonical `snapshot:<positive-id>` binding, quality summary counts re-derived from case recommendations, and retained iterate-class reasons when incomplete evidence forces a deferred decision. | Keep manual decision gated on retained real evidence files. |
| Sandbox M4 runtime-smoke artifact bundle validator | hardened | `make sandbox-m4-evidence-packet-test` includes `go test ./scripts/sandbox_m4_runtime_smoke_artifacts`, covering accepted bundles plus missing, unexpected, symlinked, duplicate-key, mutable-image, and wrong-mode artifacts; `make sandbox-m4-runtime-smoke-artifacts` runs the same checker after manual Docker-backed artifact collection. | Keep it as an early bundle guard; packet assembly and verification remain the cross-artifact evidence authority. |
| Sandbox M4 review-evidence template | manual | `make sandbox-m4-review-evidence-template`; focused tests cover fail-closed draft output, explicit representative-sample opt-in, output-file writing, invalid candidate IDs, unsupported runtime-smoke statuses, and symlink runtime-smoke artifact rejection. `make manual-target-isolation` keeps the helper out of normal CI. | Use real representative quality and runtime-smoke artifacts before any M4 runtime baseline decision, and keep generated review evidence fail-closed until an operator completes the review. |
| Sandbox M4 evidence packet tests | hardened | `make sandbox-m4-evidence-packet-test`; validates packet artifact assembly and offline retained-packet verification, packet-local generated artifact and helper output paths, copied quality manifest/direct/sandbox SubReport input freezing under packet-local `quality-inputs/` paths, quality manifest/output identity matching, production SubReport parser validation and required-ref binding for copied reports, generic unpadded selected-candidate evidence with selected runtime-ref and required runtime-smoke-ref binding, digest-pinned runtime candidates, exact required runtime-smoke-name set, distinct bounded normalized relative runtime-smoke `evidence_ref` values plus lowercase `evidence_sha256` digests, regular-file/no-symlink checks before source artifact hashing or copying, copied runtime-smoke artifact existence and digest matching under packet-local `runtime-smoke-artifacts/` paths, required single-line bounded quality output `sample_basis`, required single-line bounded human review text, single-line bounded case-level reviewed-case evidence, duplicate-key and unknown-field rejection, duplicate baseline check rejection, unique single-line bounded evidence-bound quality case IDs and refs, quality summary/case consistency before writing, retained command metadata verification, closed packet inventory verification, decision selected-candidate / `reviewed_case_count` cross-checks against review/quality inputs, decision reason consistency, decision evidence cross-checks against frozen inputs, and SHA-256 digest binding for frozen artifacts. | Add artifact retention policy when release storage exists. |
| Sandbox M4 evidence-chain readiness | manual | `make manual-evidence-readiness MANUAL_EVIDENCE_TARGET=sandbox-m4-evidence-chain`; focused tests cover canonical retained artifact presence, duplicate-key/trailing-value JSON rejection, digest-ref validation, SHA-256 digest reporting, missing artifact reporting, and no local-path leakage. | Keep as operator gap audit only; semantic validation remains in the quality, review, decision, and packet helpers. |
| Diagnosis room policy tests | mature | `make diagnosis-room-policy-test`; covers short-conversation policy and strict output schema boundary, including duplicate-key and trailing-value rejection before JSON Schema validation. | Add compression policy only after V1 scope changes. |
| Diagnosis room workflow tests | mature | `make diagnosis-room-workflow-test`; covers Temporal room workflow, sandbox, strict turn-output acceptance, persistence, lifecycle audit, and close notification. | Add replay histories before changing workflow versioning. |
| Diagnosis auth boundary tests | mature | `make diagnosis-auth-test`; covers AuthProvider, OIDC, RBAC, strict diagnosis-room request decoding, tenant-bound sessions and WS tickets, persistence, and relay behavior. | Add browser-level tenant-selection regression coverage when the workspace UI lands. |
| Diagnosis chat persistence tests | mature | `make diagnosis-chat-persistence-test`; covers ChatSession and ChatTurn invariants plus PostgreSQL repository behavior. | Add retention policy tests when deletion/archival exists. |
| Docker Provider live smoke harness | manual | `make container-provider-smoke`; proves real Docker lifecycle, duplicate-key-free JSON-object output, optional retained proof JSON, and invocation-label cleanup. | Keep outside CI unless runner isolation is dedicated. |
| Docker Provider timeout cleanup smoke | manual | `make container-provider-timeout-smoke`; proves timeout cleanup against a real Docker daemon and can retain expected-error proof JSON. | Run before accepting runtime image changes. |
| Docker Provider output cap smoke | manual | `make container-provider-output-cap-smoke`; proves output-cap behavior against a real Docker daemon and can retain expected-error proof JSON. | Pair with retained proof when cap policy changes. |
| Egress allow-deny smoke harness | manual | `make egress-allowdeny-smoke`; proves proxy allow, proxy deny, direct bypass failure, digest-pinned helper image use, and optional retained proof JSON. | Re-run for any production proxy topology change. |
| Manual agent runtime adapter smoke harness | manual | `make agent-runtime-smoke`; validates digest-pinned candidate image security, file contract, regular-file/no-symlink output proof input, bounded reads, duplicate-key-free non-empty JSON-object output, and retained proof JSON with output SHA-256 plus no host-local paths. | Require real candidate image evidence before baseline acceptance. |
| Custom thin runner candidate smoke | manual | `make custom-thin-runner-smoke`; builds and smokes the local scratch runner candidate, including regular-file/no-symlink checks plus strict duplicate-key/trailing-value rejection for mounted ADR-0013 input JSON. It can also retain the canonical runtime-smoke artifact bundle during the same ephemeral registry run. | Compare quality against representative M2 outputs before proceeding. |
| Agent runtime adapter candidate result | partial | The custom runner contract proof and isolated Eino M5 runner are landed; production ContainerProvider wiring and real M4 quality comparison remain pending. | Accept an M4 quality baseline only after the real evidence packet passes its decision gate. |
| Container sandbox runtime execution gate | manual | Docker live smoke targets prove lifecycle, timeout cleanup, and output cap behavior. | Keep manual until CI has a trusted Docker runtime. |
| WebSocket auth handshake and relay test | mature | `make diagnosis-auth-test`; covers ticket issuance, upgrade, origin checks, strict client-frame JSON decoding, submit, query, timeout, and disconnect behavior. | Add browser-level auth regression only when auth UI lands. |
| Bounded-turn enforcement test | mature | `make diagnosis-room-workflow-test`; workflow Update Validator guards server-side limits. | Add product-config test when tenant-specific limits exist. |
| Audit completeness test | mature | `make diagnosis-room-workflow-test`; verifies idempotent opened, turn-persisted, closed, and notification audit events. | Add audit export checks when export surface lands. |
