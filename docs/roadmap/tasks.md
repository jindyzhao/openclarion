# Roadmap

> Last updated: 2026-06-01
> Author: jindyzhao
> Status: private incubation

## Milestones

```text
M0 Bootstrap  ->  M1 Control Plane  ->  M2 Report Loop  ->  M3 Frontend+Ops  ->  M4 Agent Sandbox  ->  M5 Short-Conversation Diagnosis
```

## M0: Bootstrap

- [x] governance files
- [x] GitHub issue and PR templates
- [x] CI documentation hygiene check
- [x] Go module skeleton
- [x] Docker Compose for PostgreSQL and Temporal
- [x] OpenAPI 3.1 skeleton (`api/openapi.yaml` with healthz)
- [x] oapi-codegen-exp generation chain verified
- [ ] Ent and Atlas toolchain (deferred to M1: pinned at first import per
      DEPENDENCIES.md "first-import pin" rule)
- [x] `make generate`, `make test`, `make lint`, `make pr`
- [x] health endpoint compiles and returns 200
- [x] `vacuum` OpenAPI lint as a real blocking gate

**Acceptance**: `make pr` passes; `docker compose up -d --wait` starts
PostgreSQL+Temporal+Temporal UI; `curl http://localhost:8080/healthz` returns
`{"status":"ok"}`.

> **Scope note**: Ent/Atlas/Temporal SDK pins are intentionally deferred to M1.
> Per the "first-import pin" rule (DEPENDENCIES.md), Go modules enter `go.mod`
> only when production code first imports them. ADR-0012 was amended on
> 2026-05-22 to move the Temporal Workflow Update round-trip validation to M1
> (the first milestone with a real Temporal workflow).

## M1: Go Control Plane

- [x] MetricsProvider interface and Prometheus implementation
- [x] AlertEvent Ent schema and migrations
- [x] alert window replay harness
- [x] deterministic grouping algorithm
- [x] EvidenceSnapshot schema and builder
- [x] Temporal workflow starts from an EvidenceSnapshot-backed DiagnosisTask
- [x] API endpoints: list alerts, list snapshots

**Acceptance**: import 20 historical alerts -> auto-group -> persist
EvidenceSnapshot -> queryable via API.

> **Scope note**: Provider fake policy -- each provider port ships with its
> own fake in the same first-import PR (M1-PR4 already shipped fake
> `MetricsProvider`). The previously listed standalone "fake providers for
> all other interfaces" checklist item has been removed because there is no
> standalone milestone task for it; subsequent provider ports (LLM in M2,
> IM in M2, Container in M4, Auth in M5) bring their own fakes alongside
> the port definition.

## M2: Headless Report Loop

- [x] LLMProvider interface
- [x] OpenAI-compatible provider implementation
- [x] fake LLM provider (deterministic, for tests)
- [x] prompt templates (single alert, cascade, alert storm)
- [x] structured JSON output parser with schema validation
- [x] retry mechanism (3 attempts with error feedback)
- [x] SubReport and FinalReport Ent schemas
- [x] ReportFanOutWorkflow, ReportBatchWorkflow, and FinalReportWorkflow
- [x] alert replay snapshot refs map to an idempotent report workflow start request
- [x] golden prompt tests (structure validation, not content)
- [x] IMProvider interface and Webhook implementation
- [x] report notification flow
- [x] notification delivery log persistence
- [x] API endpoints: list reports, get report detail
- [x] HTTP endpoint triggers alert-window replay and report workflow start
- [x] CLI one-shot trigger replays an alert window and starts the report workflow
- [x] local E2E: HTTP replay trigger -> report workflow -> webhook notification
- [x] manual live smoke gate for real Prometheus -> Temporal -> Webhook verification
- [ ] live external E2E: real Prometheus -> Temporal -> Webhook notification

**Acceptance**: trigger alert batch -> auto-generate SubReport+FinalReport ->
Webhook receives notification.

## M3: Frontend and Observability

- [x] Next.js project initialization (`web/`)
- [x] generated TypeScript API types from OpenAPI
- [x] report list page
- [x] report detail page with evidence traceability
- [x] basic dashboard: alert counts, report success rate
- [x] Playwright route smoke for dashboard, report list, and report detail
- [x] OpenTelemetry HTTP server instrumentation foundation
- [x] OpenTelemetry collector smoke and outbound HTTP trace propagation
- [x] Temporal workflow trace propagation
- [x] Prometheus metrics endpoint
- [x] HTTP structured error logging with request/trace correlation IDs
- [x] access logs and broader cross-boundary correlation IDs

**Acceptance**: open browser -> view report list -> click into report detail
with evidence traceability links.

## M4: Agent Sandbox Baseline and Exploration

- [x] ContainerProvider interface
- [x] Docker sandbox security spec validation gate
- [x] agent runtime selection gate documented (adapter-first; OpenClaw /
  Hermes Agent / custom thin runner candidates)
- [x] manual runtime adapter smoke harness (`make agent-runtime-smoke`)
- [x] alert-first signal extension boundary documented
- [x] candidate runtime adapter smoke result: custom thin runner contract proof
  against ADR-0013 file contract (`make custom-thin-runner-smoke`)
- [x] runtime-agnostic Docker sandbox (non-root, readonly fs, network allowlist)
- [x] evidence injection via mounted files
- [x] Docker Provider live execution smoke (`make container-provider-smoke`)
- [x] Docker Provider timeout cleanup smoke (`make container-provider-timeout-smoke`)
- [x] Docker Provider output cap smoke (`make container-provider-output-cap-smoke`)
- [x] tool scripts (metric query, topology lookup)
- [x] timeout and cleanup logic
- [x] output extraction via /workspace/out/output.json (file-based contract)
- [x] fail-closed guard for allowlist network mode until egress enforcer is configured
- [x] allowlist target normalization and static subset enforcer
- [x] short-lived credential validation and Docker env injection boundary
- [x] Docker daemon access boundary documented
- [x] concrete egress proxy/firewall allow-deny smoke
- [x] offline quality comparison harness (`make sandbox-quality-compare-test`)
- [x] batch quality evidence manifest mode for representative sample runs
- [x] quality manifest scenario coverage gate for `single_alert`, `cascade`, and `alert_storm`
- [x] code-level minimum sandbox baseline audit (`make sandbox-baseline-audit`)
- [x] minimum sandbox baseline for M5 (auditable file-based I/O, cleanup, limits)
- [ ] quality comparison: sandbox report vs direct LLM report
- [ ] decision gate: report-enhancement track proceeds, iterates, or stays deferred

**Acceptance**: given evidence -> agent in sandbox calls tools -> return
enhanced report with measurable quality delta vs M2 output; minimum sandbox
baseline is ready for M5 short-conversation diagnosis.

## M5: Short-Conversation Interactive Diagnosis (V1 Required)

> Implements the per-turn container invocation contract from
> [ADR-0013](../adr/ADR-0013-per-turn-container-invocation.md).

Delivered as part of V1 with intentionally minimal scope. See
[phases/05-interactive-diagnosis.md](../design/phases/05-interactive-diagnosis.md)
for the V1 scope boundary.

- [x] AuthProvider interface and OIDC implementation
- [x] RBAC checks (owner and admin; leader is deferred)
- [x] DiagnosisRoomWorkflow control-plane skeleton (Temporal Update, signals, queries, durable timers)
- [x] per-turn ContainerProvider Activity boundary with strict diagnosis-turn
      output schema validation
- [x] ChatSession and ChatTurn Ent schemas + repository boundary
- [x] DiagnosisRoomWorkflow persists accepted user+assistant ChatTurn pairs
- [x] bounded-turn enforcement at workflow Update boundary
- [x] fixed session lifetime + idle timeout
- [x] diagnosis room route (Next.js)
- [x] WebSocket proxy with authenticated handshake boundary
- [x] unsafe-instruction filter (deny-list) at workflow/sandbox boundary
- [x] audit logging for session lifecycle
- [x] final group notification on session close
- [ ] live browser acceptance against a real backend/worker stack
      (`make diagnosis-live-browser-smoke` harness landed; real-stack proof
      still pending)

**Acceptance**: authorized user opens diagnosis room -> short-conversation
with sandboxed agent within turn and time limits -> chat persisted, audit
logged, final notification sent.

**Explicitly Out-of-Scope (V1)**: lifecycle-end compression, multi-day
sessions, leader-tier approval, streaming partial responses.

## Future

- [ ] pgvector retrieval (RAG)
- [ ] Kubernetes Job ContainerProvider
- [ ] DingTalk and Feishu IM providers
- [ ] NetBox CMDB provider
- [ ] signal-capable provider extension track after alert-analysis MVP proves
      stable
- [ ] scheduled weekly and monthly reports
- [ ] multi-tenant operations
- [ ] Email and Slack IM providers (upgrade from Webhook)

## Changelog

| Date | Author | Change |
|------|--------|--------|
| 2026-05-18 | jindyzhao | English roadmap reset and MVP cutline |
| 2026-05-19 | jindyzhao | Restructure to M0-M5; remove OpenClaw binding; add acceptance criteria |
| 2026-05-19 | jindyzhao | M5 reclassified as V1 required (short-conversation scope); defer compression and long sessions |
| 2026-05-26 | jindyzhao | M1-PR4 lands `MetricsProvider` interface + Prometheus implementation + in-memory fake provider + `alertingest.IngestOnce` library. Item ticked: `MetricsProvider interface and Prometheus implementation`. `AlertEvent Ent schema and migrations` and `Temporal workflow bootstrap (DiagnosisWorkflow shell)` were already shipped in M1-PR1 and M1-PR3 respectively and are now ticked retroactively to keep this checklist in sync with `CURRENT_STATE.md`. Remaining M1 items (`alert window replay harness`, `deterministic grouping algorithm`, `EvidenceSnapshot schema and builder`, `fake providers for all other interfaces`, `API endpoints`) are explicitly out of scope for M1-PR4. |
| 2026-05-26 | jindyzhao | M1-PR5 lands deterministic grouping algorithm (`GroupEvents` pure function in `internal/usecases/alertgrouping/`). Item ticked: `deterministic grouping algorithm`. |
| 2026-05-26 | jindyzhao | M1-PR6 lands EvidenceSnapshot deterministic builder (`BuildSnapshot` pure function in `internal/usecases/evidencebuild/`). Item ticked: `EvidenceSnapshot schema and builder` (schema shipped in M1-PR1, builder in M1-PR6). |
| 2026-05-26 | jindyzhao | M1-PR6 shipped post-merge as `22428fb`. M1-PR7 in progress: alert window replay harness in `internal/usecases/alertreplay/` (`ReplayWindow(ctx, provider, factory, Request) (Stats, error)` orchestrator that ingests via `alertingest.IngestOnce`, re-reads the half-open `[WindowStart, WindowEnd)` window with `Limit+1` as a safety valve, runs deterministic grouping, then a per-group pipeline transaction covering save / refresh / existing diff -> link events -> `BuildSnapshot` -> save snapshot when not duplicate -> close group when previously active). `AlertRepository` ports gained `ListEventsByStartsAtRange(start, end, limit)` and `FindGroupByNaturalKey(groupKey, firstSeenAt)`; both reject zero/blank inputs and the range method compares `end > start` after `NormalizeUTCMicro` so sub-microsecond windows the normaliser collapses are rejected. D7 closed->refresh tension: refresh updates mutable fields of an already-closed group when the same `(group_key, first_seen_at)` natural key recurs, but status is preserved (no reopen); a window whose `first_seen_at` differs creates a NEW row by design. M1 checklist `alert window replay harness` will be ticked when the PR merges; the previously listed standalone `fake providers for all other interfaces` checklist item is removed and replaced by a scope note above. |
| 2026-05-27 | jindyzhao | M1 control-plane read API and W4 OpenAPI gates landed locally. `GET /api/v1/alerts` and `GET /api/v1/evidence-snapshots` return newest-first persisted data via generated `oapi-codegen-exp` handlers; `AlertRepository.ListEvents` and `EvidenceRepository.List` provide bounded deterministic queries. W4 added `make openapi-breaking`, `make openapi-fingerprint`, and `make doc-claims-check` with CI jobs and Schedule rows. |
| 2026-05-28 | jindyzhao | M1 Temporal workflow start is now snapshot-bound: `DiagnosisWorkflowInput` carries `TaskID` + `EvidenceSnapshotID`; `StartDiagnosisTask` validates the task-snapshot binding and idempotently transitions the task to `running`; `workflow.GetVersion` preserves replay compatibility for older histories. The remaining M1 checklist item `Temporal workflow starts from a snapshot` is ticked. |
| 2026-05-28 | jindyzhao | CI control-plane hardening continued after M1 checklist closure: `workflow-parity` now enforces PR concurrency cancellation, default bash shell, fixed Ubuntu runner labels, action SHA version comments, and a 15-minute M0-M2 job timeout cap. |
| 2026-05-29 | jindyzhao | CI workflow registry hardening landed locally: `workflow-parity` now requires each committed workflow file to use `ci.yml` or `<gate>.yml`, be listed in `docs/design/ci/README.md`'s Workflow File Registry, and declare a unique top-level workflow `name:`. `go test ./scripts` now carries temporary-repository negative fixtures for duplicate/unregistered workflows, invalid workflow filenames, and missing workflow names. |
| 2026-05-29 | jindyzhao | CI workflow permission-scope hardening landed locally: `workflow-parity` now rejects workflow/job `permissions:` entries broader than `contents: read` unless the entry carries an inline `# parity-allow: <reason>` justification, with root `scripts` Go fixtures covering both rejection and justified expansion. |
| 2026-05-29 | jindyzhao | CI PR-secrets hardening landed locally: `workflow-parity` now rejects `${{ secrets.* }}` references in `pull_request` workflows and requires any `pull_request_target` workflow to carry an explicit `# pull-request-target-review-policy: <review process>` marker, with root `scripts` fixtures covering both failure modes and the allowed opt-in path. |
| 2026-05-29 | jindyzhao | Project terminology gate landed locally: `make docs-hygiene` now runs `scripts/check_project_terminology.sh` with `docs/design/ci/terminology.tsv`, rejects the hyphenated canonical-product variant, generic platform positioning, and `SignalEvent` / `SignalGroup` / `CaseGroup` aliases outside architecture boundary docs. |
| 2026-05-29 | jindyzhao | Structured logging analyzer landed locally: `openclarion-arch` now rejects direct `fmt.Print*` and standard-library `log.Print*` / `log.Fatal*` / `log.Panic*` calls in non-test production packages, while allowing CLI packages under `cmd/`, repository scripts under `scripts/`, and generated Ent code. |
| 2026-05-29 | jindyzhao | HTTP client boundary analyzer landed locally: `openclarion-arch` now rejects `http.DefaultClient` and bare `http.Get` / `http.Head` / `http.Post` / `http.PostForm` in non-test production packages, while allowing CLI packages under `cmd/` and repository scripts under `scripts/`. |
| 2026-05-29 | jindyzhao | SQL open boundary analyzer landed locally: `openclarion-arch` now rejects direct `database/sql.Open` calls outside the persistence repository / generated Ent boundary, so usecase, provider, transport, and runtime wiring code receive repositories/factories instead of opening database connections directly. |
| 2026-05-29 | jindyzhao | Process execution boundary analyzer landed locally: `openclarion-arch` now rejects direct `os/exec.Command` / `os/exec.CommandContext` calls outside CLI packages, repository scripts, and the sandbox boundary, keeping production alert-analysis code behind Provider or audited sandbox-runtime execution paths. |
| 2026-06-01 | jindyzhao | Core filesystem boundary analyzer landed locally: `openclarion-arch` now rejects direct local file reads, metadata/directory probes, writes, creates, removes, renames, links, temp paths, and local filesystem adapters in non-test `internal/domain` and `internal/usecases` packages, keeping file-backed evidence/configuration at provider, repository, or boundary DTOs. |
| 2026-05-29 | jindyzhao | Core clock boundary analyzer landed locally: `openclarion-arch` now rejects direct `time.Now()` calls in non-test `internal/domain` and `internal/usecases` packages, keeping core alert-analysis logic deterministic by requiring timestamps to arrive through input DTOs, boundary orchestration, or injected clocks. |
| 2026-06-01 | jindyzhao | Core context boundary analyzer landed locally: `openclarion-arch` now rejects `context.Background()`, `context.TODO()`, and `context.WithoutCancel()` calls in non-test `internal/domain` and `internal/usecases` packages, keeping core alert-analysis logic tied to caller cancellation, deadlines, and boundary orchestration rather than internally created root or detached contexts. |
| 2026-05-30 | jindyzhao | Core environment/config boundary analyzer landed locally: `openclarion-arch` now rejects direct process-environment reads/mutations and process flag parsing in non-test `internal/domain` and `internal/usecases` packages, keeping configuration in boundary wiring and injected DTOs rather than hidden global process state. |
| 2026-05-29 | jindyzhao | Reflection / unsafe import boundary analyzer landed locally: `openclarion-arch` now rejects `reflect` and `unsafe` imports in handwritten production files while allowing tests and generated code, keeping escape hatches out of the alert-analysis control plane unless a reviewed infra boundary is introduced. |
| 2026-05-29 | jindyzhao | Panic boundary analyzer landed locally: `openclarion-arch` now rejects `panic()` in handwritten production files outside `main`, `init`, or an explicit deferred recover boundary. Alert ingestion and grouping canonical JSON helpers now return errors instead of defensively panicking. |
| 2026-05-29 | jindyzhao | Goroutine supervision boundary analyzer landed locally: `cmd/openclarion` now manages the HTTP server lifecycle with `errgroup.WithContext`, and `openclarion-arch` rejects future raw `go` statements in handwritten production files outside `internal/supervision`. |
| 2026-06-01 | jindyzhao | Core mutable global state analyzer landed locally: `openclarion-arch` now rejects package-level mutable collection state in non-test handwritten `internal/domain` and `internal/usecases` packages, including pointer/interface-backed collection initializers. Existing schema bytes and severity ranking state were converted to constants or pure functions so the core alert-analysis path cannot be polluted through process-global map/slice state. |
| 2026-05-29 | jindyzhao | ADR supersession closure hardening landed locally: `make adr-check` now validates `supersedes` / `superseded_by` front matter as a closed cross-reference, requiring referenced ADRs to exist, be marked `superseded`, and carry a matching back-pointer. |
| 2026-05-29 | jindyzhao | ADR accepted-body immutability hardening landed locally: `make adr-check` now uses `ADR_BASE_REF` in PR/base-aware runs to reject body edits to ADRs that were already `accepted` at the base ref, forcing changes through new superseding ADRs. |
| 2026-05-29 | jindyzhao | DCO author-email hardening landed locally: `make dco-check` now requires a `Signed-off-by:` email matching the commit author email, reports mismatched sign-offs separately from missing trailers, and fixes the no-upstream local fallback to validate `HEAD`; `go test ./scripts` carries temporary git-repository fixtures. |
| 2026-05-29 | jindyzhao | Markdown link gate hardening landed locally: `make links-check` now validates same-file and cross-file Markdown `#anchor` fragments against generated heading slugs or explicit HTML anchors, and `go test ./scripts` carries positive/missing-anchor fixtures. |
| 2026-05-29 | jindyzhao | Docker base-image pinning hardening landed locally: `make forbidden-latest` now scans committed Dockerfiles and rejects external `FROM` images without `@sha256:<digest>`, while allowing `scratch` and previous build stages. `go test ./scripts` carries positive and negative Dockerfile fixtures. |
| 2026-05-29 | jindyzhao | ADR Consequences governance hardening landed locally: all existing ADRs now include a non-empty `Consequences` section, and `make adr-check` rejects future ADRs with missing or empty Consequences sections through fixture-backed root `scripts` tests. |
| 2026-05-29 | jindyzhao | DCO AI-branding hardening landed locally: `make dco-check` now rejects commit messages or trailers that contain `Generated-by:`, AI co-author trailers, or common model/tool names, with temporary git-repository fixtures proving the failure modes. |
| 2026-05-29 | jindyzhao | Go coverage floor landed locally: `make go-coverage` runs `scripts/check_go_coverage.sh`, filters generated API/Ent packages and root script-test aggregators, and requires selected handwritten Go packages with statements to stay at or above the default 40.0% package coverage floor. Root `scripts` tests cover package filtering, below-threshold failures, and invalid threshold handling. |
| 2026-05-29 | jindyzhao | Generated file header gate landed locally: `make generated-headers` now rejects OpenAPI Go, Ent, or frontend OpenAPI TypeScript generated outputs that lose their generator-owned header/direct-edit warning, with fixture-backed root `scripts` tests. |
| 2026-05-29 | jindyzhao | ADR front-matter schema hardening landed locally: every ADR now declares machine-readable `id` and `title`, and `make adr-check` rejects missing required front-matter keys, unknown keys, invalid dates/statuses, and file/H1/front-matter identity drift. |
| 2026-05-29 | jindyzhao | Critical Go module pinning hardening landed locally: `make forbidden-latest` now rejects missing, indirect, non-concrete, or replaced root-module pins for critical first-import modules covering Ent, Temporal SDK, and OpenTelemetry. |
| 2026-05-29 | jindyzhao | Go `replace` allowlist hardening landed locally: `make forbidden-latest` now rejects any committed `replace` directive in first-party Go modules unless `docs/design/DEPENDENCIES.md` carries the matching `replace-allow: <module> => <target>` marker with owner and non-expired `expires: YYYY-MM-DD`, with root `scripts` tests covering root-module, nested-module, under-specified, expired, and allowlisted block replacements. |
| 2026-05-29 | jindyzhao | Go tool directive pinning hardened locally: `make forbidden-latest` now rejects external Go 1.24+ `tool` directive package paths unless the provider module has a concrete `require` version pin, with root `scripts` tests covering valid backing pins, missing requires, non-concrete versions, and longest-prefix module matching. |
| 2026-05-29 | jindyzhao | Integration-test host-network boundary hardened locally: `make testcontainers-contract` now keeps direct outbound/host networking out of executable Go tests by rejecting `net/http.Get` / `Head` / `Post` / `PostForm`, `net/http.DefaultClient` / `DefaultTransport`, and `net.Dial` / `Listen` entry points. Tests prove `httptest.NewServer` with injected clients remains allowed while direct host/public calls fail. |
| 2026-05-29 | jindyzhao | Markdown docs reachability hardening landed locally: `make links-check` now rejects orphan Markdown files under `docs/` that are not reachable from `docs/README.md` through relative Markdown links. The docs index now links the design checklist/dependency policy and frontend subdocuments, and `go test ./scripts` carries an orphan-document failure fixture. |
| 2026-05-29 | jindyzhao | Generation freshness/idempotence hardening landed locally: `make generate-fresh` snapshots tracked plus non-ignored untracked files, runs `make generate`, rejects any generator-induced diff, then runs `make generate` again to reject non-deterministic second-run output. CI now has a dedicated `generate-fresh` job and root `scripts` tests cover stable, tracked-diff, untracked-diff, and second-run-diff cases. |
| 2026-05-29 | jindyzhao | Markdown structure lint landed locally: `make markdownlint` runs pinned `markdownlint-cli2 0.22.1` from the existing `web` lockfile, enforcing heading-level increments, unordered-list indentation, and prose line length for governed Markdown. The initial run fixed long checklist entries in `docs/design/CHECKLIST.md`, and `go test ./scripts` covers wrapper behavior. |
| 2026-05-29 | jindyzhao | PR title policy landed locally: `make pr-title-check` validates pull request titles against the Conventional Commits header shape `type(scope)!: description`, and CI now runs it as a PR-only job using `github.event.pull_request.title`. Root `scripts` tests cover accepted headers, invalid headers, missing titles, and multiline rejection. |
| 2026-05-29 | jindyzhao | PR description policy landed locally: `make pr-description-check` requires non-empty `## Risk` and `## Rollback` sections in pull request bodies, reads CI input from `GITHUB_EVENT_PATH`, and is wired as a PR-only job. Root `scripts` tests cover local env input, GitHub event JSON input, missing/empty sections, empty bodies, and fenced-code false positives. |
| 2026-05-29 | jindyzhao | Testcontainers integration-test contract landed locally: `make testcontainers-contract` now parses real Go test imports with `go/parser` and rejects packages that import `database/sql` without a same-package `testcontainers-go` import, excluding analyzer fixtures under `testdata/`. Root `scripts` tests cover same-file harnesses, split setup files, missing Testcontainers imports, fixture exclusions, and commented/string import false positives. |
| 2026-05-29 | jindyzhao | Gate hardening maturity checklist landed locally: `docs/design/ci/GATE_HARDENING_CHECKLIST.md` now records a maturity level, evidence, and next-hardening note for every activated Progressive Gate Schedule row, and `make gate-hardening-check` fails on missing, stale, duplicate, placeholder, or invalid maturity records. |
| 2026-05-29 | jindyzhao | Go license compliance gate landed locally: `make go-licenses-check` runs pinned `go-licenses v1.6.0` with `--include_tests` against the root module and `tools/openclarion-linter`, ignores first-party package prefixes while still checking dependencies, and accepts only the SPDX IDs recorded by `go-license-allow:` in `docs/design/DEPENDENCIES.md`. Root `scripts` tests cover allowlist parsing, command wiring, missing policy markers, and tool failure propagation. |
| 2026-05-29 | jindyzhao | npm OSV lockfile scan landed locally: `make osv-scan` runs pinned `osv-scanner v1.9.2` against committed `package-lock.json` files outside `node_modules`, rejects first-party npm manifests without lockfiles, and currently reports zero findings for `web/package-lock.json`. Root `scripts` tests cover lockfile discovery, node_modules exclusions, missing lockfiles, and tool failure propagation. |
| 2026-05-29 | jindyzhao | External HTTPS link inventory landed locally: `make external-links-check` inventories governed Markdown external links without network access, and `OPENCLARION_EXTERNAL_LINKS_LIVE=1` remains available for explicit HEAD/GET liveness checks. The scheduled workflow is deferred to a separate workflow-only PR to preserve workflow change isolation. Root `scripts/external_link_check` tests cover inventory mode, live success, HEAD-to-GET fallback, broken-link failures, and timeout validation. |
| 2026-05-30 | jindyzhao | E2E verification verdict consistency landed locally: `make docs-hygiene` now runs `scripts/e2e_verification_check`, which rejects missing, symlinked, or non-regular `docs/design/END_TO_END_VERIFICATION.md` inputs and fails if Chain A/B/C tables use a verdict not defined in the Verdict Scale table. The current scale now explicitly defines `proven-local` and `partial`. |
| 2026-05-28 | jindyzhao | M2 provider contract started: `LLMProvider` and provider-neutral structured JSON request/response DTOs landed in `internal/usecases/ports`, with deterministic scripted fake provider in `internal/providers/llm/fake`. Runtime fake-provider analyzer now rejects any `internal/providers/*/fake` import from production `cmd/openclarion` wiring. |
| 2026-05-28 | jindyzhao | M2 LLM output validation framework landed: `internal/usecases/llmoutput` rejects model refusals, non-`stop` finish reasons/truncation, invalid JSON, unsupported output modes, invalid schemas, and JSON Schema violations before output can be accepted. Validation failures carry reason and retryability metadata for the future retry loop. |
| 2026-05-28 | jindyzhao | M2 LLM retry framework landed: `internal/usecases/llmretry` runs up to three validated provider attempts by default, appends validation error feedback as a user message only for retryable `llmoutput` failures, and stops immediately on refusal, provider errors, invalid request metadata, or context cancellation. |
| 2026-05-28 | jindyzhao | M2 report draft contracts landed: `internal/usecases/reportdraft` defines typed SubReport/FinalReport draft outputs and strict structured-output compatible JSON Schemas, with tests for valid parse, invalid draft rejection, schema-copy isolation, and OpenAI strict schema subset constraints. This does not close the separate SubReport/FinalReport Ent schema or workflow checklist items. |
| 2026-05-28 | jindyzhao | M2 report prompt templates landed: `internal/usecases/reportprompt` builds provider-neutral SubReport requests for single-alert, cascade, and alert-storm scenarios plus FinalReport fan-in requests. Structure-only golden tests validate prompt/request shape, JSON fallback instructions, schema selection, idempotency-key shape, invalid input rejection, and schema-compatible fixture outputs without calling a real provider. Report workflows remained pending at this point. |
| 2026-05-28 | jindyzhao | M2 OpenAI-compatible provider landed: `internal/providers/llm/openai` implements Chat Completions `LLMProvider` calls with strict `json_schema` and `json_object` fallback modes, startup strict-capability probing, request-id header propagation from the idempotency key, refusal/finish/model metadata mapping, and `httptest` coverage. Temporal Activities/workflows and notification flow remain pending. |
| 2026-05-28 | jindyzhao | M2 report persistence landed: `SubReport` and `FinalReport` domain constructors, Ent schemas, generated client code, Atlas migration `20260528022647_report_schemas.sql`, `ReportRepository`, UoW `Reports()`, and testcontainers coverage for per-snapshot SubReport idempotency, global FinalReport idempotency, fan-in edge links, and newest-first report list queries. Temporal workflows, notification, and report HTTP APIs were still pending at that point. |
| 2026-05-28 | jindyzhao | M2 report Temporal workflows landed locally: `ReportFanOutWorkflow` executes `GenerateSubReport`, and `FinalReportWorkflow` executes `GenerateFinalReport`. Activities use `reportprompt` + `llmretry` + `reportdraft`, persist via `ReportRepository`, short-circuit on existing idempotency keys before re-calling the LLM, and worker registration now includes both workflows. IMProvider/Webhook, notification ordering, report HTTP APIs, and production LLM wiring were still pending at that point. |
| 2026-05-28 | jindyzhao | M2 IM provider contract landed locally: `IMProvider`, `IMNotification`, `IMDelivery`, and retry-classified `IMError` now live in `internal/usecases/ports`. Deterministic fake provider lives in `internal/providers/im/fake`; Webhook implementation lives in `internal/providers/im/webhook`, posts JSON with idempotency/report headers, optional Bearer auth, context-aware HTTP requests, 2xx delivery mapping, and retryability classification for 429/5xx/network errors. Notification Activity/workflow wiring and delivery persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 report notification flow landed locally: `FinalReportWorkflow` now executes `SendReportNotification` only after `GenerateFinalReport` returns a persisted `FinalReportID`. The notification Activity loads the persisted report, builds idempotency key `final_report:<id>/notification`, calls `IMProvider.SendNotification`, maps non-retryable IM errors into Temporal non-retryable application errors, and returns delivery metadata. End-to-end alert trigger wiring, report HTTP APIs, and delivery-log persistence were still pending at that point. |
| 2026-05-28 | jindyzhao | M2 report read APIs landed locally: OpenAPI now includes `GET /api/v1/reports` and `GET /api/v1/reports/{report_id}`, generated Go server types are refreshed, HTTP handlers read through `ReportRepository`, and detail responses include linked SubReports with `evidence_snapshot_id` for M3 evidence traceability. End-to-end alert trigger wiring and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 report batch workflow landed locally: `ReportBatchWorkflow` validates a report batch, starts one `ReportFanOutWorkflow` child per EvidenceSnapshot, preserves input order when collecting persisted SubReport IDs, then starts `FinalReportWorkflow` for fan-in persistence and notification. Worker registration and Temporal tests cover the parent-child workflow graph. Client trigger dispatch, production provider injection, and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 runtime provider injection landed locally: `cmd/openclarion` now injects OpenAI-compatible LLM and Webhook IM providers into Temporal Activities when `OPENCLARION_LLM_MODEL` / `OPENCLARION_IM_WEBHOOK_URL` are configured. LLM startup probes strict `json_schema` support and falls back as designed; partial env config fails fast. Client trigger dispatch and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 report trigger framework landed locally: `alertreplay.ReplayWindowForReport` returns persisted EvidenceSnapshot refs without changing the counter-only `ReplayWindow` API, `ports.ReportWorkflowStarter` defines the usecase boundary, Temporal `ReportStarter` starts `ReportBatchWorkflow` with stable workflow ID / task queue / timeout / duplicate policies, and `reporttrigger.BuildStartRequest` / `ReplayAndStart` map replay output into workflow starts. External HTTP/CLI/scheduled trigger surface and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 HTTP report trigger landed locally: OpenAPI now includes `POST /api/v1/report-triggers/replay-window`; the HTTP handler decodes a replay window, uses default alert grouping, calls the report trigger usecase, returns replay stats plus workflow handle, and returns 503 when Prometheus trigger wiring is not configured. `cmd/openclarion` enables this endpoint when `OPENCLARION_PROMETHEUS_URL` is set, using the Prometheus provider plus Temporal `ReportStarter`. CLI/scheduled trigger surface, live Prometheus->Temporal->Webhook E2E verification, and delivery-log persistence remain pending. |
| 2026-05-28 | jindyzhao | M2 notification delivery log landed locally: `ReportNotificationDelivery` domain state, Ent schema, migration `20260528034745_report_notification_deliveries.sql`, and repository methods now persist pending/delivered/failed notification attempts by global idempotency key. `SendReportNotification` creates or reuses the row before IMProvider calls, updates it on success/failure, and skips duplicate provider calls once a delivered row exists. Live Prometheus->Temporal->Webhook E2E verification remains pending. |
| 2026-05-28 | jindyzhao | M2 local report E2E landed: `internal/e2e/report_pipeline_test.go` starts Postgres + Temporal dev server + a real report worker, triggers `POST /api/v1/report-triggers/replay-window`, drives Prometheus/OpenAI/Webhook protocol endpoints through `httptest`, waits for `ReportBatchWorkflow`, and asserts webhook delivery plus persisted FinalReport/SubReport/ReportNotificationDelivery rows. Live external Prometheus->Temporal->Webhook verification remains pending. |
| 2026-05-28 | jindyzhao | M2 CLI one-shot report trigger landed locally: `openclarion report-replay` parses an RFC3339 replay window, connects to PostgreSQL, Prometheus, and Temporal from env config, calls the shared `reporttrigger.ReplayAndStart` usecase, prints replay stats plus workflow handle JSON, and can `--wait` for ReportBatchWorkflow completion. Scheduled reports and live external Prometheus->Temporal->Webhook verification remain pending. |
| 2026-05-28 | jindyzhao | M2 manual live smoke gate landed locally: `make report-live-smoke` runs `scripts/run_report_live_smoke.sh`, calls `openclarion report-replay --wait` against real configured services, and validates the JSON output with `scripts/report_live_smoke_output` so a live run must prove snapshot refs, persisted FinalReport ID, SubReport IDs, and notification status. Live external Prometheus->Temporal->Webhook execution remains pending until real environment evidence is captured. |
| 2026-05-29 | jindyzhao | M2 live report proof validator hardened locally: `scripts/report_live_smoke_output` now requires valid replay stats with zero ingest/replay failures, enforces one SubReport per retained snapshot, requires `notification_idempotency_key` to match `final_report:<id>/notification`, validates provider message ID formatting when supplied by the upstream, and only accepts successful notification statuses (`accepted` or `delivered`). `ReportBatchWorkflowResult` and `openclarion report-replay --wait` now expose that local notification key so retained live smoke artifacts remain traceable even when a Webhook 2xx response has no upstream message ID. `make report-live-smoke-output-test` is wired into CI so a retained `make report-live-smoke` artifact has a stronger machine-checked evidence shape before the live external E2E item can close. |
| 2026-05-29 | jindyzhao | M2 report live proof timestamp hardening landed locally: `openclarion report-replay` now emits a UTC `checked_at` field in the retained JSON artifact, and `scripts/report_live_smoke_output` rejects missing, malformed, whitespace-padded, or future `checked_at` values. Live external Prometheus->Temporal->Webhook evidence remains pending until the manual gate is run against real services and its validator-checked output is retained. |
| 2026-05-29 | jindyzhao | M2 report live proof request binding hardened locally: `openclarion report-replay` now records sanitized replay request metadata in the retained JSON artifact, and `scripts/report_live_smoke_output` rejects invalid alert windows, non-positive limits, unsupported scenarios, missing wait intent, invalid wait timeouts, or replay windows ending after `checked_at`. This binds future live external E2E evidence to the exact window and scenario it exercised. |
| 2026-05-29 | jindyzhao | Retained live proof timestamp canonicalization hardened locally: `scripts/report_live_smoke_output` now rejects non-UTC or non-canonical `checked_at`, `request.window_start`, and `request.window_end` timestamps, and `scripts/diagnosis_live_smoke_output` rejects non-UTC or non-canonical `checked_at` timestamps. This keeps retained M2 and M5 live evidence stable for diffing and audit. |
| 2026-05-29 | jindyzhao | M2 report live proof identity binding hardened locally: `scripts/report_live_smoke_output` now rejects requested workflow ID mismatches, whitespace-padded workflow/run IDs, duplicate snapshot IDs, non-contiguous snapshot group indexes, and duplicate SubReport IDs. This keeps retained intelligent-alert report evidence tied to distinct snapshots and distinct SubReports before live external E2E can be claimed. |
| 2026-05-29 | jindyzhao | M2 report live proof identifier hygiene hardened locally: `scripts/report_live_smoke_output` now bounds retained workflow IDs and run IDs to 256 bytes, rejects whitespace inside those IDs, bounds optional request correlation keys, bounds notification idempotency keys, and rejects multiline/tabbed or over-512-byte provider message IDs before a live Prometheus->Temporal->Webhook proof can support acceptance. |
| 2026-05-29 | jindyzhao | M2 report live proof replay-stat consistency hardened locally: `scripts/report_live_smoke_output` now rejects retained `make report-live-smoke` artifacts when `events_loaded` exceeds the requested limit, group outcome counts do not add up to `groups_built`, snapshot saved/duplicate counters do not match retained snapshot refs, `groups_built` does not match snapshot count, or retained snapshot `event_count` values do not sum back to `events_loaded`. Live external Prometheus->Temporal->Webhook evidence remains pending until a real validator-checked artifact is retained. |
| 2026-05-29 | jindyzhao | W3-2b forbidden-import analyzer equivalence evidence hardened locally: `tools/openclarion-linter/analyzer/forbiddenimports` now includes `analysistest` red fixtures for every retired legacy deny-list prefix, plus provider-boundary red/green fixtures and a retained deny-list drift test, so `scripts/check_no_forbidden_imports.sh` remains retired with stronger executable proof. |
| 2026-05-28 | jindyzhao | M3 backend observability foundation landed locally: `cmd/openclarion` now serves `GET /metrics` via an isolated Prometheus registry and instruments generated API handlers with low-cardinality request counter, duration histogram, and in-flight gauge metrics. OpenTelemetry traces, structured correlation IDs, frontend, and dashboard work remain pending. |
| 2026-05-28 | jindyzhao | M3 OpenTelemetry HTTP server tracing foundation landed locally: `internal/observability/tracing` configures W3C propagation, no-op default tracing, opt-in OTLP HTTP export, service resource attributes, stable route-pattern span names, and deterministic in-memory exporter tests. Collector smoke verification, outbound propagation, structured correlation IDs, frontend, and dashboards remain pending. |
| 2026-05-28 | jindyzhao | M3 HTTP correlation logging foundation landed locally: `internal/observability/correlation` preserves or generates bounded `X-Request-ID` values, exposes request/trace/span slog attributes from context, and HTTP transport error logging now uses context-aware `slog.LogAttrs`. Access logs and propagation beyond inbound HTTP remain pending. |
| 2026-05-28 | jindyzhao | M3 frontend skeleton landed locally: `web/` is a Next.js 16 / React 19 App Router project with generated OpenAPI TypeScript types, thin route pages for `/reports` and `/reports/{id}`, report list/detail feature modules, evidence snapshot traceability in detail view, and frontend gates for typecheck, lint, unit tests, build, dead-code scan, npm audit, and OpenAPI TS freshness. Dashboard, Playwright smoke, and broader frontend UX remain pending. |
| 2026-05-28 | jindyzhao | M3 basic dashboard landed locally: `GET /api/v1/dashboard` exposes recent alert counters plus final-report latest-delivery success stats; `/dashboard` is now the default web route with metric cards and alert/report breakdown panels backed by generated OpenAPI TypeScript types. |
| 2026-05-28 | jindyzhao | M3 HTTP access logging and request-id propagation landed locally: generated API routes now emit structured `http access` logs with request/trace/span correlation, stable ServeMux route patterns, status, duration, and response bytes without logging raw request targets; outbound Prometheus, OpenAI-compatible LLM, and Webhook HTTP clients propagate bounded `X-Request-ID` from context. OpenTelemetry collector smoke and trace propagation across outbound/Temporal boundaries remain pending. |
| 2026-05-28 | jindyzhao | M3 OpenTelemetry collector smoke and outbound HTTP trace propagation landed locally: `HTTPTracing` now exposes instance-scoped outbound `Transport` / `HTTPClient` helpers using the same tracer provider and W3C TraceContext+Baggage propagator as inbound middleware. Runtime Prometheus, OpenAI-compatible LLM, and Webhook clients use those wrappers when tracing is configured, and tests prove `traceparent` + `X-Request-ID` propagation plus an OTLP HTTP collector receiving exported spans. Temporal workflow trace propagation remains pending. |
| 2026-05-28 | jindyzhao | M3 Temporal workflow trace propagation landed locally: runtime Temporal clients now install the Temporal OpenTelemetry tracing interceptor when tracing is enabled, reusing OpenClarion's instance-scoped tracer provider and W3C TraceContext/Baggage propagator. Tests prove the interceptor records a workflow span accessible from workflow context. |
| 2026-05-28 | jindyzhao | M3 Playwright route smoke landed locally: `make ci-frontend-smoke` builds the Next.js app, installs the pinned Chromium browser, starts a mocked OpenClarion API plus the production Next.js server, and verifies `/dashboard`, `/reports`, and `/reports/{report_id}` render generated-contract-shaped fixture data. |
| 2026-05-28 | jindyzhao | M4 ContainerProvider contract landed locally: `internal/usecases/ports/container.go` defines the shared M4/M5 file-based sandbox invocation contract, fixed workspace paths, timeout/output caps, network policy DTOs, duplicate-key-free request/result validation, and deterministic fake provider coverage in `internal/providers/container/fake`. Docker runtime implementation, egress enforcement, and sandbox-security gate remain pending. |
| 2026-05-28 | jindyzhao | M4 sandbox security spec gate landed locally: `internal/providers/container/docker/spec.go` converts `ContainerRunRequest` into a security-reviewed Docker runtime spec and `make sandbox-security` now blocks missing digest pins, root users, writable rootfs, missing `no-new-privileges`, privileged runs, missing capability drops, missing CPU/memory limits, writable or unsafe input mounts, missing writable `/workspace/out` output mount, invalid output cap, and Docker socket mounts. Candidate runtime proof and production egress enforcement remain pending. |
| 2026-05-28 | jindyzhao | M4 agent runtime selection gate landed locally: `docs/design/agent-runtime-selection.md` records the adapter-first policy, OpenClaw/Hermes/custom thin runner evaluation criteria, and the rule that framework dependencies remain inside candidate sandbox images until a runtime baseline passes security/lifecycle smoke. `make forbidden-agent-runtime` prevents accidental control-plane dependency lock-in. |
| 2026-05-29 | jindyzhao | M4 agent-runtime dependency boundary widened locally after runtime-boundary review: `make forbidden-agent-runtime` now rejects common framework families beyond the initial OpenClaw/Hermes/LangChain list, including LangGraph, LlamaIndex, Semantic Kernel, Pydantic AI, Agno, and Mastra, while avoiding a false positive on diagnosis-oriented package names. The custom thin runner remains a file-contract proof only, not an accepted agent runtime baseline. |
| 2026-05-29 | jindyzhao | M4 agent-runtime hardcoding boundary hardened locally: `make forbidden-agent-runtime` now also scans non-test Go control-plane source under `cmd`, `internal`, and `scripts`, rejecting hard-coded runtime family names so candidate runtime identities stay in docs, evidence packets, and sandbox image contexts until a baseline is accepted. |
| 2026-05-29 | jindyzhao | M4 agent-runtime hardcoding policy externalized locally: `make forbidden-agent-runtime` now reads `docs/design/ci/agent-runtime-forbidden.tsv`, so OpenClaw/Hermes-style runtime-family names stay in configurable governance data instead of shell-script or production-code bindings. Tests prove arbitrary configured candidate families are rejected without editing the gate script. |
| 2026-05-29 | jindyzhao | M4 agent-runtime hardcoding policy hygiene hardened locally: `make forbidden-agent-runtime` now rejects duplicate TSV policy rows, whitespace-padded scopes/patterns, malformed rows, and policies missing either manifest or code coverage before scanning source. This keeps the runtime-family blocklist reviewable while preserving the generic sandbox-runtime architecture. |
| 2026-06-01 | jindyzhao | M4 agent-runtime hardcoding source scope hardened locally: `make forbidden-agent-runtime` now covers non-test shell and frontend JavaScript/TypeScript source under `scripts/` and `web/src/` in addition to Go source, with regression fixtures proving shell/UI runtime branches fail and docs/tests remain valid evidence locations. |
| 2026-05-28 | jindyzhao | M4 manual agent runtime smoke harness landed locally: `make agent-runtime-smoke` runs a digest-pinned candidate image with network-none, non-root user, readonly rootfs, `no-new-privileges`, dropped capabilities, memory/CPU/PID limits, readonly ADR-0013 input mounts, writable `/workspace/out` output mount with `fsize` cap, timeout cleanup, and duplicate-key-free JSON-object output validation. Actual OpenClaw/Hermes/custom candidate smoke results remain pending. |
| 2026-05-29 | jindyzhao | M4 runtime smoke output proof hardened locally: `scripts/agent_runtime_smoke_output` now rejects symlink/non-regular proof paths, bounds reads to the configured output cap plus one byte, rejects duplicate-key/trailing JSON, and rejects empty `{}` output objects. `make agent-runtime-smoke` therefore requires a candidate runtime to emit at least one direct-file machine-readable field while staying framework-neutral for OpenClaw, Hermes Agent, or custom thin runner images. This remains a file-contract smoke only; report-quality baseline acceptance still requires representative quality comparison and the M4 decision gate. |
| 2026-05-28 | jindyzhao | Alert-first signal extension boundary documented: OpenClarion remains an intelligent alert analysis product, while provider, evidence, workflow, report, and sandbox-runtime boundaries now explicitly avoid hard binding the architecture to one monitoring system or one alert domain. |
| 2026-05-29 | jindyzhao | Insight Pipeline / Agent Workspace boundary documented locally: OpenClarion now explicitly separates the automated alert-to-report Insight Pipeline from the user-initiated Agent Workspace while keeping one product, repository, and deployment for the current stage. The boundary keeps workflow lifecycles, output schemas, conversation state, and operation authority separate while sharing evidence, reports, providers, auth, audit, OpenAPI, PostgreSQL, Temporal infrastructure, and observability. |
| 2026-05-28 | jindyzhao | M4 Docker Engine provider lifecycle landed locally: `internal/providers/container/docker/provider.go` now prepares per-invocation readonly input files, maps the security-reviewed `RunSpec` to Docker Engine create options, starts/waits with a fixed timeout, stop/kill/removes on timeout or cancellation, copies `/workspace/out/output.json`, and validates raw result invariants. Unit tests use a fake EngineClient; real candidate runtime smoke, egress allowlist enforcement, and quality comparison remain pending. |
| 2026-05-28 | jindyzhao | M4 Docker egress fail-closed guard landed locally: `ContainerNetworkAllowlist` requests no longer reach Docker Engine create unless an injected `EgressEnforcer` accepts the requested host allowlist and network mode. This prevents a plain Docker bridge network from being mistaken for real per-target egress enforcement. |
| 2026-05-28 | jindyzhao | M4 egress policy hardening landed locally: provider-neutral allowlist targets are now normalized as exact `host[:port]` entries and reject URLs, paths, wildcards, whitespace, invalid ports, IPv6 literal syntax, and duplicates before runtime allocation. Docker now has `StaticAllowlistEnforcer`, which rejects allowlist-mode container creation unless the request is a subset of an externally provisioned proxy/firewall allowlist network. This is a tested guardrail; concrete Envoy/Squid/iptables allow-deny smoke remains pending. |
| 2026-05-28 | jindyzhao | M4 egress allow-deny smoke harness landed and passed locally: `make egress-allowdeny-smoke` builds a small Go helper, creates a Docker internal sandbox network plus an upstream network, starts allowed/denied upstream services and a dual-network allowlist proxy, proves the sandbox client can reach the allowed target through the proxy, receives 403 for the denied target, and cannot bypass the proxy to the upstream network directly. This proves the proxy topology; production wiring into a candidate runtime remains pending. |
| 2026-05-28 | jindyzhao | M4 short-lived credential boundary landed locally: `ContainerRunRequest` now carries one-invocation credentials with validated env names, non-empty values, required expiry, duplicate-name rejection, and a provider-time check that rejects expired credentials or credentials whose expiry exceeds the container timeout. The Docker provider injects accepted credentials through `container.Config.Env` only at create time and unit tests prove invalid lifetimes prevent container creation without leaking values in errors. Credential issuance/rotation remains future runtime wiring. |
| 2026-05-28 | jindyzhao | M4 Docker daemon access boundary documented: `docs/design/docker-daemon-boundary.md` records V1 host-socket constraints, forbids Docker socket exposure inside agent containers, rejects plaintext remote daemon access, requires TLS-verified client-authenticated remote Docker if remote mode is ever used, and tracks post-V1 rootless Docker / dedicated sandbox host / Kubernetes Job options behind the same `ContainerProvider` contract. |
| 2026-05-28 | jindyzhao | M4 Docker Provider live smoke landed and passed locally: `make container-provider-smoke` resolves the default BusyBox source image to a digest, runs the Docker-backed `ContainerProvider.Run` path through a real daemon, verifies ADR-0013 readonly inputs, writable `/workspace/out` output mount, archive output copy, duplicate-key-free JSON-object output, and no invocation-labelled container leak. This also corrected two fake-test blind spots: SDK create options must set only one image field, and `ContainerWait` must use `not-running` semantics for short-lived containers. |
| 2026-05-28 | jindyzhao | M4 Docker Provider timeout cleanup smoke landed locally: `make container-provider-timeout-smoke` runs the same Provider path with a two-second timeout and long-running command, expects the timeout error, and verifies no invocation-labelled container remains after stop/remove cleanup. |
| 2026-05-28 | jindyzhao | M4 Docker Provider output cap smoke landed locally: `make container-provider-output-cap-smoke` runs the Provider with a 64-byte output cap and a command that writes more than the cap, accepts either `fsize`-enforced process failure or Provider output-size rejection, and verifies no invocation-labelled container remains. |
| 2026-05-28 | jindyzhao | M4 custom thin runner candidate smoke landed locally: `make custom-thin-runner-smoke` builds a scratch image containing a minimal ADR-0013 file-contract runner, pushes it to an ephemeral localhost registry to obtain a real digest-pinned `repo@sha256` reference, then proves the same image through `make agent-runtime-smoke` and `make container-provider-smoke`. This closes the contract/lifecycle candidate smoke result; OpenClaw/Hermes framework evaluation and report-quality comparison remain pending. |
| 2026-05-28 | jindyzhao | M4 sandbox tool helper contracts landed locally: `scripts/agent_tool_metric_query` performs a bounded read-only Prometheus `/api/v1/query` instant query and emits one JSON object; `scripts/agent_tool_topology_lookup` reads a bounded static JSON topology file and emits one service-centered JSON object. `make agent-tool-scripts-test` covers success and rejection paths, and `make ci` now includes the focused tool-helper gate. Runtime image packaging and quality comparison remain pending. |
| 2026-05-28 | jindyzhao | M4 custom thin runner image packaging proof expanded locally: `make custom-thin-runner-smoke` now compiles the metric/topology helper binaries into the scratch candidate image and proves the packaged topology helper by running the digest-pinned image with `--entrypoint /tools/agent_tool_topology_lookup` under non-root, readonly, network-none settings before the runtime and Provider smokes. Production egress wiring and quality comparison remain pending. |
| 2026-05-28 | jindyzhao | M4 offline quality comparison harness landed locally: `scripts/sandbox_quality_compare` compares a direct M2 SubReport JSON with a sandbox candidate SubReport JSON after validating both through the production `reportdraft.ParseSubReport` path. `make sandbox-quality-compare-test` covers improved, equivalent, invalid, and fail-on-regression cases, and the target is now part of `make ci`. Real representative sandbox-vs-direct report comparison and the M4 proceed/iterate/defer decision remain pending. |
| 2026-05-28 | jindyzhao | M4 sandbox baseline audit landed locally: `scripts/sandbox_baseline_audit` emits a JSON proof for fixed ADR-0013 file paths, network-none batch defaults, M5 read-only turn input mounts, Docker runtime security posture, resource limits, allowlist subset enforcement, and bounded strict request/raw-output JSON validation. `make sandbox-baseline-audit` is now part of `make ci`; live Docker smokes remain the evidence for daemon cleanup behavior, and real report-quality comparison remains pending. |
| 2026-05-28 | jindyzhao | M4 minimum sandbox baseline for M5 accepted locally: `make sandbox-baseline-audit`, `make custom-thin-runner-smoke`, `make container-provider-timeout-smoke`, `make container-provider-output-cap-smoke`, and `make egress-allowdeny-smoke` all passed against the current tree. This proves auditable file-based I/O, packaged helper execution, timeout cleanup, output-size enforcement, and proxy allow/deny behavior for the M5 short-conversation foundation. Real representative report-quality comparison and the M4 proceed/iterate/defer decision remain pending. |
| 2026-05-28 | jindyzhao | M4 quality comparison harness now accepts a batch evidence manifest: `scripts/sandbox_quality_compare --manifest <file>` validates every direct/sandbox SubReport pair through the production parser, resolves relative paths from the manifest directory, emits per-case deltas plus aggregate recommendation counts, and fails on any regression when `--fail-on-regression` is set. Manifest evidence now carries `sample_basis`, per-case alert scenario labels, and `scenario_coverage`; real representative sample outputs are still required before closing quality comparison. |
| 2026-05-28 | jindyzhao | M4 sandbox decision evidence gate landed locally: `scripts/sandbox_m4_decision` combines baseline audit output, manifest-mode quality comparison output, runtime smoke evidence, and human review evidence into a conservative proceed/iterate/defer JSON decision. `make sandbox-m4-decision-test` is now in CI, while manual `make sandbox-m4-decision` requires real evidence files before any M4 decision can be recorded. |
| 2026-05-28 | jindyzhao | M4 sandbox evidence packet assembler landed locally: `scripts/sandbox_m4_evidence_packet` freezes baseline audit output, manifest-mode quality comparison output, copied review evidence, decision output, and packet summary into one empty artifact directory. `make sandbox-m4-evidence-packet-test` is now in CI; manual packet creation remains pending real candidate evidence. |
| 2026-05-29 | jindyzhao | M4 sandbox decision evidence source validation hardened locally: `scripts/sandbox_m4_decision` now requires each runtime-smoke source in review evidence to match its canonical `make` target, preventing free-form pass notes from satisfying candidate runtime, provider lifecycle, timeout, output-cap, or egress smoke evidence. |
| 2026-05-29 | jindyzhao | M4 quality evidence scenario coverage hardened locally: `scripts/sandbox_quality_compare` now requires manifest `sample_basis` plus per-case alert prompt scenario labels and emits `scenario_coverage`; `scripts/sandbox_m4_decision` now defers proceed unless the quality comparison covers `single_alert`, `cascade`, and `alert_storm`. |
| 2026-05-29 | jindyzhao | M4 sandbox evidence packet artifact validation hardened locally: `scripts/sandbox_m4_evidence_packet` now validates generated baseline, quality, and decision helper stdout before writing packet artifacts. The packet assembler rejects missing required baseline pass checks, weak manifest quality outputs without sample/scenario coverage, and invalid decision enums or missing reasons. |
| 2026-05-29 | jindyzhao | M4 sandbox evidence packet review evidence validation hardened locally: `scripts/sandbox_m4_evidence_packet` now rejects copied review evidence without a dated representative sample basis, runtime candidate, all required runtime smoke entries with canonical `make` sources, or human reviewer/status metadata. |
| 2026-05-29 | jindyzhao | M4 baseline audit check uniqueness hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now reject duplicate baseline audit check names before retained M4 evidence can drive a decision or packet, preventing map-overwrite behavior from hiding a failed or missing required baseline check. |
| 2026-05-29 | jindyzhao | M4 sample-basis consistency hardened locally: `scripts/sandbox_m4_decision` now defers when review evidence `sample_basis` differs from the manifest-mode quality comparison, and `scripts/sandbox_m4_evidence_packet` rejects copied review evidence before writing when its `sample_basis` does not match the generated quality comparison. |
| 2026-05-29 | jindyzhao | M4 evidence schema/status validation hardened locally: the sandbox decision and evidence-packet helpers now reject quality comparison artifacts without the current SubReport schema, reject cases missing `review_required: true`, reject future-dated review evidence, and accept only `pass` / `fail` runtime and human review statuses. This keeps the remaining quality comparison and M4 decision items evidence-bound rather than documentation-bound. |
| 2026-05-29 | jindyzhao | M4 quality summary consistency hardened locally: the sandbox decision and evidence-packet helpers now recompute quality summary counts from `cases[].recommendation`, reject mismatched top-level summaries, and reject duplicate quality case IDs before packet quality artifacts are written. This keeps retained alert-analysis quality evidence case-derived instead of hand-editable. |
| 2026-05-29 | jindyzhao | M4 evidence packet digest binding hardened locally: `scripts/sandbox_m4_evidence_packet` now records SHA-256 digests for `baseline-audit.json`, `quality-comparison.json`, `review-evidence.json`, and `decision.json` in `packet.json`, binding retained runtime-baseline evidence to exact artifact contents rather than only local file paths. |
| 2026-05-29 | jindyzhao | M4 decision evidence packet consistency hardened locally: `scripts/sandbox_m4_evidence_packet` now rejects `decision.json` when its evidence summary does not match the same packet's baseline audit status, quality recommendation, case count, scenario coverage, sample basis, runtime candidate, runtime smoke pass count, representative-sample flag, human review status, or `--min-cases` input. |
| 2026-05-29 | jindyzhao | M4 quality manifest portability hardened locally: `scripts/sandbox_quality_compare --manifest` now requires slash-separated relative report paths under the manifest directory, rejects parent traversal and drive/URL-style paths, rejects direct/sandbox path pairs that normalize to the same file, and rejects repeated normalized report paths across cases. This prevents local absolute paths, self-comparisons, or duplicated report files from inflating retained quality evidence. |
| 2026-05-29 | jindyzhao | M4 quality evidence binding hardened locally: manifest-mode `scripts/sandbox_quality_compare` now requires per-case `required_evidence_refs`, validates that both direct and sandbox SubReports contain every declared ref, and carries those refs into quality output consumed by the decision and evidence-packet gates. This keeps representative runtime quality evidence bound to the same frozen alert evidence instead of only to paired filenames. |
| 2026-05-29 | jindyzhao | M4 quality EvidenceSnapshot binding hardened locally: manifest-mode quality cases, M4 decision input, and M4 evidence-packet quality artifacts now require at least one canonical `snapshot:<positive-id>` ref in `required_evidence_refs`. This keeps representative runtime quality evidence tied to frozen EvidenceSnapshot IDs, while runtime candidate identities remain evidence-supplied and are not hard-coded into the control plane. |
| 2026-05-29 | jindyzhao | M4 quality manifest sample-basis hygiene hardened locally: `scripts/sandbox_quality_compare --manifest` now rejects missing, whitespace-padded, multiline/tabbed, or over-2048-byte `sample_basis` values before emitting retained quality comparison output. This keeps the quality evidence basis stable before the M4 decision and evidence-packet gates consume it. |
| 2026-05-29 | jindyzhao | M4 quality manifest identity/path hygiene hardened locally: manifest-mode `scripts/sandbox_quality_compare` now rejects multiline/tabbed or overlong case IDs, evidence refs, and report paths before comparing reports or emitting quality output. This keeps retained representative alert-analysis evidence reviewable without binding the platform to a specific agent runtime. |
| 2026-05-29 | jindyzhao | M4 runtime candidate evidence immutability hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now require review/decision `runtime_candidate` fields to use immutable `name@sha256:<64-hex-digest>` image references, so tag-only sandbox runtime claims cannot support a decision or evidence packet. |
| 2026-05-29 | jindyzhao | M4 human review evidence explanation hardened locally: `scripts/sandbox_m4_decision` now defers when review evidence lacks `human_review.notes`, and `scripts/sandbox_m4_evidence_packet` rejects copied review evidence before writing if notes are missing or whitespace-padded. Retained human review claims now require an auditable explanation rather than only pass/fail status. |
| 2026-05-29 | jindyzhao | M4 review evidence case binding hardened locally: `scripts/sandbox_m4_decision` now requires review evidence `reviewed_cases` to match quality-comparison case IDs exactly; missing or stale case IDs defer, and case-level review failures iterate. `scripts/sandbox_m4_evidence_packet` now cross-checks decision `reviewed_case_count` against review and quality artifacts before freezing a packet. |
| 2026-05-29 | jindyzhao | M4 evidence packet review-case identity matching hardened locally: `scripts/sandbox_m4_evidence_packet` now independently compares frozen review `reviewed_cases[].id` values with frozen quality `cases[].id` values, rejecting packets that only match counts but contain missing or stale reviewed case IDs. |
| 2026-05-29 | jindyzhao | M4 decision reason retention hardened locally: `scripts/sandbox_m4_decision` keeps `defer` precedence for incomplete evidence, but retained `reasons` now also include any detected iterate-class failures such as runtime smoke source failures, quality regressions, or failed case review. This keeps retained evidence from hiding explicit failures behind an incomplete-evidence decision. |
| 2026-05-29 | jindyzhao | M4 evidence packet decision-reason consistency hardened locally: `scripts/sandbox_m4_evidence_packet` now rejects decision artifacts whose `reasons` are blank, whitespace-padded, duplicated, inconsistent with `proceed`, or incorrectly reuse the proceed success reason for `iterate` / `defer`. This keeps packet decisions tied to retained evidence semantics instead of accepting arbitrary helper text. |
| 2026-05-29 | jindyzhao | M4 runtime candidate evaluation evidence generalized locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now require review evidence to name a generic `selected_candidate` and matching `candidate_evaluations[]` entry with status `pass`, while candidate IDs remain operator-supplied evidence values rather than code-defined OpenClaw/Hermes/custom enums. This keeps the runtime gate auditable without hard-binding OpenClarion to any particular agent framework. |
| 2026-05-29 | jindyzhao | M4 runtime candidate wording boundary clarified locally: runtime smoke and architecture docs now describe candidates as operator-supplied evidence and accepted sandbox adapters, not platform enums, product defaults, or Go control-plane branches. OpenClaw/Hermes-style names remain examples in docs, governance policy, and red-test fixtures only. |
| 2026-05-29 | jindyzhao | M4 selected-candidate runtime reference binding hardened locally: pass-status `candidate_evaluations[]` entries now require an immutable `runtime_candidate` image ref, and the selected candidate's evaluation ref must match the top-level review-evidence `runtime_candidate`. `scripts/sandbox_m4_decision` defers on missing or mismatched selected refs, while `scripts/sandbox_m4_evidence_packet` rejects such review evidence before freezing a packet. |
| 2026-05-29 | jindyzhao | M4 selected-candidate runtime smoke reference binding hardened locally: pass-status `candidate_evaluations[]` entries now require `runtime_smoke_refs` covering every required runtime smoke name, so a selected generic runtime candidate cannot claim pass status without explicitly citing the canonical runtime contract, Provider lifecycle, timeout cleanup, output-cap, and egress smoke evidence. |
| 2026-05-29 | jindyzhao | M4 runtime smoke evidence vocabulary hardened locally: `scripts/sandbox_m4_decision` and `scripts/sandbox_m4_evidence_packet` now reject unknown `runtime_smokes[].name` values, so retained review evidence can only contain the exact required runtime contract, Provider lifecycle, timeout cleanup, output-cap, and egress smoke names rather than ad hoc pass claims. |
| 2026-05-29 | jindyzhao | M4 runtime smoke artifact retention hardened locally: the sandbox decision and evidence-packet helpers now require every retained `runtime_smokes[]` entry to carry a distinct bounded single-line `evidence_ref` and a 64-character lowercase `evidence_sha256` digest, so future candidate acceptance cannot rely only on hand-written pass/status claims without retained smoke output pointers. |
| 2026-05-29 | jindyzhao | M4 runtime smoke artifact-ref portability hardened locally: retained `runtime_smokes[].evidence_ref` values must now be normalized relative artifact paths; absolute paths, traversal segments, URI-style refs, backslashes, spaces, and duplicate refs are rejected before a sandbox decision or evidence packet can support candidate acceptance. |
| 2026-05-29 | jindyzhao | M4 evidence packet runtime-smoke artifact freezing hardened locally: `scripts/sandbox_m4_evidence_packet` now resolves runtime-smoke `evidence_ref` paths under `--runtime-smoke-artifacts-root` or the review-evidence directory, verifies each source file against `evidence_sha256`, copies the files into `runtime-smoke-artifacts/`, and records packet-local copied paths plus digests in `packet.json`. |
| 2026-05-29 | jindyzhao | M4 evidence packet path portability hardened locally: retained `packet.json` now records `out_dir` as `.` and stores generated artifact plus helper output paths as slash-separated packet-local paths, while execution and validation still use filesystem paths internally. |
| 2026-05-29 | jindyzhao | M4 evidence packet quality-input freezing hardened locally: `scripts/sandbox_m4_evidence_packet` now copies the quality manifest and every manifest-named direct/sandbox SubReport into packet-local `quality-inputs/` paths, validates manifest/output identity, validates copied reports with the production SubReport parser, requires declared refs to appear in both reports, localizes the retained quality helper manifest argument, and records copied input SHA-256 digests in `packet.json`. |
| 2026-05-29 | jindyzhao | M4 evidence packet offline verification hardened locally: `scripts/sandbox_m4_evidence_packet --verify-packet <packet-dir-or-json>` now revalidates retained packet-local paths, helper command metadata, closed packet file inventory, core artifact SHA-256 digests, quality-input copies, runtime-smoke artifact copies, and decision evidence consistency without rerunning helper commands. `make sandbox-m4-evidence-packet-verify` exposes the manual retained-packet audit command before any real runtime baseline is accepted. |
| 2026-05-29 | jindyzhao | M4 evidence packet source-file regularity hardened locally: `scripts/sandbox_m4_evidence_packet` now rejects symlinks and other non-regular files before reading, hashing, or copying quality manifests, review evidence, direct/sandbox SubReports, and runtime-smoke artifacts. This prevents retained runtime-baseline packets from being assembled through indirect links to files outside the reviewed evidence tree. |
| 2026-05-29 | jindyzhao | M4 standalone evidence source-file regularity hardened locally: `scripts/sandbox_quality_compare` now rejects symlink/non-regular manifests and direct/sandbox SubReports before parsing, and `scripts/sandbox_m4_decision` rejects symlink/non-regular baseline, quality, and review evidence inputs before parsing. This gives the manual comparison and decision targets the same direct-file evidence boundary as the retained packet assembler. |
| 2026-05-29 | jindyzhao | M4 review evidence text hygiene hardened locally: the sandbox decision and evidence-packet helpers now reject multiline, tabbed, whitespace-padded, empty, or over-2048-byte human-authored review evidence text across sample basis, reviewer notes, candidate evaluation source/notes, and reviewed-case notes. This keeps retained runtime decision packets stable for review and diffing. |
| 2026-05-29 | jindyzhao | M4 decision/packet identity hygiene hardened locally: the sandbox decision and evidence-packet helpers now reject whitespace-padded `selected_candidate` values plus multiline/tabbed or overlong quality/review case IDs and required evidence refs before accepting retained evidence. Runtime candidate identities remain generic evidence values rather than OpenClaw/Hermes/custom code branches. |
| 2026-05-29 | jindyzhao | Retained JSON evidence strictness hardened locally: `internal/strictjson` rejects duplicate object keys and unknown struct fields before Go unmarshalling, and M2 report live proof, M4 quality comparison, M4 decision, M4 evidence packet, and M5 diagnosis live proof validators now use it for operator-supplied or helper-emitted artifacts. This prevents later duplicate keys from silently overriding earlier evidence fields and prevents stale/extra proof fields from being mistaken as accepted evidence in retained proof JSON. |
| 2026-05-29 | jindyzhao | OpenAPI fingerprint lock strictness hardened locally: `make openapi-fingerprint` now rejects duplicate object keys and trailing JSON in `docs/design/ci/locks/openapi-critical.lock` before comparing critical-node hashes, with root `scripts` tests covering weak-lock rejection. |
| 2026-05-29 | jindyzhao | M4 topology helper input strictness hardened locally: `scripts/agent_tool_topology_lookup` now rejects symlink/non-regular mounted topology files before opening them and rejects duplicate object keys plus trailing JSON values in mounted static topology files before emitting service-centered lookup output, aligning the helper with the repository's strict evidence parsing boundary. |
| 2026-05-29 | jindyzhao | M4 metric helper response strictness hardened locally: `scripts/agent_tool_metric_query` now rejects duplicate-key, unknown-field, and trailing-value Prometheus response JSON in both the top-level envelope and nested query data before emitting sandbox helper output. |
| 2026-05-29 | jindyzhao | ContainerProvider strict JSON boundary hardened locally: request-side evidence/conversation/message files and result-side sandbox `output.json` now share `internal/strictjson` duplicate-key and trailing-value rejection before provider execution or schema-specific acceptance. This keeps the M4 batch and M5 per-turn sandbox contract audit-friendly while preserving the intelligent-alert-analysis product direction. |
| 2026-05-29 | jindyzhao | M4 custom thin runner input strictness hardened locally: the local scratch runner now rejects symlink/non-regular ADR-0013 evidence, conversation, and message JSON paths before reading and rejects duplicate object keys plus trailing JSON before hashing files or writing its contract proof. This keeps the candidate runtime proof aligned with the strict ContainerProvider boundary without expanding the project beyond intelligent alert analysis. |
| 2026-05-29 | jindyzhao | M4 runtime-agnostic Docker sandbox baseline closed locally after revalidation. `make sandbox-security`, `make sandbox-baseline-audit`, `make container-provider-smoke`, `make container-provider-timeout-smoke`, `make container-provider-output-cap-smoke`, and `make egress-allowdeny-smoke` passed against Docker 29.1.5, proving non-root readonly execution, resource limits, timeout cleanup, output-cap handling, network-none defaults, allowlist subset enforcement, and proxy allow/deny behavior. Remaining M4 work is representative alert-analysis quality comparison and the recorded proceed/iterate/defer decision. |
| 2026-05-29 | jindyzhao | PR wall-clock budget hardening landed locally: `make pr` now runs `make ci` through `scripts/pr_budget` with a default 15-minute enforced budget and timeout termination, while `make pr-budget-test` and a dedicated CI job cover wrapper parsing, warn mode, over-budget failure, and child exit-code preservation. The former Future Hardening `make pr` budget row is retired. |
| 2026-05-29 | jindyzhao | Allowlist discipline hardening landed locally: `make allowlist-discipline` now validates repository allowlist entries for adjacent owner, expiry, and removal-trigger metadata; the current gitleaks false-positive allowlist entries carry 2026-08-31 expiry dates, and the former Future Import / Future Hardening allowlist-discipline rows are retired. |
| 2026-05-29 | jindyzhao | Workflow change isolation hardening landed locally: PR-only `make workflow-change-guard` now rejects PRs that touch more than one GitHub Actions workflow file, keeping CI review focused while preserving the standard script + Makefile + docs companion edits for a single workflow change. The former Future Hardening single-PR-per-workflow-file row is retired. |
| 2026-05-29 | jindyzhao | PR impact reference hardening landed locally: PR-only `make pr-impact-reference-check` now requires issue or ADR references in PR bodies when ADR or future sandbox-boundary paths change, preserving decision traceability for architecture and runtime-governance edits. The former Future Hardening issue/ADR reference row is retired. |
| 2026-05-29 | jindyzhao | Go toolchain version consistency hardening landed locally: `make go-toolchain-check` now treats root `go.mod` as the patch-pinned Go toolchain source, requires first-party submodule `go.mod` directives to match it exactly, requires `.golangci.yml` to use the corresponding major.minor language version, and rejects hard-coded `actions/setup-go` versions in workflows. |
| 2026-05-30 | jindyzhao | Webhook IM provider response strictness hardened locally: response bodies now use a 1 MiB plus-one bounded read so oversized upstream responses are detected instead of silently truncated, and 2xx JSON delivery envelopes reject non-object payloads, duplicate keys, or trailing values before retaining raw provider metadata. Unknown success metadata remains preserved for provider compatibility, and focused tests cover ambiguous responses, oversized responses, and metadata preservation. |
| 2026-05-28 | jindyzhao | M5 diagnosis room policy boundary landed locally: `internal/usecases/diagnosisroom` provides pure short-conversation validation for turn/time/message/context limits, duplicate `message_id` rejection, in-flight turn rejection, caller-supplied time checks, ADR-0013 mount context byte accounting, and basic unsafe-instruction denylist matches. `make diagnosis-room-policy-test` is now in CI; WebSocket, AuthProvider/RBAC, Temporal room workflow, ChatSession/ChatTurn persistence, audit events, and final notification remain pending. |
| 2026-05-28 | jindyzhao | M5 auth/RBAC/WS ticket boundary landed locally: `internal/usecases/ports/providers.go` defines provider-neutral bearer authentication DTOs, and `internal/usecases/diagnosisauth` covers owner/admin RBAC, leader-tier rejection, cryptographically random URL-safe ticket issuance, TTL <= 30s, atomic single-use consumption, expiry/wrong-session rejection, and a defensive in-memory store. `make diagnosis-auth-test` is now in CI; OIDC, PostgreSQL ticket storage, and WebSocket handler wiring remain pending. |
| 2026-05-28 | jindyzhao | M5 PostgreSQL WS ticket store landed locally: `DiagnosisAuthTicket` Ent schema plus migration `20260528122257_diagnosis_auth_tickets.sql` persist short-lived ticket metadata with `sha256(token)` instead of raw tokens. `NewDiagnosisAuthTicketStore` implements duplicate-token mapping, expiry/unknown rejection, UTC microsecond normalization, and conditional single-winner consumption under concurrency. `make diagnosis-auth-test` now covers both pure usecase and persistence paths; OIDC and WebSocket handler wiring remain pending. |
| 2026-05-28 | jindyzhao | M5 OIDC AuthProvider landed locally: `internal/providers/auth/oidc` verifies signed OIDC ID tokens through issuer discovery/JWKS, enforces client ID audience/expiry/signature checks, maps configured role claims into owner/admin roles, and rejects malformed bearer/token/role inputs. `internal/providers/auth/fake` provides deterministic scripted auth. `make diagnosis-auth-test` now covers provider, usecase, and persistence auth paths; WebSocket ticket endpoint/upgrade wiring remains pending. |
| 2026-05-28 | jindyzhao | M5 WebSocket auth handshake boundary landed locally: OpenAPI now defines `POST /api/v1/diagnosis/ws-ticket`; `internal/transport/http` issues tickets only after AuthProvider authentication and owner/admin RBAC, and `GET /ws/diagnosis` consumes a valid same-session ticket before upgrade. The WS path rejects bad origins before ticket consumption and hands off a redacted ticket to the next handler. `make diagnosis-auth-test` now includes transport coverage; full chat relay, DiagnosisRoomWorkflow, ChatSession/ChatTurn persistence, audit events, and final close notification remain pending. |
| 2026-05-28 | jindyzhao | M5 ChatSession/ChatTurn persistence boundary landed locally: domain constructors enforce session lifecycle and append-only turn invariants; Ent schemas and Atlas migration `20260528132100_chat_sessions_turns.sql` add `chat_sessions` and `chat_turns`; `DiagnosisRepository` now persists sessions, updates lifecycle metadata, appends turns, rejects duplicate per-session `message_id` / `sequence`, and lists transcript turns in canonical order. `make diagnosis-chat-persistence-test` is now in CI; WebSocket relay, DiagnosisRoomWorkflow, lifecycle audit events, and final close notification remain pending. |
| 2026-05-28 | jindyzhao | M5 DiagnosisRoomWorkflow control-plane skeleton landed locally: `DiagnosisRoomWorkflow` registers `submit-turn` Update with a policy Validator, `state` Query for reconnect/read state, `close` and `cancel` Signals, and durable idle/session timers using workflow time. It stores accepted user turns in workflow state only and does not yet run the sandbox activity or persist turns. `make diagnosis-room-workflow-test` is now in CI; WS relay, per-turn ContainerProvider Activity, persistence Activity wiring, lifecycle audit events, and final close notification remain pending. |
| 2026-05-28 | jindyzhao | M5 per-turn sandbox Activity boundary landed locally: `RunDiagnosisTurn` builds the ADR-0013 `ContainerRunRequest` for the `diagnosis-assistant` agent, mounts frozen evidence, prior conversation, and latest user message, uses network-none defaults plus the policy turn timeout, validates raw `output.json` against the V1 diagnosis-turn JSON Schema, and returns the accepted assistant turn to `DiagnosisRoomWorkflow`. The workflow now appends user+assistant turns to workflow state and proves concurrent Update rejection while a turn Activity is in flight. ChatTurn persistence wiring, WS relay, lifecycle audit events, and final close notification remain pending. |
| 2026-05-28 | jindyzhao | M5 DiagnosisRoomWorkflow transcript persistence landed locally: startup calls `EnsureDiagnosisChatSession`, accepted Updates call `PersistDiagnosisTurn` after sandbox output validates, and the persistence Activity writes the user+assistant `ChatTurn` pair plus `ChatSession.turn_count` in one idempotent transaction. Activity retries re-fetch existing rows by per-session message IDs. `make diagnosis-room-workflow-test` now covers direct persistence Activity idempotency plus workflow scheduling. WS relay, lifecycle audit events, frontend route, and final close notification remain pending. |
| 2026-05-28 | jindyzhao | M5 WebSocket relay landed locally: `ports.DiagnosisRoomWorkflowClient` keeps the HTTP/WebSocket layer independent of Temporal, `DiagnosisRoomClient` maps room sessions to stable Temporal workflow ids and calls `UpdateWorkflow` / `QueryWorkflow`, and `DiagnosisWebSocketRelay` forwards authenticated `submit_turn` and `query_state` frames after single-use ticket consumption. `make diagnosis-auth-test` now covers relay result frames, reconnect query frames, bounded Update timeout behavior, and disconnect handling that does not cancel the submitted workflow Update. Lifecycle audit events, final close notification, and frontend route remain pending. |
| 2026-05-28 | jindyzhao | M5 lifecycle audit landed locally: `EnsureDiagnosisChatSession`, `PersistDiagnosisTurn`, and `CloseDiagnosisChatSession` now write idempotent `DiagnosisTaskEvent` rows for `diagnosis_room.opened`, `diagnosis_room.turn_persisted`, and `diagnosis_room.closed`; `CloseDiagnosisChatSession` also persists terminal `ChatSession.closed_at` and `close_reason`. `make diagnosis-room-workflow-test` covers lifecycle Activity idempotency and workflow close scheduling. Final close notification and frontend route remain pending. |
| 2026-05-28 | jindyzhao | M5 diagnosis-room close notification landed locally: `SendDiagnosisRoomCloseNotification` reuses `IMProvider` after `CloseDiagnosisChatSession`, sends a diagnosis-task-scoped idempotent notification for the alert group, records `diagnosis_room.close_notification_sent`, and skips duplicate provider sends when the audit event already exists. `IMNotification` now supports `DiagnosisTaskID` without weakening FinalReport notification validation. `make diagnosis-room-workflow-test` covers workflow scheduling and direct Activity idempotency. Frontend route remains pending. |
| 2026-05-28 | jindyzhao | M5 diagnosis-room frontend route landed locally: `/diagnosis-room` is a thin App Router page over `web/src/features/diagnosis-room`, ticket issuance uses generated OpenAPI types, WebSocket frame handling is isolated to the diagnosis-room feature module, and the Playwright route smoke uses a mocked API/WebSocket server to prove ticket bootstrap, `ready`/`state`, and one `submit_turn`/`turn_result` exchange in a production Next server. Live browser acceptance against a real backend/worker stack remains pending. |
| 2026-05-28 | jindyzhao | M5 live diagnosis-room browser smoke harness landed locally: `make diagnosis-live-browser-smoke` runs `web/playwright.live.config.ts` / `npm run smoke:live` against a real backend API, existing diagnosis-room session, bearer token, and sandbox-capable worker. The gate requires one browser connect -> state query -> submit_turn -> turn_result round trip and writes a JSON proof. Real-stack execution evidence remains pending before the M5 checklist item can close. |
| 2026-05-28 | jindyzhao | M5 diagnosis-room runtime wiring landed locally: `cmd/openclarion` now injects OIDC AuthProvider, PostgreSQL-backed WebSocket ticket store, ChatSession-backed session resolver, Temporal `DiagnosisRoomClient`, exact-origin CORS / WebSocket origin allowlist, and Docker-backed `ContainerProvider` from env. Runtime wiring tests cover configured, unconfigured, and partial-config fail-fast paths. Live browser proof still requires starting a real room session and running `make diagnosis-live-browser-smoke`. |
| 2026-05-29 | jindyzhao | M5 diagnosis-room creation path landed locally: `POST /api/v1/diagnosis/rooms` authenticates the OIDC bearer principal, loads a frozen EvidenceSnapshot, starts `DiagnosisRoomWorkflow` through `DiagnosisRoomStarter`, waits until workflow startup has persisted its `DiagnosisTask` and `ChatSession`, then returns the session/workflow handle. `make diagnosis-live-browser-smoke` can now create a room from `OPENCLARION_LIVE_EVIDENCE_SNAPSHOT_ID` instead of requiring a pre-existing session. Real-stack browser evidence remains pending before the final M5 live acceptance item can close. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof validator landed locally: `scripts/diagnosis_live_smoke_output` validates the retained `make diagnosis-live-browser-smoke` JSON proof and rejects malformed, trailing-log-polluted, weak-evidence, or mismatched room-creation artifacts. It now also requires structured Playwright browser observations for state load, `turn_result`, submitted-message visibility, connected status after the turn, assistant-turn count increment, user+assistant transcript pair increment, completed-turn log consistency with the assistant count, and transcript count consistency with the pair model. `make diagnosis-live-smoke-output-test` is now wired into CI. Real-stack browser execution evidence remains pending before the final M5 live acceptance item can close. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof URL/time validation hardened locally: `scripts/diagnosis_live_smoke_output` now rejects future `checked_at` values and rejects `web_base_url` / `api_base_url` values with user info, query strings, or fragments. This keeps retained real-stack proof metadata credential-free and stable. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof request binding hardened locally: `make diagnosis-live-browser-smoke` now records the exercised request mode, session id, evidence snapshot id, and message length in the retained JSON proof, and `scripts/diagnosis_live_smoke_output` rejects mismatches between request metadata and the top-level proof before the artifact can support live M5 acceptance. Real-stack browser execution evidence remains pending before the final M5 live acceptance item can close. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof scalar canonicalization hardened locally: `scripts/diagnosis_live_smoke_output` now rejects whitespace-padded `checked_at`, string evidence snapshot IDs, completed-turn text, and created-room run IDs rather than accepting trimmed values. This keeps retained browser-smoke proof fields stable and machine-readable before live M5 acceptance can be claimed. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room evidence snapshot ID canonicalization reconciled locally: `scripts/run_diagnosis_live_browser_smoke.sh` now writes numeric proof IDs, `scripts/diagnosis_live_smoke_output` accepts only `null` or positive JSON integer values for top-level and request `evidence_snapshot_id` fields, and the created-room proof fixtures now use numeric IDs instead of quoted numbers. This aligns the proof producer, tests, validator behavior, and the documented canonical-scalar requirement before any real-stack browser proof can support M5 acceptance. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room submitted-message proof binding hardened locally: the live Playwright proof now records the browser-submitted message length and SHA-256 digest, the smoke harness writes the trimmed message length and digest into retained request metadata, and `scripts/diagnosis_live_smoke_output` rejects mismatches between browser observation and top-level proof metadata. This keeps retained live browser evidence tied to the message actually submitted in the UI without retaining message plaintext. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof identity hygiene hardened locally: `scripts/diagnosis_live_smoke_output` now rejects whitespace-containing or overlong top-level/request session IDs plus created-room workflow/run IDs before a browser proof can support acceptance. This keeps retained real-stack evidence identifiers stable for WebSocket reconnect, Temporal workflow, and audit correlation. |
| 2026-05-29 | jindyzhao | M5 live diagnosis-room proof evidence text hygiene hardened locally: `scripts/diagnosis_live_smoke_output` now rejects missing, whitespace-padded, multiline/tabbed, or over-512-byte retained `evidence` summaries before accepting browser proof. The summary must remain a bounded single-line `turn_result` claim rather than arbitrary logs. |
| 2026-05-29 | jindyzhao | Retained live proof source-file regularity hardened locally: `scripts/report_live_smoke_output` and `scripts/diagnosis_live_smoke_output` now reject symlink and other non-regular proof-file paths before reading JSON. This closes the same indirect-file evidence gap for M2 report smoke and M5 browser smoke validators that was already closed for M4 decision packets. |
| 2026-05-29 | jindyzhao | M4 runtime smoke output source-file regularity hardened locally: `scripts/agent_runtime_smoke_output` now rejects symlink and other non-regular output proof paths before opening them, then reads through a hard cap rather than trusting pre-read file size. This keeps candidate runtime smoke artifacts direct, bounded, and audit-friendly before any M4 baseline decision can cite them. |
| 2026-05-29 | jindyzhao | M4 topology helper source-file regularity hardened locally: `scripts/agent_tool_topology_lookup` now rejects symlink and other non-regular topology JSON paths before reading. This keeps future sandbox runtime helper inputs direct, bounded, and reviewable when helper output is used as evidence for report quality comparison. |
| 2026-05-29 | jindyzhao | M4 custom thin runner ADR-0013 source-file regularity hardened locally: the runner now rejects symlink and other non-regular evidence, conversation, and message JSON paths before reading and hashing them. This keeps the local contract proof from accepting indirect mounted inputs while still avoiding planner, memory, approval, or runtime-framework behavior in Go. |
| 2026-05-30 | jindyzhao | Dependabot linter-tooling policy tightened locally: ordinary minor/major `golang.org/x/tools` updates in `tools/openclarion-linter` are now suppressed because the custom golangci-lint plugin must match the exact `x/tools` version embedded in the pinned `GOLANGCI_LINT_VERSION` binary. Coordinated tooling updates and Dependabot security updates remain allowed. |
| 2026-05-30 | jindyzhao | D10 phase-checklist split deferral re-evaluated locally: keep `docs/design/CHECKLIST.md` as the single milestone checklist for now, because the detailed status load is already carried by `CURRENT_STATE.md`, `docs/roadmap/tasks.md`, phase docs, and M4 evidence documents. Future splitting now requires concrete checklist-size, phase-owner, or reviewer-friction evidence. |
| 2026-05-30 | jindyzhao | OpenAPI breaking-change sunset tests landed locally: root `scripts` tests now prove `check_openapi_breaking.sh` soft-fails only before the 2026-06-10 sunset, hard-fails on and after the sunset, accepts non-breaking diffs, and rejects invalid sunset or current-date configuration before invoking `oasdiff`. |
| 2026-05-30 | jindyzhao | OpenAPI fingerprint spec-input strictness hardened locally: `make openapi-fingerprint` now parses `api/openapi.yaml` through a single-document duplicate-key-rejecting YAML node walk before hashing critical API nodes. |
| 2026-05-30 | jindyzhao | OpenAI-compatible provider response-envelope strictness hardened locally: successful Chat Completions responses are now bounded to 4 MiB and reject duplicate-key or trailing-value JSON before unmarshalling, while unknown provider metadata remains ignored for compatibility. Ambiguous JSON API error envelopes are rejected before capability fallback decisions, and focused `httptest` coverage exercises ambiguous success envelopes, oversized success envelopes, plain-text API errors, and ambiguous capability-probe errors. |
| 2026-05-30 | jindyzhao | HTTP ingress JSON strictness hardened locally: report replay, diagnosis room creation, diagnosis WebSocket ticket issuance, and diagnosis WebSocket client frames now reject duplicate object keys, unknown fields, trailing JSON values, and overlarge request bodies before calling usecases or workflow clients. This extends the existing strict retained evidence JSON boundary to user-facing request input without changing OpenClarion's intelligent alert analysis direction. |
| 2026-05-30 | jindyzhao | Prometheus provider endpoint configuration hardened locally: `prometheus.NewProvider` now rejects userinfo-bearing endpoint URLs before constructing the upstream client, keeping runtime endpoint credentials out of base URLs while leaving Bearer authentication on the explicit provider option. |
| 2026-05-30 | jindyzhao | Prometheus MetricsProvider response strictness hardened locally: Prometheus alert success envelopes and API 400/422 error envelopes are now capped and checked for duplicate-key or trailing-value JSON before `client_golang` unmarshalling, with focused tests for ambiguous success/error envelopes and oversized parsed bodies. |
| 2026-05-30 | jindyzhao | Docker Provider output archive strictness hardened locally: sandbox `CopyFromContainer` output now rejects nested, traversal, symlink, and hardlink archive members before accepting `output.json`, preserving the runtime-neutral file-contract boundary. |
| 2026-05-30 | jindyzhao | Go toolchain YAML parser hardening landed locally: `make go-toolchain-check` now parses `.golangci.yml` and workflow YAML as single-document config without duplicate keys, anchors, aliases, or merge keys before reading gate-owned version metadata, without treating unrelated GitHub Actions / golangci-lint fields as gate failures. |
