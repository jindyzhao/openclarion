name: ci

on:
  pull_request:
  push:
    branches: [main]

jobs:
  docs-hygiene:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check non-English literals
        run: bash scripts/check_no_non_english_chars.sh
