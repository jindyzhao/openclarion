// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/openclarion/openclarion/internal/persistence/ent/alertgroup"
	"github.com/openclarion/openclarion/internal/persistence/ent/evidencesnapshot"
)

// EvidenceSnapshot is the model entity for the EvidenceSnapshot schema.
type EvidenceSnapshot struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// FK to alert_groups.id; the grouping result this snapshot was materialised from
	AlertGroupID int `json:"alert_group_id,omitempty"`
	// sha256 hex of canonical(payload); per-group idempotency key (UNIQUE per alert_group_id, see Indexes)
	Digest string `json:"digest,omitempty"`
	// full evidence jsonb (labels, metrics, topology, runbook excerpts); immutable once persisted
	Payload json.RawMessage `json:"payload,omitempty"`
	// jsonb map provider->{status,fetched_at,error}; status in "ok"|"partial"|"failed"
	Provenance json.RawMessage `json:"provenance,omitempty"`
	// row-level status: "complete" | "partial" | "failed"; worst of all provider statuses
	Status string `json:"status,omitempty"`
	// dotted-path keys requested but not produced; populated when status=partial
	MissingFields []string `json:"missing_fields,omitempty"`
	// temporal workflow id that materialised this snapshot; nullable for manually-seeded test rows
	CreatedByWorkflow string `json:"created_by_workflow,omitempty"`
	// server-side materialisation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the EvidenceSnapshotQuery when eager-loading is set.
	Edges        EvidenceSnapshotEdges `json:"edges"`
	selectValues sql.SelectValues
}

// EvidenceSnapshotEdges holds the relations/edges for other nodes in the graph.
type EvidenceSnapshotEdges struct {
	// Group holds the value of the group edge.
	Group *AlertGroup `json:"group,omitempty"`
	// Tasks holds the value of the tasks edge.
	Tasks []*DiagnosisTask `json:"tasks,omitempty"`
	// SubReports holds the value of the sub_reports edge.
	SubReports []*SubReport `json:"sub_reports,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [3]bool
}

// GroupOrErr returns the Group value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e EvidenceSnapshotEdges) GroupOrErr() (*AlertGroup, error) {
	if e.Group != nil {
		return e.Group, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: alertgroup.Label}
	}
	return nil, &NotLoadedError{edge: "group"}
}

// TasksOrErr returns the Tasks value or an error if the edge
// was not loaded in eager-loading.
func (e EvidenceSnapshotEdges) TasksOrErr() ([]*DiagnosisTask, error) {
	if e.loadedTypes[1] {
		return e.Tasks, nil
	}
	return nil, &NotLoadedError{edge: "tasks"}
}

// SubReportsOrErr returns the SubReports value or an error if the edge
// was not loaded in eager-loading.
func (e EvidenceSnapshotEdges) SubReportsOrErr() ([]*SubReport, error) {
	if e.loadedTypes[2] {
		return e.SubReports, nil
	}
	return nil, &NotLoadedError{edge: "sub_reports"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*EvidenceSnapshot) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case evidencesnapshot.FieldPayload, evidencesnapshot.FieldProvenance, evidencesnapshot.FieldMissingFields:
			values[i] = new([]byte)
		case evidencesnapshot.FieldID, evidencesnapshot.FieldAlertGroupID:
			values[i] = new(sql.NullInt64)
		case evidencesnapshot.FieldDigest, evidencesnapshot.FieldStatus, evidencesnapshot.FieldCreatedByWorkflow:
			values[i] = new(sql.NullString)
		case evidencesnapshot.FieldCreatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the EvidenceSnapshot fields.
func (_m *EvidenceSnapshot) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case evidencesnapshot.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case evidencesnapshot.FieldAlertGroupID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field alert_group_id", values[i])
			} else if value.Valid {
				_m.AlertGroupID = int(value.Int64)
			}
		case evidencesnapshot.FieldDigest:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field digest", values[i])
			} else if value.Valid {
				_m.Digest = value.String
			}
		case evidencesnapshot.FieldPayload:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field payload", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Payload); err != nil {
					return fmt.Errorf("unmarshal field payload: %w", err)
				}
			}
		case evidencesnapshot.FieldProvenance:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field provenance", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Provenance); err != nil {
					return fmt.Errorf("unmarshal field provenance: %w", err)
				}
			}
		case evidencesnapshot.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case evidencesnapshot.FieldMissingFields:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field missing_fields", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.MissingFields); err != nil {
					return fmt.Errorf("unmarshal field missing_fields: %w", err)
				}
			}
		case evidencesnapshot.FieldCreatedByWorkflow:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field created_by_workflow", values[i])
			} else if value.Valid {
				_m.CreatedByWorkflow = value.String
			}
		case evidencesnapshot.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the EvidenceSnapshot.
// This includes values selected through modifiers, order, etc.
func (_m *EvidenceSnapshot) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryGroup queries the "group" edge of the EvidenceSnapshot entity.
func (_m *EvidenceSnapshot) QueryGroup() *AlertGroupQuery {
	return NewEvidenceSnapshotClient(_m.config).QueryGroup(_m)
}

// QueryTasks queries the "tasks" edge of the EvidenceSnapshot entity.
func (_m *EvidenceSnapshot) QueryTasks() *DiagnosisTaskQuery {
	return NewEvidenceSnapshotClient(_m.config).QueryTasks(_m)
}

// QuerySubReports queries the "sub_reports" edge of the EvidenceSnapshot entity.
func (_m *EvidenceSnapshot) QuerySubReports() *SubReportQuery {
	return NewEvidenceSnapshotClient(_m.config).QuerySubReports(_m)
}

// Update returns a builder for updating this EvidenceSnapshot.
// Note that you need to call EvidenceSnapshot.Unwrap() before calling this method if this EvidenceSnapshot
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *EvidenceSnapshot) Update() *EvidenceSnapshotUpdateOne {
	return NewEvidenceSnapshotClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the EvidenceSnapshot entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *EvidenceSnapshot) Unwrap() *EvidenceSnapshot {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: EvidenceSnapshot is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *EvidenceSnapshot) String() string {
	var builder strings.Builder
	builder.WriteString("EvidenceSnapshot(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("alert_group_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.AlertGroupID))
	builder.WriteString(", ")
	builder.WriteString("digest=")
	builder.WriteString(_m.Digest)
	builder.WriteString(", ")
	builder.WriteString("payload=")
	builder.WriteString(fmt.Sprintf("%v", _m.Payload))
	builder.WriteString(", ")
	builder.WriteString("provenance=")
	builder.WriteString(fmt.Sprintf("%v", _m.Provenance))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("missing_fields=")
	builder.WriteString(fmt.Sprintf("%v", _m.MissingFields))
	builder.WriteString(", ")
	builder.WriteString("created_by_workflow=")
	builder.WriteString(_m.CreatedByWorkflow)
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// EvidenceSnapshots is a parsable slice of EvidenceSnapshot.
type EvidenceSnapshots []*EvidenceSnapshot
