// Code generated by ent, DO NOT EDIT.

package ent

import (
	"encoding/json"
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/openclarion/openclarion/internal/persistence/ent/alertsourceprofile"
)

// AlertSourceProfile is the model entity for the AlertSourceProfile schema.
type AlertSourceProfile struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// operator-facing unique display name
	Name string `json:"name,omitempty"`
	// "prometheus" | "alertmanager"
	Kind string `json:"kind,omitempty"`
	// http(s) base URL without userinfo, query, or fragment
	BaseURL string `json:"base_url,omitempty"`
	// "none" | "bearer"; secret material is represented only by secret_ref
	AuthMode string `json:"auth_mode,omitempty"`
	// deployment-managed secret reference, never the secret value
	SecretRef string `json:"secret_ref,omitempty"`
	// whether operators have enabled this source for policy binding
	Enabled bool `json:"enabled,omitempty"`
	// operator labels for ownership, environment, or routing metadata
	Labels map[string]string `json:"labels,omitempty"`
	// server-side profile creation timestamp
	CreatedAt time.Time `json:"created_at,omitempty"`
	// server-side last-mutation timestamp
	UpdatedAt    time.Time `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*AlertSourceProfile) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case alertsourceprofile.FieldLabels:
			values[i] = new([]byte)
		case alertsourceprofile.FieldEnabled:
			values[i] = new(sql.NullBool)
		case alertsourceprofile.FieldID:
			values[i] = new(sql.NullInt64)
		case alertsourceprofile.FieldName, alertsourceprofile.FieldKind, alertsourceprofile.FieldBaseURL, alertsourceprofile.FieldAuthMode, alertsourceprofile.FieldSecretRef:
			values[i] = new(sql.NullString)
		case alertsourceprofile.FieldCreatedAt, alertsourceprofile.FieldUpdatedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the AlertSourceProfile fields.
func (_m *AlertSourceProfile) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case alertsourceprofile.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case alertsourceprofile.FieldName:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field name", values[i])
			} else if value.Valid {
				_m.Name = value.String
			}
		case alertsourceprofile.FieldKind:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field kind", values[i])
			} else if value.Valid {
				_m.Kind = value.String
			}
		case alertsourceprofile.FieldBaseURL:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field base_url", values[i])
			} else if value.Valid {
				_m.BaseURL = value.String
			}
		case alertsourceprofile.FieldAuthMode:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field auth_mode", values[i])
			} else if value.Valid {
				_m.AuthMode = value.String
			}
		case alertsourceprofile.FieldSecretRef:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field secret_ref", values[i])
			} else if value.Valid {
				_m.SecretRef = value.String
			}
		case alertsourceprofile.FieldEnabled:
			if value, ok := values[i].(*sql.NullBool); !ok {
				return fmt.Errorf("unexpected type %T for field enabled", values[i])
			} else if value.Valid {
				_m.Enabled = value.Bool
			}
		case alertsourceprofile.FieldLabels:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field labels", values[i])
			} else if value != nil && len(*value) > 0 {
				if err := json.Unmarshal(*value, &_m.Labels); err != nil {
					return fmt.Errorf("unmarshal field labels: %w", err)
				}
			}
		case alertsourceprofile.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case alertsourceprofile.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the AlertSourceProfile.
// This includes values selected through modifiers, order, etc.
func (_m *AlertSourceProfile) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this AlertSourceProfile.
// Note that you need to call AlertSourceProfile.Unwrap() before calling this method if this AlertSourceProfile
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *AlertSourceProfile) Update() *AlertSourceProfileUpdateOne {
	return NewAlertSourceProfileClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the AlertSourceProfile entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *AlertSourceProfile) Unwrap() *AlertSourceProfile {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: AlertSourceProfile is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *AlertSourceProfile) String() string {
	var builder strings.Builder
	builder.WriteString("AlertSourceProfile(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("name=")
	builder.WriteString(_m.Name)
	builder.WriteString(", ")
	builder.WriteString("kind=")
	builder.WriteString(_m.Kind)
	builder.WriteString(", ")
	builder.WriteString("base_url=")
	builder.WriteString(_m.BaseURL)
	builder.WriteString(", ")
	builder.WriteString("auth_mode=")
	builder.WriteString(_m.AuthMode)
	builder.WriteString(", ")
	builder.WriteString("secret_ref=")
	builder.WriteString(_m.SecretRef)
	builder.WriteString(", ")
	builder.WriteString("enabled=")
	builder.WriteString(fmt.Sprintf("%v", _m.Enabled))
	builder.WriteString(", ")
	builder.WriteString("labels=")
	builder.WriteString(fmt.Sprintf("%v", _m.Labels))
	builder.WriteString(", ")
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// AlertSourceProfiles is a parsable slice of AlertSourceProfile.
type AlertSourceProfiles []*AlertSourceProfile
